# codebase-as-structure — state at D-064

*Written 2026-09-13 at the close of the fifth unpointed round; a readable copy is published at https://claude.ai/code/artifact/1d6d88e4-38fd-48b6-8792-450847c16e7a. Every number below is read from the repository at commit `577cd3d`; every claim about what a reading found cites its decision. The log (`DECISIONS.md`, D-001 → D-064) is the record; this is the map of it.*

## 1. Where it stands

| | |
|---|---|
| Substrate (`repo-substrate`) | 0.4.1; gate `179d8acb7b0c` (K 25, eps 0.01, delta 0.05, M 3; D-033) over four reference repositories — eslint, typeorm, uluops-registry-api, mcp-secure-server |
| Gate verdict | 21 signals `asserted`, 2 `unvalidated` (the predictive indices `bug_pressure_index`, `change_pressure_index`), 2 `untested` (D-015) |
| Rulesets | maintainability 0.2.10 (8 features), onboarding 0.2.6 (5 features); loader refuses record words, quantile words, extra "high"s, sibling names, a pNN a name leaves out, a caveat_case without a caveat |
| Brief | 0.30.0 — the code-rendered register page; no model in the path since D-049 |
| Tests | 151, gating every commit |
| Decisions | 64 (D-001 → D-064), each with **Assumes / Breaks if** |
| Tracker (`codebase-as-structure`) | 47 runs; every finding statused against its commit |
| Pages | 35 tags of four pages each; latest `reports/2026-09-06ai-m3x/` |
| Milestones (D-003) | M1 complete (D-015). M2 complete (D-020). M3 *answered, not delivered* (D-051): the model was the milestone; a register is what shipped. Phase 1 time-lapse built (D-021–D-023). Phase 3 (corpus) not begun; its sentence type is decided (D-059) |

The page a reader gets today: a bold rule (a position is not a claim about condition; a `>= p90` row holds a tenth of the rooms or more where they tie, and the rooms column says how many are tied; no count compares across repositories; a room is a source file outside the test convention with computed signals — N of M files), a counts paragraph, a six-line legend, a thirteen-row feature table whose column headers carry their own legends, every room at the most positions by path with size and importers, a shared-rooms matrix in which features drawing one set share one row, an excluded-marks line, the stance, provenance. Every cell is a field of the facts sheet. Every fixed text has a test against the sheet it is rendered from — on a fixture, which the header says.

## 2. The arc since the last handoff (2026-09-10)

**The cut (D-049).** Eleven hostile seatings found the model-written reading reciting the register; everything it could add was a field. Alex chose the cut. The brief became the register page, rendered by code.

**The pointed series (D-050 → D-057).** Eight seatings, each cast to the previous decision's "Breaks if", each finding the next layer of the page and closing it with a structure: distinct sets as the unit; "3 of 4" cells; records derived from declared inputs; the three-valued containment marker; a scope named by its manifest; a roster so an unfired feature keeps its row; the one-edge-set fact; citation grammar; caveats with cases. The numbers were clean from D-052 on. The findings narrowed to one shape — a definition landing one step from the text it governs.

**Forecast cycle 3 (D-058).** `cascade-depth-analyzer` and `perverse-outcome-detector` on the Breaks-if ledger found the register clean and the practice unfalsifiable at every joint: D-035's stop rule had two horns and the log counted one while the other fired eighteen of eighteen; the tests never read a page; the seat was cast to the clause and the clause to the newest cell; the persona's pole flips on one finding; the seam classification was made by the seat's operator; D-004 Q3 is analytic. Four decisions about the series' identity were put to Alex.

**Alex delegated (D-059).** The decisions were made to take the pen out of the operator's hand at the moment of reading: the stop rule retired and replaced by a null result the operator cannot write (an *unpointed* seating returning STEELMAN_RESILIENT); the breaks-if answered after a reading that did not know it; Phase 3's sentence type fixed under Q3; two seatings ordered — a control and, for the first time, a skimmer.

**The unpointed rounds (D-060 → D-064).** Five controls with rivals cast away from recent work — a hotspot vendor, an eslint core maintainer, a TypeORM release manager, an onboarding-doc author, a security reviewer — found twenty-four layers, none at a pointed seam, and no seating has returned STEELMAN_RESILIENT. Five skimmer readings found that every defence the page had was prose the sixty-second reader skips, and moved the defences into the cells. One Rams audit (Alex's lens for one round) found the accretion and cut it. The fifth control reached below the page into the substrate.

## 3. What the process discovered

These are the findings that generalise beyond the cell they were found on. Each is a decision stratum now; each cites where it fired.

1. **A page can be falsifiable in every cell and the practice around it unfalsifiable at every joint** (D-058). The stop rule that could not fire, the persona whose pole flips on one traceable finding, the clause written to the cell just built with its remedy attached, the disposal rule that is a theorem — all operated by one agent. The register's discipline about fields did not extend to the practice's discipline about itself. The remedy was structural, not another test: change who holds the counter (D-059).

2. **Pointing steers yield; it does not produce it** (D-060, D-061). The first unpointed control found five new layers and none of the pointed seam. The seam pattern of D-055–D-057 was partly an artefact of where the seat was aimed. The seat's yield is the page's; the clause's job is the parent's own check, after the reading.

3. **The remaining shape is one: a sentence whose scope differs from the field beside it** (D-061 → D-063). A rule over-covering ("each row states its share" while conjunctive rows printed no cutoff), a caveat under-covering (a disqualifier the predicate reads more widely than the caveat says), a cutoff withheld, a definition missing. D-042/D-043's move closes each: generate the sentence from the field, or print the field and cut the sentence. A sentence written by the parent the day before was false beside its row (D-061: "the top 10% by construction" beside 83 of 473 with 80 tied at the cutoff) — tested against the predicate string, not the count.

4. **A mechanism disclosed in general terms hides the instance where it dominates** (D-062). "Ties carry the row past a tenth" beside a row where all 70 rooms sat at one cutoff (a mass commit); "one edge set read twice" beside a room whose 1374 importers were all test files; "pooled" beside a graph in which one edge of 11,689 crosses a package scope. The device — count the instance on the row — existed ("this case: 12 of 26 here") and had been applied to two rows. Repository-independent disclosure, D-042's rule for caveats, is right for what a predicate reads and wrong for what this repository is; the page now carries both.

5. **Defences written as prose lose to nouns and table shape** (D-060 → D-064, five skimmer readings). The sixty-second reader reads column two (the feature name) and column four (the count) and the one table with file paths. A bold rule stated once is read and bounces against thirteen consequence nouns and a descending list. A bulleted legend's heads are read and its bodies are not. What lands is what sits in the cell the reader reads: the position as the first column, the tie count beside the count it qualifies, the ◌ on the row, the column header's own legend. The lexicon itself — `crack`, `dark_room`, `flooded_basement` — is the cost of the metaphor; Alex kept the names (D-062) and the stance discloses the norm.

6. **Iterative hardening accretes** (D-062, the Rams audit). Twelve seatings' worth of defensive sentences had stacked on a register whose fields were clean: ~28% of the fixed text was repetition, dead defence or the tool's own record; D-043's rule ("the note is generated from the constants") generated one sentence per seating. The reductive audit's calls were checkable one by one against the log, which is what made the lens worth its seat.

7. **D-004 Q3 is analytic, and that has a consequence for Phase 3** (D-058, D-059). "A position is not a claim about condition" disposes of every sentence a table cannot carry — a cause, a comparison, a recommendation — and the corpus's output is comparisons. Decided: a corpus-relative claim is a field with a corpus denominator ("dark_room share 0.19; corpus decile 9 of 10, n = 100") — a position in the corpus's distribution — never a verdict. Q3 survives Phase 3 as a rule about sentences.

8. **The clock's oldest tick is often one commit** (D-062, D-063). On typeorm all 70 dark rooms share one `last_touched_days` (a 2022 mass commit); on eslint 80 of 83. A `>= p90` row is a tenth *or more*; the tie is now a count in the rooms column. On registry the corridor's "at or above the median" fan-out resolves to 1 — 95 of 267 rooms import nothing — so a corridor is a hub that imports anything; every ranked term now states the value its rank resolved to here.

9. **The import graph holds the test files, and the resolver has a hole** (D-057, D-064). PageRank runs over all nodes including test files, so a test import raises `centrality` and `test_fan_in` together; on typeorm 84% of the graph's nodes are tests. And on mcp-secure-server all 34 unresolved imports are test files importing `src/security/` through the `@/` tsconfig alias — static, resolvable, unresolved — so every test-graph cell over `src` is a lower bound and a policy file the page calls "unreinforced" has two tests. eslint's 20 unresolved are 19 from tests. **D-006's breaks-if ("alias-heavy repos produce confident mis-resolution") has fired on two of four reference repositories.** The page now states the distribution; the fix is the substrate's.

10. **The self-relative calibration is the deepest assumption and the least-read sentence** (D-060 → D-064). Every pNN ranks one repository's rooms, so every repository has ~10% at each ranked position, and the four briefs sit side by side in `reports/`. The bold rule forbids the comparison in its least-read clause. Per-package calibration (mapper §7 Q7) has its sizes on every page — eslint pools 473 rooms of which 435 sit in the root scope; mcp-secure-server 66 of 202 with fourteen `cookbook/` scopes, every one under the 30-room floor.

## 4. What remains

### For Alex to decide

1. **Substrate 0.5.0 — alias resolution.** Pass the repository's `tsconfig` `paths`/`baseUrl` to dependency-cruiser (D-006 Q8). It changes `fan_in`, `test_fan_in`, `centrality` and every derived index on two of four reference repositories, therefore the gate: a re-extraction of four repositories, a gate re-run (~7 min), a new fingerprint, four regenerated pages. The first change the seatings have forced below the mapper since D-033. **Recommendation: do it next.** The page's test-graph cells are lower bounds until it lands, and the page says so.
2. **Phase 3 — the corpus.** Form decided (D-059: a field with a corpus denominator); content open (mapper §7 Q8: which fields first, the corpus n floor, whether a decorative feature gets a corpus position). Per-package calibration (Q7) is the same door. Recommendation: after 0.5.0, not before — the corpus should be extracted under the corrected resolver.
3. **The seat's continuation.** D-059's rule stands: a version bump seats an unpointed reader; the seat ends when one returns STEELMAN_RESILIENT. Five unpointed controls have returned HATER_EXPOSED 87–90 with four to six findings each, all in the sentence layer, all closed. Recommendation: continue under the rule; the fifth round's finding was the substrate's, which the rule was written to reach.

### Queued, small

- Validation spec §3's glossary still says `asserted` is confirmed by "a cross-modal check"; its own §2.4.2 puts every signal on the gate in G1, G2 or G4 (D-061). A spec correction.
- A partition relation the relation column cannot draw: on registry corridor = hub \ leaf_utility exactly (13 + 14 = 27, corridor ∩ leaf_utility = 0) (D-063).
- Configuration files as rooms: 14 vitest/eslint configs on mcp-secure-server supply six of `flooded_basement`'s 37; a file-name classifier the population rule does not have (D-064).
- A tier of one room reads as "the winner" and the caption cannot land against a set of one (D-062); the alternatives were worse for the reader who used the table.
- The viewer (artifact `fa3e321e…`) does not carry the matrix or the tier table; `most_marked_rooms` stays on the sheet for it and renders only on a sheet without `top_tier`.

### Direction candidates, unchanged since D-049

Registry layer-geometry over-budget as the trigger for Q7; a user-selectable architectural style as a rendering skin (`style.json`, never built — D-051); Blender components from `skeleton.json`; a breaks-if ledger parser (the extraction for forecast cycle 3 is `docs/breaks-if-ledger-2026-09-12.md`); a substrate contract independent of git.

## 5. Limits stated plainly

- **No outside reader yet.** Twenty-three hostile seatings, five skimmer models, one Rams audit, three forecast cycles — every reader has been an AI persona or Alex. The skimmer readings are the only evidence about the page's actual audience, and they are a model of one.
- **One agent holds the pen at most joints.** D-059 moved the counter out of its hand at the moment of reading and the breaks-if to after it; the cast is still the operator's. The control that matters most is the one whose rival has no interest in any recent cell, and that cast is a judgment.
- **The instrument beneath the register has a known limit** (item 9) and a known assumption (self-relative calibration, item 10). Both are on the page; neither is closed.
- **The log is 47,000 words** and a build input for two tests; larger than one read. The ledger extraction is what fits.

## 6. Pointers

- Read first: `DECISIONS.md` D-058 → D-064; `docs/handoff-2026-09-10.md` (state, series, addenda).
- Regenerate (no model): `uv run python scripts/regenerate_readings.py --validation out/validation-k25c/validation.json --tag <tag> --skip-timelapse`.
- Gate re-run (~7 min; always the tuned config): `uv run substrate-validate run --repo reference/typeorm --repo ~/uluops/misc/npm-packages/mcp-secure-server --tuning-repo ~/uluops/uluops-registry-api --tuning-repo reference/eslint --config config/tuned.toml --out out/validation-<tag> --vconfig config/validation-k25.toml --cache out/cache`.
- A seating (D-059): unpointed — no clause quoted, no cell named, a rival cast away from the last four decisions' cells; the hostile-reader has Read/Grep/Glob and no shell; recount every number it relies on before acting; the breaks-if answered afterward; save the run; status every issue against the D-number and commit.

## Addendum, 2026-09-13 (later): D-065 — decision 1 taken; substrate 0.5.0

Alex took §4's first decision. The work found that the resolver was already given the tsconfig; the alias failure on mcp-secure-server was the substrate's own JSONC stripper reading the `/*` in `"@/*": ["./src/*"]` as a comment opener, recorded as `tsconfig_malformed` on the substrate and rendered nowhere. §1's row "Substrate 0.4.1; gate `179d8acb7b0c`" is now **0.5.0; gate `115c4483645a`** (`out/validation-k25d/`, archived `reports/2026-09-13-m1c-k25d/`); the gate verdict is **20 asserted** — `recent_commit_share` fell to untested on the registry's seven new commits (0.001 of headroom at k25c), not on the fix; every graph signal held. Brief **0.31.0**, pages `reports/2026-09-13-m3x/`, **154 tests**, 65 decisions. §3's discovery ten ("the fifth round reached the substrate") gains its correction: the mechanism was one layer lower than D-064 placed it, and "two of four" was one of four — eslint's and typeorm's unresolved imports are dangling relative paths no resolver places. §4's decision 2 (Phase 3) is now unblocked by decision 1; decision 3 (the seat) stands, and 0.31.0's bump owes an unpointed control on mcp-secure-server — the page whose cells moved — and a skimmer. The regenerate recipe reads `out/validation-k25d/validation.json`. Time-lapse frames are not yet regenerated under 0.5.0. Everything else in D-065.

## Addendum, 2026-09-13 (evening): D-066 — the sixth unpointed round; substrate 0.6.0

The round 0.31.0's bump owed. The control (the cookbook's author, run 48; HATER_EXPOSED 89) found the next classification below the page: the repository's own package name counted as *external* — 19 imports on mcp-secure-server, 228 on typeorm, 11 on eslint — so substrate **0.6.0** resolves a bare specifier naming a declared package to that package's entry (both instruments), gate **`661f93333b65`** (`out/validation-k25e/`, archived `reports/2026-09-13-m1c-k25e/`), still 20 asserted, no verdict moved. The skimmer on registry (run 49) returned **ALIGNED 84**, the first in the series. Brief **0.32.0** (maintainability 0.2.11, onboarding 0.2.7; pages `reports/2026-09-13b-m3x/`; 155 tests): a ranked row states the next value beyond its cutoff and the rooms there; a wing of many scopes says how many hold the feature; the population sentence names its denominator; a caveat's count sits beside its clause. §4 gains one decision for Alex: **file kinds as a substrate fact** (config / migration / placeholder — three instances across D-064 and D-066); recommendation: a G1 flag, each ruleset chooses. Regenerate reads `out/validation-k25e/validation.json`.

## Addendum, 2026-09-13 (night): D-067 — file kinds, Alex's call taken; substrate 0.7.0; the pin

Alex agreed with D-066's recommendation. Substrate **0.7.0**: `file_kind` (config / migration / placeholder / source) by declared conventions, three G1 flags, regexes in the fingerprint; the ruleset chooses (`exclude_kinds`; maintainability 0.3.0 and onboarding 0.3.0 exclude all three; an overlay must match its base); every pNN ranks the reduced population and the page's population rule counts what was removed (mcp 16, registry 61, typeorm 9, eslint 6). Gate **`bae5f62948e7`** (`out/validation-k25f/`, archived `reports/2026-09-13-m1c-k25f/`): 22 of 28 asserted — `is_config` asserted; `is_migration` / `is_placeholder` untested, constant on two repositories each (D-030's rule); `recent_commit_share` flapped back to asserted on one more registry commit — D-065's clause 3 fired — so **`--repo path@sha`** is built: the gate recipe pins the two live repositories. Brief **0.33.0**, pages `reports/2026-09-13c-m3x/`, **159 tests**, 67 decisions. §4: decision 2 (Phase 3) and 3 (the seat) stand; the file-kind decision is taken.

## Addendum, 2026-09-13 (night): D-068 — the seventh unpointed round; substrate 0.8.0; the first pinned gate

Control (the `packages/codemod` maintainer on typeorm 0.33.0, run 50; HATER_EXPOSED 91): five sentences that say less than their fields — the legend's cross-scope and package-name counts with no test-file split (214 of 219 are codemod fixtures), a two-scope wing counted rather than named, the centrality illustration as the registry's literal, "the test convention" undefined, the reinforcement caveats short the run-time mechanism. Skimmer (mcp 0.33.0, run 51): **ALIGNED 86**, the second in a row; the caveat counts in the eighth column; "excluded" meaning two things after D-067. Substrate **0.8.0** carries the package-name samples, the declared names and the effective config; gate **`e28401778e67`** (`out/validation-k25g/`, archived `reports/2026-09-13-m1c-k25g/`), run **with the pin** — all four shas held, 22 of 28 asserted, nothing moved. Brief **0.34.0**, maintainability 0.3.1, pages `reports/2026-09-13d-m3x/`, **160 tests**, 68 decisions. Regenerate reads `out/validation-k25g/validation.json`. Next round owed on eslint or the registry.

## Addendum, 2026-09-13 (late): D-069 — the eighth unpointed round; substrate 0.9.0; the clock's convention

Control (a core-rules maintainer on eslint 0.34.0, run 52; HATER_EXPOSED 90): the dark-room cutoff is the Prettier commit the repository lists in `.git-blame-ignore-revs` — the fourth tree-declared convention the substrate did not read (tsconfig paths, package names, file kinds, now blame-ignore) — and three more eighth-column sentences (a ranked term's tie inside a conjunction; an importer sum's concentration; a gap in exponent notation). Skimmer (typeorm 0.34.0, run 53): **ALIGNED 88**, the third in a row. Substrate **0.9.0**: `last_touch_commit_files`, `last_touch_blame_ignored`; gate **`84bae11b50a7`** (pinned; `out/validation-k25h/`, archived `reports/2026-09-13-m1c-k25h/`), 23 of 30 asserted. Brief **0.35.0**, maintainability 0.3.2 (the clock rows carry a caveat with a counted case; `extra_caveats` in the ruleset grammar so `flooded_basement` carries the clock's too — on eslint 73 of 73), pages `reports/2026-09-13f-m3x/`, **161 tests**, 69 decisions. Time-lapse frames regenerating under this gate. Next round owed on the registry.

## State at the close of 2026-09-13 (D-069)

The morning's §1 table, re-read at the end of the day. Every number is read from the repository at commit `28a1af8`.

| | |
|---|---|
| Substrate (`repo-substrate`) | **0.9.0**; gate **`84bae11b50a7`** (K 25, eps 0.01, delta 0.05, M 3), the live repositories pinned (`mcp-secure-server@5348b3ef614d`, `uluops-registry-api@e947c5b67542`); `out/validation-k25h/`, archived `reports/2026-09-13-m1c-k25h/` |
| Gate verdict | **23 of 30 asserted**; the five new G1 facts since morning — `tsconfig_aliases`, `package_name_imports`, `file_kind`'s three flags, `last_touch_commit_files`, `last_touch_blame_ignored` — of which the three flags constant on two or more repositories read untested by D-030's rule |
| Rulesets | maintainability **0.3.2** (kinds excluded; clock caveats with a counted case; `extra_caveats` grammar), onboarding **0.3.0** |
| Brief | **0.35.0** |
| Tests | **161** |
| Decisions | 69 (D-065 → D-069 today) |
| Tracker | 53 runs (48–53 today); every issue statused against its commit |
| Pages | `reports/2026-09-13f-m3x/`; time-lapse frames current under this gate (`reports/2026-09-13f-phase1x/`), after standing at 0.4.1 since D-033 |
| Seat | eight unpointed controls; the last five with the field layer holding under full recount; the last three skimmers ALIGNED |

**What the day found, in one line each.** The alias failure was our JSONC stripper, not the resolver (D-065). The repository's own package name was external on all four repositories (D-066). File kinds became a fact and a ruleset choice (D-067, Alex's call). The legend's counts lacked the split every row carried; a two-scope wing was counted not named; the centrality illustration was another repository's literal (D-068). The clock does not read `.git-blame-ignore-revs`; a ranked term inside a conjunction had no tie (D-069). Four of the five reached below the page, and each was a convention the tree declares that the substrate had treated as data.

**What remains for Alex.** Phase 3 (the corpus; form decided D-059, content open — mapper §7 Q8); the seat's continuation under D-059 (never yet returned STEELMAN_RESILIENT; 0.35.0's bump owes a control on the registry and a skimmer on eslint). Recorded and not built: the eighth column's shape (a caveat table under the register), the excluded kinds' own positions, reading `.git-blame-ignore-revs` into the clock itself, a `constant_on` note on the report for flags that are true constants of a tree, Q7 per-scope calibration.

## Addendum, 2026-09-16: D-070 — the ninth unpointed round; substrate 0.10.0; the clause that fired

Control (the `src/utils/` author on registry 0.35.0, run 54; HATER_EXPOSED 90, six substance, **convergence: yes** — the seat's first): D-069's own breaks-if clause 1 fired verbatim — the registry has no `.git-blame-ignore-revs`, and the clock caveat's counted case printed "0 of 34 here" as this repository's count beside "18 tied at the cutoff — one commit of 131 files". Five sentence-layer findings with it: the stance generic where the system spec names the case and `singleton.ts` is it; "config" a word wider than its regex (`knexfile.ts`); the largest parent withheld under a third; a containee drawing one set listed twice; "line count" is non-blank. Skimmer (eslint 0.35.0, run 55): **DRIFTED 82**, the first in four, on the reader's override — the ◌ glyph as the exclusion's only carrier where a skimmer reads; the prompt had named the regions its reader skips (logged). Substrate **0.10.0**: `blame_ignore_revs_file` in the config (fingerprinted), `summary.blame_ignore_revs = {present, listed}`; gate **`177c129be9b5`** (pinned, `out/validation-k25i/`, archived `reports/2026-09-16-m1c-k25i/`), 23 of 30 asserted, nothing moved. Brief **0.36.0**, maintainability **0.3.3**, pages `reports/2026-09-16-m3x/`, **163 tests**, 70 decisions; tracker runs 54–55, 19 issues statused. §1's table: substrate 0.10.0, gate `177c129be9b5`, brief 0.36.0, 163 tests, 55 runs. §4 gains one for Alex: tool-named configs (`knexfile.ts`) as a declared name list, or not — the page now states the convention either way. The next round is owed on mcp-secure-server or typeorm, the skimmer's prompt back to D-060's form. Regenerate reads `out/validation-k25i/validation.json`; the time-lapse frames stand at `84bae11b50a7` (0.9.0) — the 0.10.0 change is a summary field and a config name, and no frame value moves under it; regenerate when the next substrate change moves a value.

## Addendum, 2026-09-16 (later): D-071 — the tenth unpointed round; brief 0.37.0

Control (the `src/security/transport/` maintainer on mcp-secure-server 0.36.0, run 56; HATER_EXPOSED 85, four substance, **convergence: yes** — the second in a row) and skimmer (registry 0.36.0, plain prompt, run 57; **ALIGNED 82**) both found the cell D-070 built: the under-a-third parent rendered without the tie the sheet records (mcp's `package_entry` a sixteen-way tie shown as a superlative; registry's `lit_room` a three-way). The control's new layer: `load_index >= p90 (here 0.77128)` with the blend's inputs and weights nowhere on the page. Brief **0.37.0**: the tie renders on both branches (past three partners, counted); the legend states every blend the page reads with its tuned weights; the sheet's unit text updated. No substrate or gate change (gate stays `177c129be9b5`). Pages `reports/2026-09-16b-m3x/`, **164 tests**, 71 decisions, 57 runs. D-070's clause 2 fired in part (the ◌ row furnished like a diagnostic row); the ◌ rows' own table is recorded as the next form, after one more plain-prompt skimmer on a page with a fired ◌ row. Next round: control on typeorm, skimmer on eslint or typeorm.
