# The "Breaks if" ledger, D-034 → D-057 (extracted 2026-09-12 for forecast cycle 3)

Each entry: the decision's title, its **Assumes** sentence and its **Breaks if** clause(s), verbatim from DECISIONS.md. The clauses are the system's own pre-registered refutation conditions; several later entries record which earlier clauses fired and on what.


## D-034 · 2026-09-06 · Forecast cycle 2: the gate binds to the substrate it was earned under; the skeleton says where its statuses came from; the legend says what a name means

**Assumes.** The reference set's statuses are the right thing to carry onto a new repository of the same config — the gate is an instrument calibration, not a per-repository audit.

**Breaks if.** A signal's status turns out to depend on repository kind (a `coupled` signal stable on four libraries and unstable on a monorepo): then the reference set on the skeleton is the reader's warning and Phase 3's corpus is the fix.


## D-035 · 2026-09-06 · Theory → practice cycle 2: a check performed once and thereafter presented as performed

**Assumes.** One hostile reading per version is enough to catch a grammar drifting toward one model's habits (D-034's C7).

**Breaks if.** Two consecutive readings find nothing the lint missed — then the seat is ceremony and the rule is retired with that evidence; or a reading finds something on every brief — then the lint's authority is the ceremony and D-028's "the lint is the hostile reader" is revisited.


## D-036 · 2026-09-06 · The first seating under D-013 rule 5: the brackets hold, the connective prose does not; brief 0.3.0 reads the sentences between the citations

**Assumes.** The connective-prose rules are the next layer, not the last: the reading named scope, quantity and sample as the classes, and R9–R11 cover the instances found, not the classes.

**Breaks if.** The second seating finds the same classes again with new instances the rules do not reach — then the rules are chasing the prose and the register needs a structural change (a brief that is a table with one sentence per row, say) rather than another rule.


## D-037 · 2026-09-06 · The second seating: the overclaims moved out of the verbs into arithmetic and implicature; the sheet is typed rather than the prose tabled

**Assumes.** Three seatings on one repository's brief are comparable (D-013 rule 2); the third is seated on a different repository's brief to test that.

**Breaks if.** The third seating finds the same shape — a true sentence with an unlicensed comparison — through a field the sheet does not carry. Then the sheet is not the fix, the prose is, and the tabular brief is built as a prototype first (the `prototype` skill) to see whether a row per feature reads at all.


## D-038 · 2026-09-06 · The third seating: a nominal closure in the project's own hand, a rule that generated prose, and the tabular brief goes to a prototype

**Assumes.** The tabular page is a rendering of the same facts sheet under the same lint (a row is a sentence with fixed slots); the register survives the change of form.

**Breaks if.** The prototype shows that rows do not read — a reader cannot tell a building from a table of fifteen features — in which case the prose brief stays, with its seatings, and the sheet keeps growing denominators as they are found.


## D-039 · 2026-09-07 · The brief carries a register: the inventory is rendered by code, the prose is the reading

**Assumes.** A reader of the page reads the register first; the prose refers to it rather than restating it.

**Breaks if.** The fourth seating finds the reading restating the register in sentences (the inventory has moved, not shrunk), or finds the register itself carrying an implicature (a column that reads as a ranking) — in which case the register gets a lint of its own, over its cells, and the prose is cut to the stance and the overlap sentences.


## D-040 · 2026-09-07 · The fourth seating: the register is a surface too; its cells get tests, the reading gets R16

**Assumes.** A cell that is a field of the sheet needs no rule; only a *derived* cell (relation remainders, largest directory, cross-profile totals) needs a test, and those are enumerated here.

**Breaks if.** The fifth seating finds a derived cell not on that list carrying a claim, or finds the reading still restating the register through the counts R16 leaves sayable — then R16 widens to feature counts and the reading is cut to the relations, the decorative disclosure and the stance.


*Answer to D-039's clause(s), recorded here:* Both horns fired and a third the decision had not anticipated. (1) *The inventory moved, not shrunk*: about half the reading's words were the rooms and by-wing columns in prose, because no rule refused a number the register already carried. (2) *The reading grew its own overclaims*, three, all of the shape the previous seatings named — "three relations cut the count of distinct sets" (only the identical one merges; nestings leave two sets), "91 rooms … out of 287 diagnostic marks" (a ratio across units R12 did not read), "the graph marks fall on both sides" (an implicature that the age marks do not). (3) *The register's derived cells make claims nothing checks*: the ⊂ cell printed the superset's remainder with the subset's possessive ("5 of its rooms outside" reads as false and contradicts the operator beside it); "dominant directory" printed for every feature with R15's one-third threshold disabled in register mode and ties broken lexicographically in silence (`scaffolding … src/security/utils 4 / 9` with six directories tied at four; `package_entry … 1 / 8`); the directory was the undeclared immediate parent (`src/security/layers` holds nine rooms as a parent and about thirty-one


## D-041 · 2026-09-08 · The fifth seating: a cell's else-branch is where the next overclaim lived; the reading contradicted the register; R16 becomes a binding

**Assumes.** The fallback branches enumerated here are the register's; a new column adds its fallback to the tests when it is added.

**Breaks if.** The sixth seating finds the reading false against the register through a construction R17's lexicon does not name — then the binding is structural (every feature name in the reading carries its register relation alongside, or the reading may not name a feature that has one), not lexical.


*Answer to D-040's clause(s), recorded here:* (a) *A derived cell outside the list carried a claim* — twice, and the reader's diagnosis is the finding: D-040 tested what a cell says when it has something to say and left untested what it says when it does not. `pos = position_name or "no consequence in the name"` printed a verdict about a feature's name from the absence of a position name, never reading `name_implies_consequence`; under 0.6.0 that gap printed "–" and was visible, under 0.7.0 it printed a denial, and D-038's own failure mode (a consequence-implying name whose position name never landed) would now read as "no consequence". `package_entry`, one room, `src` holding one of one, printed "none holds a third" because the six-room size guard was folded into a flag named for a ratio. A singleton's two `⊂` cells carried remainders of 39 and 132 and read as large findings; "1 dark_room rooms"; a parent directory named `scripts` beside the wing `scripts` at a different population. (b) *The reading restated through what R16 left sayable* — and worse, R16 failed in both directions: it withheld the deflating number ("the scripts wing holds import-graph roots [×11]" when eight of eleven are there; "reaches there too" for one ro


## D-042 · 2026-09-08 · The sixth seating: enumerate the explanations, not the numbers; a caveat is a limit on the predicate, never a claim about the repository

**Assumes.** The explanations on the page are now the building line's causes, the register's note, the caveats and the stance; each is either a field, a limit on a predicate, or a fixed sentence the seatings have read.

**Breaks if.** The seventh seating finds the register's note or the stance carrying a claim a repository refutes — then the note is generated from the sheet's fields the way the building line is, and the stance is reduced to the norm's name.


*Answer to D-041's clause(s), recorded here:* Yes, twice, and neither through the model. The building line's gloss — "129 of them fall on the same rooms twice *because two profiles carry one predicate*" — was written by D-040 on a repository whose only identical pair was the shared-predicate kind. On typeorm 70 of the 129 are `dark_room`/`flooded_basement`: one profile, two predicates, a conjunct that excludes nothing, which the register's own relation cell three lines down says. The reading transcribed the gloss and cited `foundation ×59` as warrant for 129 — a positive causal clause false against the register, through no word in R17's lexicon. And the caveat three seatings asked for (D-041 item 5) asserted a fact about the repository — "the package's entry has fan-in and cannot qualify" — false of the one room it was printed beside: `packages/codemod/src/index.ts` is both `import_root` and `package_entry` with fan-in 0. The reading then wrote "the register's caveat holds that…" with the falsifying room in the same sentence. Also: R16 as a binding licensed by-wing restatement on a one-wing building, since naming `src` costs nothing when `src` is 85% of the rooms; "accordingly" asserted a proportionality by a part of speech R1


## D-043 · 2026-09-09 · The seventh seating: the under-covering disclosure; the note is generated from the constants, every position names its record, the stance is a norm's name

**Assumes.** The note now contains no claim a repository could refute, since every clause in it is a constant or a definition.

**Breaks if.** The eighth seating finds the generated note or the record names carrying a claim a repository refutes — then `_RECORDS` is read from the validation document's grounding table rather than hand-listed here; or finds the reading false against the register through a construction neither R17 nor R17b names — then the reading may not name a feature except beside its register relation, verbatim.


*Answer to D-042's clause(s), recorded here:* Not converged, and the reason is a new class the reader named: the *under-covering disclosure*. Six seatings found sentences claiming more than their warrant, each closed by refusing a word, binding a number or testing a cell. This one found the opposite shape — a disclosure whose stated scope is narrower than the surface it covers, with no offending token to refuse. The note said "relations are drawn between sets of three or more rooms" and stated no kind, while the column draws only identity and containment, so `relation to: none` on `import_root` (three rooms shared with `package_entry`) and `crack` (about twenty with `lit_room`) was true only under a definition the note did not state. The note's position gloss named three records — import graph, clock, test graph — and omitted edit pressure and load, the two whose position names most read as condition, which 0.4.0 had named inline per feature. The stance's "drawn as it is" was a fidelity claim the page refutes four ways, and its "load should be reinforced" a norm only the excluded `toothpick_wing` encodes. Also: the reading paraphrased the caveat into a property of rooms — "reading no fan-in and so not an entrance" — false of t


## D-044 · 2026-09-09 · The eighth seating: no new layer; the note was generated from the constants and not from the filter beside them; the stop count stays at zero

**Assumes.** The register's fixed texts are now: the generated note, the position fallbacks, the directory fallbacks, the relation fallback, the building line and the caveats — each either generated from the constants and filters it describes or tested against the rows it sits beside.

**Breaks if.** The ninth seating finds two of those texts contradicting each other on one page — then the register's texts are assembled from one table of clauses, each keyed to the branch that prints it, and a test asserts every branch's clause is in the note.


## D-045 · 2026-09-09 · The paired reading: the reader changed, the page did not; fixes are made as wide as the shape they close

**Assumes.** The revised reader is the instrument from here; runs 17–24 and 25 onward are two series, and D-013 rule 2 compares within each.

**Breaks if.** The ninth seating finds a defect in a fix shipped here — then the fix-narrowness class gets a test of its own: every refusal is exercised on the page where it was found *and* on a page where the shape appears in a different form.


## D-046 · 2026-09-10 · The converse of D-045: a refusal is no wider than its shape, and every refusal is tested on a sentence it must admit

**Assumes.** The generator is a fair witness to over-refusal: it writes what the register permits and the refusals it hits are the ones a careful writer would hit.

**Breaks if.** A regeneration failure turns out to be the model evading a rule rather than obeying one — then the failure is evidence about the model and the admit-test for that rule must be written by hand from the spec, not from the draft the model produced.


## D-047 · 2026-09-10 · The ninth seating (series 2): a fixed text on the page is tested against the computation it labels

**Assumes.** The revised reader's series is comparable within itself (runs 25, 26, …).

**Breaks if.** The tenth seating finds the reading reciting again with nothing overclaimed and nothing missed — then the prose is cut to the relations, the decorative disclosure and the stance, and D-039's assumption that a reading is what a table cannot do is revisited with the readings as evidence.


## D-048 · 2026-09-10 · The tenth seating (series 2): the reading is bound to what the register does not print; the tokenizer is the scope of every rule

**Assumes.** A sentence naming a room under every feature that marks it is a reading and not a recitation of a hidden cell.

**Breaks if.** The eleventh seating finds the R19 sentence recited the way R16's entitlement is — a most-marked room read off the field with nothing said about what its marks make of it — with nothing overclaimed and nothing else missed. Then the cut has its evidence: the prose goes to relations, disclosure and stance, and D-039 is revisited with eleven readings. D-035's stop count stays at zero; the eleventh is the first seating that could start it.


## D-049 · 2026-09-10 · The eleventh seating (series 2): the R19 sentence is the field read aloud; the register's closures land, the cut is put to Alex

**Assumes.** The eleven readings are a fair sample of what a model does under this prompt and lint (the prompt was revised at D-048 and the reader was revised at D-045; the recitation trend held across both).

**Breaks if.** Alex names a job for the prose that is not a field — then D-039 stands and the seat continues on that job.


*Answer to D-048's clause(s), recorded here:* Its first conjunct holds, structurally: the R19 sentence is `most_marked_rooms[0]` read aloud — room, mark count, feature list in the sheet's order — with nothing said about what the marks make of the room; and R19, as an obligation on form, cannot tell that sentence from a reading. Recitation rose from 10 of 14 sentences to 16 of 19; every sentence D-048 had credited as non-recitation on the previous page (the conjunct diffs, the distinct-set derivation) collapsed to its cell on this one. Its second and third conjuncts fail: two non-numeric overclaims remain ("The remaining marks sit differently" — a complement family false for import_root and package_entry on the distributive reading, D-045's shape without the family word; "reads no fan-in *as* entrance" — the caveat's sense inverted against its own register row, past R17b's negation lexicon and through the D-045 addendum's bag-of-words exemption), and three things only prose could say are still unsaid (the caveat's four live rooms — import_root ∩ package_entry — the reduction of six names to four predicates through the relation cells, and the tie at the top). **So the breaks-if does not fire as written.** The reader's argument, 


## D-050 · 2026-09-11 · The twelfth seating (series 2), the first on a page with no author: the breaks-if fires on the most-marked table and not on the matrix; brief 0.19.0, onboarding 0.2.4

**Assumes.** Distinct sets is what "most-marked" means — a count of predicates that fire on a room, not of marks that print — and a reader takes "rooms at this count" as the row's own denominator.

**Breaks if.** The thirteenth seating finds the distinct-sets column carrying a severity (six sets read as worse than five — a set is a predicate that fired, not a condition, D-004 Q3), or finds the lead's three clauses read as a paragraph rather than three labels — then the column gets a note that says what a set is, and the lead is split into the cells it labels. D-035's stop rule counts on the register unchanged: two consecutive seatings finding nothing the register's tests missed retire the seat.


## D-051 · 2026-09-11 · What M3 is now: answered, not delivered — the brief stays as the register page, C5 rewritten, the milestone closed on the evidence

**Assumes.** The twelve readings are the evidence the milestone can be closed on — one prompt lineage (revised D-048), one reader lineage (revised D-045), four repositories, one register that grew to meet each finding.

**Breaks if.** Someone names a job for prose over this sheet that is not a field and survives D-004 Q3 — then M3 reopens as a new milestone with that job as its hypothesis, not by reinstating the cut generator; or a reader of the register page asks a question the page cannot answer that the reading once did — the seatings' "what only prose could have said" sections, all empty since D-049, are the standing check.


## D-052 · 2026-09-11 · The thirteenth seating: the lead read as a paragraph, the record beside the entry, and two counts of sets; brief 0.20.0

**Assumes.** A cell reading "3 of 4" is read as a fraction of a tier, not as a score; the record lexicon is complete for the signals the shipped rulesets read.

**Breaks if.** The fourteenth seating finds a record beside a position that is not the record the predicate reads — then the lexicon is checked against the substrate's signal sources in a test, not by hand; or finds "3 of 4" read as a rank ("third of four") — then the cell is written out in words.


*Answer to D-050's clause(s), recorded here:* *"The thirteenth seating finds the distinct-sets column carrying a severity (six sets read as worse than five …), or finds the lead's three clauses read as a paragraph rather than three labels."* Clause 1 does not fire: the column's texts are "ordered by" and "most"; a count of positions inherits D-004 Q3 from the positions it counts; the residual carrier (a superlative title over a descending sort) failed frame removal. Clause 2 fires, on the third clause. "A row below that count is one of the rooms at its count, first by path, and its row says how many there are" was true of the list and false as a label on the second and third rows of a tier — `src/routes/v1/schemas.ts` is second by path at five sets, `src/schemas/enums.ts` third. And the tier of four at five sets was listed as three (`src/terminal/styles.ts` cut by the five-row cap) with a 4 beside three rows and no sentence, while the top tier had words. D-049 item 2's disclosure was narrower than its shape; D-050's lead said in a caption what only a cell could say per row.


## D-053 · 2026-09-11 · The fourteenth seating: a blend's records come from its declared inputs; the page defines a wing and counts the packages it pools; brief 0.21.0, maintainability 0.2.5

**Assumes.** `ALLOWED_INPUTS` is the truth about what a blend reads (a tuned weight of zero still names the record — the record list says what the blend is built from, not what this tuning weights).

**Breaks if.** The fifteenth seating finds a record named that the tuned weights give zero and reads it as a claim about this repository's numbers — then the list is computed from the effective weights under the page's gate; or finds "by its predicate" read as "by construction, so uninformative" where the outside count is the finding — then the cell leads with the count.


*Answer to D-052's clause(s), recorded here:* *"… finds a record beside a position that is not the record the predicate reads — then the lexicon is checked against the substrate's signal sources in a test, not by hand."* Fires, in its weaker form: "high edit-pressure node (edit record)" beside `crack`, and `toothpick_wing`'s four records, omit the clock — `bug_pressure_index` reads recency (inverse `last_touched_days`) at weight 0.2 under this page's gate (`derived.py`; the effective config under `179d8acb7b0c`). D-048 had corrected `load_index`'s record list by hand and left the other four blends; the same omission was latent for `change_pressure_index`, `neglect_index` and `complexity_proxy_index`. *"… or finds '3 of 4' read as a rank"* — does not fire: the column header and three "3 of 3" rows refute the ordinal reading of "1 of 7", and the reader rejected it as rhetoric.


## D-054 · 2026-09-12 · The fifteenth seating: the scope count was over the wrong population; a tie names its partner; a tier orders by path; a signal's reason is the signal's; the citation grammar; brief 0.22.0, maintainability 0.2.6

**Assumes.** The population's scopes are the pooling that matters — a test file's scope is not ranked by any pNN, so it is not pooled.

**Breaks if.** The sixteenth seating finds "rooms per scope, largest first" read as a ranking of scopes by importance rather than a size (then the list is by name, as the wings are); or finds a `[signal_reason]` composed onto a row whose predicate reads the signal through a blend that the tuning zeroes — a reason true of the signal and idle on the row (then the reason is composed per effective weight, D-053's unbuilt remedy); or finds the citation grammar's unnamed `§n.n` resolving to the wrong spec on a heading two specs share (then every citation on the page names its spec).


*Answer to D-053's clause(s), recorded here:* *"… finds a record named that the tuned weights give zero and reads it as a claim about this repository's numbers"* — does not fire: under `179d8acb7b0c` the blend reads commit_count, recency and revert_count; "clock" and "edit record" each carry a weighted input, and fix history is one signal of the edit record, not the record. The juxtaposition on `toothpick_wing` (position "edit record", reason "zero to fix history") was not the clause; it was item 4 below. *"… or finds 'by its predicate' read as 'by construction, so uninformative'"* — does not fire as substance: the reader read past `dark_room`'s "8 of these rooms outside it" once, restated it as an ordering, and kept it as rhetoric; the count-first remedy is not taken.


## D-055 · 2026-09-12 · The sixteenth seating: a containment the predicates guarantee is drawn under the floor; the root scope has its own name; a decorative row prints its predicate; the ordering text is one; brief 0.23.0

**Assumes.** A containment by conjunction of terms holds on every distribution (it does: the inner predicate's rooms satisfy every term of the outer's).

**Breaks if.** The seventeenth seating finds a by-predicate containment drawn under the floor read as the tiny feature's *finding* — "toothpick_wing is inside crack" taken as this repository's news where it is the ruleset's construction (then the under-floor cell leads with "by construction" and drops the outside count); or finds "(root manifest)" read as a directory (then the scope list names the manifest path, `package.json`); or finds the decorative row's predicate read as a diagnosis because it now looks like the diagnostic rows' (then the ◌ row's predicate is set in the reason's register — "reads `…`" — not in code).


*Answer to D-054's clause(s), recorded here:* None of the three clauses fires. *"rooms per scope, largest first" read as a ranking* — constructed and dissolved: "largest" is a size word and the size is beside each name; kept as rhetoric. *A `[signal_reason]` composed onto a row that reads the signal through a zeroed blend* — cannot fire on this ruleset: the loader composes over predicate terms, and `bug_pressure_index` is an input to no blend in `ALLOWED_INPUTS`; both rows that carry the reason read the signal directly at p90. *An unnamed `§n.n` resolving to the wrong spec* — every `§` on the page is spec-named already; the reader noted that `truthful-extractor-spec.md` also has a §5.3 outside the grammar's three-spec roster, so an unnamed one would never see it. The clause's remedy — every citation names its spec — is now a test rather than a state.


## D-056 · 2026-09-12 · The seventeenth seating: the marker's silence gets a value; a scope is named by its manifest; the sheet's order survives its own serialisation; ◌ travels with the name; brief 0.24.0

**Assumes.** The shared-signal walk over declared inputs is the right unit for "reads with it" — a weight of zero on the shared input would still name it (D-053's assumption, restated).

**Breaks if.** The eighteenth seating finds "reads last_touched_days with it" read as the containment's *cause* — the three rooms are in `lit_room` *because* the blend reads the clock — where a 0.2 weight guarantees nothing (then the value reads "the predicates share a signal, last_touched_days; the containment is the repository's"); or finds the seam shape again with nothing else — then D-035 is re-read as this entry proposes and the seat's standing rule is put to Alex with the seventeen readings as the evidence.


*Answer to D-055's clause(s), recorded here:* Clauses 1 and 2 each fire one cell over from the text they named; clause 3 does not. *A by-predicate containment read as the tiny feature's finding* — on the cell named, no: "by its predicate" is in the parenthesis and the outside count is the repository's. (The clause said "under the floor"; `toothpick_wing` has three rooms here, which is the floor, so the under-floor branch never ran — the clause was written one step narrower than its shape, as the reader noted.) What fired is the cell's neighbour: `⊂ lit_room (18 lit_room rooms outside this set)`, unmarked beside three "by its predicate", read as independent news — a fourth mark agreeing — while `bug_pressure_index` reads `recency`, which is `last_touched_days`, the signal `lit_room` reads. The marker was two-valued and its silence had acquired a meaning the page never assigned. *"(root manifest)" read as a directory* — no; but its neighbour `cookbook 1` beside the wing's `cookbook 136` in the same paragraph was read as a contradiction or as "the cookbook directory outside the servers": D-055 item 3 renamed the root scope and left every other scope keyed by a directory a wing can share. *A decorative row's predicate read as a di


## D-057 · 2026-09-12 · The eighteenth seating: the seat is not seam-only; a feature that fired on nothing keeps its row; the marker's values are defined on the page; the import graph and the test graph are one edge set, said; brief 0.25.0, maintainability 0.2.7

**Assumes.** A caveat on the predicate is where a path convention's reach belongs (D-042), and the note is where a shared instrument belongs — the first is the ruleset's limit, the second the substrate's.

**Breaks if.** The nineteenth seating finds the one-edge-set sentence read as "so the import-graph positions are test artefacts" — corridor's centrality inflated by tests, the mark dismissed — where 58 of 262 is a share, not a cause (then the share is a cell per feature and the sentence points at it); or finds a zero row read as a claim that the repository lacks the thing ("no toothpick wings here") where a decorative feature's zero is the tuned blend's, not the repository's (then the zero row's reason leads); or is seam-only — then D-035 is re-read as D-056 proposed.


*Answer to D-056's clause(s), recorded here:* *Clause 1* ("reads last_touched_days with it" read as the containment's cause) — not on this page; its complement fired: `⊂ scaffolding (no signal in common; 120 …)` read as *independent measurements agreeing* — the corridors are covered, two records say so — where the sheet supports only that no raw-signal *name* is shared, and the substrate shows one instrument and one edge set: a test file is a node in the graph centrality is computed on, so a test import raises `centrality` and `test_fan_in` together. D-056's own remedy sentence ("the containment is the repository's") is the half-truth this reader already takes. *Clause 2* ("the seam shape again with nothing else — then D-035 is re-read and the seat's standing rule goes to Alex") — **does not fire.** Finding by finding: the marker undefined on the page and `reinforcement_index` unexpanded — seam; the 0.5 floor in a ruleset comment and not the caveat — seam (D-053 item 5's exact shape, the second note-not-caveat); "test-imported" including helpers and fixtures — **not the seam** (no definition anywhere landed wrong; a limit on the predicate nobody had written, found by reading edges); the unfired feature with no row — **not the 
