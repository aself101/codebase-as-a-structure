# Handoff — state of codebase-as-structure at 2026-09-10

*Written at the close of the session that ran D-033 through D-047 (2026-09-06 to 2026-09-10). Read this, then `DECISIONS.md` from D-036, then the pick-up memory. The specs and checklists are current to D-034; the log is the authority where they differ.*

## Where the pipeline stands

| Layer | State | Where |
|---|---|---|
| Substrate | 0.4.1, fingerprint `179d8acb7b0c`, tuned config 24c087a | `out/cache/` |
| Gate (canonical) | K = 25, eps 0.01 / delta 0.05, M_asserted 3 (D-033); 21 of 25 signals asserted | `out/validation-k25c/`, archived `reports/2026-09-06-m1c-k25c/` |
| Rulesets | maintainability 0.2.2, onboarding 0.2.1 (caveat field; scaffolding's position name is "test-imported room") | `rulesets/` |
| Skeleton | carries gate reasons, reference set, population floor, calibration, archetype reason (D-034); `map` binds to the substrate fingerprint | `mapper/engine.py` |
| Brief | **0.16.0**: header → register (code-rendered) → reading (model, 200–400 words) → provenance → lint; rules R1–R18 with admit-cases | `brief.py`, `tests/test_brief.py` |
| Readings under the canonical gate | tag `2026-09-06t`: mcp-secure-server and eslint pass 0.16.0; typeorm and registry failed their last attempt on genuine refusals | `reports/2026-09-06t-m3x/` |
| Viewer | K = 25 time-lapses, position-name legend | https://claude.ai/code/artifact/fa3e321e-a52f-4eca-a38f-c53e3f59e202 |
| Prototype | tabular brief, three variants (A adopted as the register, D-039) | branch `prototype/tabular-brief`; https://claude.ai/code/artifact/1fe479eb-d534-4841-8dc8-46bbb00d6fc6 |

Regenerate everything with `uv run python scripts/regenerate_readings.py --validation out/validation-k25c/validation.json --tag <tag> --skip-timelapse` (drop `--skip-timelapse` to redo the twelve-frame runs; add `--relint-from reports/<tag>-m3x` to re-judge existing prose). The gate re-run command and the HTTPS push recipe are in the pick-up memory.

## The hostile-reading series (D-013 rule 5)

Nine seatings and one paired reading, tracker runs 17–26, all HATER_EXPOSED, scores 86–88. The reader definition was revised between runs 24 and 25 (Alex; three prompt audits); run 25 held the page constant to isolate the reader, and runs 25+ are series 2. The citation layer has never been breached. Each seating found the next layer the page had assumed inert:

1. connective prose (D-036, D-037) → sheet typing, R9–R14
2. a rule that generated a sentence (D-038) → the register prototype, R15
3. the register's derived cells (D-040) → tests as the register's lint
4. the register's fallback cells (D-041) → every fallback says the reason that is the reason
5. code-authored explanations and ruleset caveats (D-042) → numbers split by cause; a caveat is a limit on the predicate
6. the under-covering disclosure (D-043) → the note generated from the constants; every position names its record
7. no new layer; the note generated from the constants but not the filter beside them (D-044)
8. fixes narrower than the shape they close (D-045, the paired reading)
9. fixed texts never tested against the computation they label (D-047)

And, found by the generator rather than a reader (D-046): eight refusals wider than their shape in one day, plus one stale-variable bug. **The two halves are one rule: a refusal is exactly as wide as its shape.** A hostile reader finds the narrow fix (it leaves an overclaim on the page); only the generator finds the wide one (it leaves no page). The regeneration is therefore a second instrument, and its failures are evidence about the lint.

**Practices now standing:** every refusal carries an admit-case, seeded from the prompt's own examples, tested on a paragraph (`test_every_refusal_admits_the_sentence_it_must_admit`); a fixed text on the page is tested against the computation it labels; a ruleset change names the regenerated sheet that shows it (D-038 — a closure was once logged and never written); commits gate on the test runner's exit status, and patch scripts write each file as they go.

**The open question the ninth reader posed:** the reading has stopped overclaiming and started reciting — its first paragraphs are the register read aloud, and no rule can refuse recitation without the sentence carrying something not on the sheet, which is the hole the lint declares. D-047's breaks-if: if the tenth seating finds recitation with nothing overclaimed and nothing missed, the prose is cut to the relations, the decorative disclosure and the stance, and D-039's premise (a reading is what a table cannot do) is revisited with the readings as evidence.

## Next, in order

1. **Tenth seating** on a 0.16.0 page (eslint or mcp-secure-server, both passed), under the revised reader, asking what the reading fails to say and whether it recites. This is the first seating where the answer could turn the verdict.
2. Regenerate typeorm and registry under 0.16.0 (their failures were the model's); then all four under whatever the tenth seating produces.
3. Third forecast cycle: `cascade-depth-analyzer` (Alex's recommendation) beside `perverse-outcome-detector`, reading its deepest links against the "Breaks if" ledger. The stability budget's effect on editing (D-034 C8) and the register-as-schema (future-observer B-1) are the candidates.
4. Direction candidates, unbuilt: registry layer-geometry over-budget as the trigger for mapper §7 Q6; user-selectable architectural style / civilization as a rendering skin (Alex, 2026-09-08; constraints in the memory); Blender components from `skeleton.json`; facts-sheet lexicons in config; breaks-if ledger parser; substrate contract independent of git; Phase 3 corpus calibration; per-package calibration for monorepos.

## Things to know that the code does not say

- The prototype skill wants a routed web app to host variants; this project has none, so its "new page" fallback was used. Alex wants feedback on where it strains.
- Two commits on 2026-09-08 carried messages for changes a dropped patch anchor left out; both corrected on the record (D-042, D-044 notes). The rule that came out of it is in the memory.
- The tracker returned 401 transiently on 2026-09-10; the third `/mcp` reconnect cleared it without a restart.
- Runs 10 and 11 in the tracker are the M1 gate (D-015) and the phase-1 review (D-024); 12 m3-review, 13 forecast-cycle, 14 theory→practice, 15 forecast-cycle-2, 16 theory→practice-2, 17–24 seatings one to eight, 25 the paired reading, 26 the ninth seating.

## Addendum, later the same day: D-048 and D-049 — the tenth and eleventh seatings, and the cut

*Written after the session that ran them (tracker runs 27 and 28; commits 6502122, 0d8f5c0 and the one carrying this note). Read D-048 and D-049 with its addendum in the log; this is the short form.*

**What happened.** The tenth seating (eslint 0.16.0) answered D-047's question: the reading recites, overclaims nothing, and misses what only prose could say — so the breaks-if did not fire, and D-048 bound the reading (R19: a most-marked room named under every feature that marks it) and closed six defects, one of them the tokenizer not splitting before a digit (a false pass on eslint and a false refusal on typeorm from one character class). The eleventh seating (eslint 0.17.0) found the R19 sentence to be the field read aloud, recitation up to 16 of 19, and could name no job for the prose that is not a field. The cut was put to Alex with three options; he chose it. **Brief 0.18.0 is rendered by code in full** — register (feature table; most-marked rooms with the count at the top so a capped list says it is cut; a shared-rooms matrix for every pair of diagnostic features) → decorative disclosure → stance → provenance. The lint (R1–R18) stays for hand-written drafts. Pages under tag `2026-09-06v`.

**What the record now says that it did not.** Rulesets maintainability 0.2.4 / onboarding 0.2.3: `flooded_basement` is "long-untouched room above the load floor" (its "still-imported" was cleared by 27 of 75 eslint rooms with fan_in 0), `corridor` is "high-centrality junction with fan-out at or above the median", `foundation` is "high-load node" (12 of 48 were not hub rooms); the loader refuses record words, quantile words, "high" beyond the predicate's upper pNN, and other features' names in a position name. `record_of(load_index)` names size.

**Documentation drift to decide on, not silently fix.** The system spec's C5 and the M3 identity sentence ("the one stage with a model in the path") describe a stage that no longer exists; the architect-brief spec carries a dated revision note; the system spec does not yet. `scripts/regenerate_readings.py` keeps its name though it regenerates no reading.

**Next, in order (replacing the earlier list's items 1–2).**
1. Twelfth seating on a 0.18.0 page (a repository the tenth and eleventh did not read — typeorm or registry), reading the register only: does the matrix or the most-marked table carry an implicature a cell should not (D-049 addendum's breaks-if)? D-035's stop rule now counts on the register.
2. Forecast cycle 3 (unchanged): `cascade-depth-analyzer` beside `perverse-outcome-detector` against the "Breaks if" ledger; add the cut as a candidate — what does a page with no author do to the people who read it?
3. The system spec C5 rewrite, once Alex has said what M3 is now.
4. The direction candidates above, unchanged; the viewer could carry the matrix.

## Addendum, 2026-09-11: D-050 — the twelfth seating, on a page with no author

*Tracker run 29; the commit carrying this note. Read D-050 in the log; this is the short form.*

The D-049 addendum's breaks-if fired — on the most-marked table, not the matrix. On typeorm the table listed the four rooms at seven marks and then one of eight rooms at six, first by path, under a lead that said "all are listed" (the render had no branch for fewer rooms at the top than the cap; the test covered the tie and the empty case, not the fill). And marks, one per feature per profile, ranked rooms by a unit the header discounts on the same page — the three rooms that led at seven marks were five distinct sets; `src/index.ts` at seven was six. **Brief 0.19.0** counts distinct sets, orders by sets then marks then path, and every row says how many rooms carry its count. Also: "none validated" was a literal; two position names wore one conjunct of two (the fifth consecutive position-column seating — **onboarding 0.2.4**, and the loader now refuses a pNN a name leaves out, the converse of D-048's check); the `import_root` caveat cited a spec section that resolved and carried no such limit (now D-029, and a test requires a ruleset citation's target to speak of the feature); the matrix note's "only those" was false of the under-floor pair; "no cell is a sentence" beside a three-sentence cell. Pages under tag `2026-09-06w`; 139 tests.

**What it says about the series.** Seven of eight findings were rules narrower than their shape, now on the code-rendered page; removing the author moved the overclaim from sentences to the leads and notes around the cells. The reader could name nothing only prose could say. D-035's stop count stays at zero.

**Next, in order.** (1) Thirteenth seating on a 0.19.0 page (registry or mcp-secure-server — the two the series has read least), register only, against D-050's breaks-if (the distinct-sets column read as a severity; the lead read as a paragraph). (2) Forecast cycle 3, unchanged. (3) ~~System spec C5, once Alex says what M3 is~~ — done, D-051: M3 answered rather than delivered; C5 rewritten as the register page, the June paragraph kept inside it. (4) Direction candidates, unchanged; the viewer could carry the matrix and the most-marked table.

## Addendum, 2026-09-11 (later): D-051 and D-052

**D-051 — what M3 is.** Alex chose reading 2 with 1's rewrite: M3 is recorded as answered rather than delivered (the model was the milestone; a table is what shipped); the brief stays as C5, the code-rendered register page; the lint is the contract on a hand-written draft. System spec C5 row, description, data flow, §6 and the milestone line say so, with the June §6 paragraph kept inside, marked.

**D-052 — the thirteenth seating (registry 0.19.0, run 30).** D-050's second clause fired: the lead's "first by path" was false as a per-row label, and a tier of four listed as three had a number and no words. **Brief 0.20.0**: the fourth column is "listed of rooms at this count" ("3 of 4"), and the lead labels nothing per row. Also: `package_entry`'s record is the package manifest, not the import graph (sixth consecutive position-column seating); "distinct sets" is one computation shared by the header, the co-location count and the table; co-location is counted in sets (typeorm 109, not 160); the page's own citations are tested; §6's D-051 sentence about a per-row validation status was false and is corrected. Pages under `2026-09-06x`; 141 tests.

**Next.** (1) Fourteenth seating (mcp-secure-server or eslint on 0.20.0) against D-052's breaks-if (a record that is not the predicate's; "3 of 4" read as a rank). (2) Forecast cycle 3. (3) Direction candidates; the viewer could carry the register's two tables.

## Addendum, 2026-09-11 (end of session): D-053 — the fourteenth seating

mcp-secure-server 0.20.0, run 31, the rival a monorepo maintainer. D-052's first clause fired (the record beside `crack` omitted the clock; `bug_pressure_index` reads recency at 0.2) and the remedy it named is built: **brief 0.21.0** derives a blend's records from `config.ALLOWED_INPUTS`. Also: the lead's "first by path" was false of a mixed-marks tier (now "by marks, then by path", tested on one); five of six containments were by predicate and now say so; the page defines a wing and states how many `package.json` scopes its population pools (15 here, 21 on eslint); `flooded_basement` gets its caveat (**maintainability 0.2.5**); spec §6 no longer counts seatings. Pages under `2026-09-06y`; 142 tests. Seven position-column seatings running. Stop count zero.

**Next.** (1) Fifteenth seating (eslint on 0.21.0 — 21 package scopes pooled is the biggest case of D-053's new text) against D-053's breaks-if. (2) Forecast cycle 3 — the "Breaks if" ledger now runs D-034 → D-053 and the cut is a candidate. (3) Direction candidates; per-package calibration (mapper §7 Q6) has moved from an open question to a stated limit on every page and is the strongest candidate the seatings have produced.

## Addendum, 2026-09-12: D-054 — the fifteenth seating

eslint 0.21.0, run 32, the rival a maintainer of one of the scopes the calibration sentence pools. Neither clause of D-053's breaks-if fired. What the recount found beneath the reader's "21 scopes with no per-scope count": the 473 rooms span **6** scopes — D-053 counted over every substrate node (1481), and the three other pages coincided, so the fixture (`packages >= 1`, an asserted literal) could not fail. **Brief 0.22.0**: scopes counted over the population with rooms per scope on the page; a tie names its partner with numbers ("tied with lib (as parent, not the wing) 4 / 6"); the most-marked tier orders by sets then path — within a tier marks differ only by the double count the register discounts; a reason about a signal is written once in the ruleset's `[signal_reason]` table (**maintainability 0.2.6**) and composed onto every row reading the signal, with the tuning claim tested against `config/tuned.toml`; the citation tests share one grammar (spec-named, dotted or not, `Qn`), and mapper §7 now holds Q7, the question the page had cited it for. Not built: "by its predicate" on a matrix zero (disjointness by predicate is conditional on the distribution where containment by conjunction is not). Pages under `2026-09-06z`; 143 tests. The position column resisted for the first time in eight seatings. Stop count zero.

**Next.** (1) Sixteenth seating (typeorm or registry on 0.22.0 — the two the series has read least recently) against D-054's breaks-if (rooms-per-scope read as a ranking; a signal reason idle on a row; an unnamed §n.n resolving to the wrong spec). (2) Forecast cycle 3 — the "Breaks if" ledger runs D-034 → D-054. (3) Direction candidates; per-package calibration now has its sizes on every page and its question in mapper §7 Q7.

## Addendum, 2026-09-12 (later): D-055 — the sixteenth seating

typeorm 0.22.0, run 33, the rival the `src/error` maintainer. None of D-054's three clauses fired; the rival's central attack — 33 of 61 read as a verdict — failed on the page's text and on the substrate (every `src/error` file is imported; the position is true of each). Three findings, all D-045's shape: `toothpick_wing`'s by-predicate containments refused for size (now drawn at any count, the test's `or` removed); "(root)" as wing and scope in one note (scope is "(root manifest)"); the sheet's `units` gloss still ordering by marks (one `MOST_MARKED_ORDER` text). Also: a decorative row prints its predicate; every `§` on the page names its spec, by test. **Brief 0.23.0**, pages under `2026-09-06aa`, 144 tests. Stop count zero; position column clean two seatings running; numbers clean four pages running.

**Next.** (1) Seventeenth seating (registry or mcp-secure-server on 0.23.0 — mcp has three toothpick rooms and the only repository-decided containment, `toothpick_wing ⊂ lit_room`) against D-055's breaks-if (an under-floor by-predicate containment read as the tiny feature's finding; "(root manifest)" read as a directory; a decorative row's predicate read as a diagnosis). The seat's yield is now three findings of one shape per page; if the seventeenth is the same, D-035's rule should be re-read — not "nothing the tests missed" but "nothing but the seam between a change and the text one step from it", which is a lint's job, not a reader's. (2) Forecast cycle 3 over D-034 → D-055. (3) Direction candidates unchanged.

## Addendum, 2026-09-12 (end of session): D-056 — the seventeenth seating

mcp-secure-server 0.23.0, run 34, the rival the author of the three ◌ toothpick files. D-055's clauses 1 and 2 fired one cell over from the text they named (the *unmarked* `⊂ lit_room` beside three "by its predicate" — the blend reads the clock; `cookbook 1` beside `cookbook 136`); clause 3 did not. The recount found `by_package` alphabetical in the file under a "largest first" gloss (sorted keys). **Brief 0.24.0**: the containment marker has three values ("by its predicate" / "reads last_touched_days with it" / "no signal in common"); scopes named by manifest path; `by_package` an ordered list; ◌ travels with the name; one-set containers are one entry. Pages under `2026-09-06ab`; 145 tests. Stop count zero; numbers clean five pages running; position column clean three running.

**The D-035 question, now on the record (D-056).** Three seatings running, every finding but one has been the seam between a change and the text one step from it — a lint's job — and the breaks-if clauses have twice been written one step too narrow. The one exception (a marker's undefined complement) is what a reader finds and a lint does not. D-056's breaks-if says: if the eighteenth is seam-only, the seat's standing rule (D-035 rule 4 / D-013) goes to Alex with seventeen readings as the evidence. That is a decision about the series' identity and is his.

**Next.** (1) Eighteenth seating (registry on 0.24.0 — the page with the other "no signal in common" containment, `corridor ⊂ scaffolding`) against D-056's breaks-if. (2) Forecast cycle 3 over D-034 → D-056. (3) Direction candidates unchanged.
