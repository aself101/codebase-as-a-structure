# eslint — architect's brief

*Rendered from the facts sheet by code; no model wrote any of it. Every cell is a field, and every fixed text on the page has a test that asserts it against the sheet it is rendered from (`tests/test_brief.py`); the tests run on a fixture, not on this page. Profile maintainability 0.3.3 + onboarding 0.3.0, geometry age, skeleton `da34786c788e…`, facts `e6c1f7316420…`. As of 2026-09-04, commit `3f20a57c6293`. Calibration: in-repo, self-relative (system spec §5.3), 6 package scopes pooled — per-package calibration is an open question of the mapper (architect-brief spec §5, mapper §7 Q7); one frame — stability is the `substrate timelapse` run under gate `177c129be9b5`, not this page. Brief 0.41.0.*

## Register

**A position names where a room sits in a record — the import graph, the clock, the test graph, the edit record, size — and is not a claim about the room's condition (D-004 Q3). Every pNN ranks this repository's own 467 rooms, so a `>= p90` row holds a tenth of them — or more where rooms tie at the cutoff, and the rooms column says how many are tied; a single-rank row states its share, and every ranked term states the value its rank resolved to here — and no count on this page compares across repositories, or across snapshots of this one: a ranked row holds its share of whatever rooms the repository has, so working its rooms down moves the cutoff, not the count (D-073). A room is a source file outside the test convention (a path matching `**/*.test.*`, `**/*.spec.*`, `**/*_test.*`, `**/test_*.py`, `**/test/**`, `**/tests/**`, `**/__tests__/**`, `**/spec/**`, `**/__mocks__/**`) with computed signals, and not a config or migration or placeholder file (kinds the ruleset does not count as rooms, each read by a declared convention and nothing else — config: a tool's configuration by basename — `*rc.*`, `*.config.*` or `*.conf.*` — at its package's root; migration: any file under a `migration/` or `migrations/` directory; placeholder: a file that is an empty export once comments are stripped): 467 of the 1481 files with a source extension the substrate reads (manifests, documents and the rest of the tree are not counted); the 1008 test files are nodes of the import graph and not rooms; the 6 files of a kind the ruleset does not count (config 5, placeholder 1) are in the graph and not a room.**

*467 rooms in 8 wings ((root) 1 · bin 1 · conf 2 · docs 27 · lib 388 · messages 18 · packages 8 · tools 22); 911 diagnostic marks across all profiles (667 in the base profile), one mark per feature per room; identical pairs of diagnostic features mark 47 rooms twice (47 under one predicate in two profiles, 0 where two predicates draw one set because a conjunct excludes nothing, 0 under both); the diagnostic features name 10 distinct sets of rooms; 48 excluded marks ◌ (crack 47, toothpick_wing 1); 250 rooms carry two or more distinct diagnostic sets; gate `177c129be9b5`, 7 of 8 signals asserted, none validated — asserted is a description that held under the stability budget and the corroboration its grounding class requires (validation spec §2.4), validated a forecast confirmed by a temporal holdout (validation spec §3).*

- **wing** — a directory at depth 1 of the tree (the ruleset's wing_depth), not a package; the population spans 6 package scopes, pooled (rooms per scope, largest first, each scope named by the manifest that holds it: package.json 432 · docs/package.json 22 · packages/eslint-config-eslint/package.json 5 · docs/_examples/custom-rule-tutorial-code/package.json 3 · packages/js/package.json 3 · docs/_examples/integration-tutorial-code/package.json 2); 19 of 1513 imports cross a package scope (11 from test files).
- **◌** — a feature excluded from the diagnosis (the ruleset's word is decorative): computed and counted, not claimed, because a signal it reads is unvalidated. 'Excluded' on this page means this and nothing else; a file kind the ruleset does not count as a room is said in those words.
- **largest parent directory** — the immediate parent (non-recursive) holding the most of a feature's rooms, with how many of the parent's own rooms that is; the cell says when it holds under a 3rd of the feature's rooms, and a feature under 6 rooms is not placed; a parent that shares a wing's name is marked as the parent.
- **relation to** — identity and containment, and only those, between features, diagnostic or decorative, with 3 or more rooms — and a containment the predicates guarantee at any count; two sets that overlap without one containing the other are not related here, and 'no identity or containment' says exactly that. 'By its predicate': the inner predicate conjoins every term of the outer. Otherwise the cell says which raw signals the two predicates read in common (a blend or index expanded through its declared inputs), or 'no raw signal in common' — a signal, not an instrument.
- **the import graph and the test graph** — one edge set read twice: a test file is a node whose imports count in fan_in and centrality, and test_fan_in counts those importers alone; an import-graph row says how many distinct files import its rooms and how many of those are test files, with the import edges beside them (a file importing several of the rooms is one file and several edges). Centrality is PageRank over that graph: a room's rank rises with the rank of its importers, not only with their number, so a room with 1 well-placed importer can outrank one with 4 (here lib/languages/js/validate-language-options.js at 1 is a hub and lib/config/config-loader.js at 4 is not). The graph is resolved statically: 32 imports in the tree did not resolve to a file (29 from test files; test_fan_in on the rooms those 29 aim at is a lower bound), and 467 are external packages; an import the resolver did not place, or one computed at run time, is not an edge, so a room reached only that way reads as unimported there. No tsconfig.json path alias was read (none declared, or no tsconfig.json). 11 imports name a package this repository declares (4 names) and resolve to its entry (9 from test files).
- **bug_pressure_index** — a blend the ruleset reads as one signal, each input a rank in this repository's own population: 0.5 × commit count's rank + 0.3 × revert count's rank + 0.2 × (1 − last_touched_days' rank) (the tuned weights, D-009; the substrate's effective config); the tuning weighted 0: fix count's rank, fix count's rank among the rooms with a fix, the share of commits that are fixes.
- **load_index** — a blend the ruleset reads as one signal, each input a rank in this repository's own population: 0.5 × fan_in's rank among the rooms anything imports (0 for a room nothing imports) + 0.3 × centrality's rank + 0.1 × (1 − fan_out's rank) + 0.1 × size's rank (the tuned weights, D-009; the substrate's effective config).
- **caveat** — the ruleset's own limit on what a predicate reads, never a claim about this repository; the count in it ('N of M here') is this repository's, beside the clause it counts.

| position (the record it reads) | feature | profile | rooms (tied at the cutoff) | by wing | largest parent directory (n of the parent's rooms) | relation to (= one set · ⊂ inside · ⊃ contains) | predicate (with the cutoffs resolved here); caveat, a limit on the predicate; or reason |
|---|---|---|---|---|---|---|---|
| long-untouched room (clock) | dark_room · long-untouched room | maintainability | 79 (77 tied at the cutoff — one commit of 1060 files) (77 were last touched by a commit the repository's .git-blame-ignore-revs disowns) | docs 20 (in 3 of the wing's 3 scopes), lib 40, messages 11, packages 5 (packages/eslint-config-eslint/package.json 4, packages/js/package.json 1), tools 3 | lib/shared 12 of its 19 (under a third of the feature's rooms) | ⊃ flooded_basement (by its predicate; 6 of these rooms outside it) | `last_touched_days >= p90` — rooms at or above this repository's p90 on last_touched_days (here 533.966 days): 79 of 467, 16.9%; the next value below the cutoff holds 82 rooms (one commit of 300 files; none of them last touched by a commit the repository's .git-blame-ignore-revs disowns), 8.985 days under it — caveat: reads the newest commit that touched the file, whatever the commit did (D-069): a repository-wide formatting or chore commit is a touch, so a room's last substantive edit is at least as old as the clock reads and never younger (77 of 79 here were last touched by a commit the repository's .git-blame-ignore-revs disowns) |
| long-untouched room above the load floor (import graph and clock and size) | flooded_basement · long-untouched room above the load floor | maintainability | 73 (25 have no importer) (73 were last touched by a commit the repository's .git-blame-ignore-revs disowns) | docs 18 (in 3 of the wing's 3 scopes), lib 38, messages 11, packages 3 (packages/eslint-config-eslint/package.json 2, packages/js/package.json 1), tools 3 | lib/shared 12 of its 19 (under a third of the feature's rooms) | ⊂ dark_room (by its predicate; 6 dark_room rooms outside this set) | `last_touched_days >= p90 and load_index >= 0.10` — here p90 on last_touched_days is 533.966 days (77 of 467 rooms at that value) — imported by 92 files, 31 (34%) of them test files (135 import edges; a file importing several of these rooms is one file here) — caveat: reads a floor on the load blend, not an importer (D-048): a room that nothing imports can clear the floor on inverse fan-out and size alone (25 of 73 here have no importer) — caveat: reads the newest commit that touched the file, whatever the commit did (D-069): a repository-wide formatting or chore commit is a touch, so a room's last substantive edit is at least as old as the clock reads and never younger (73 of 73 here were last touched by a commit the repository's .git-blame-ignore-revs disowns) |
| high-load node (import graph and size) | foundation · high-load node | maintainability + onboarding (one predicate, two profiles) | 47 | conf 2, lib 44, tools 1 | lib/shared 9 of its 19 (under a third of the feature's rooms) | ⊃ ◌ toothpick_wing (by its predicate; 46 of these rooms outside it) | `load_index >= p90` — rooms at or above this repository's p90 on load_index (here 0.78632): 47 of 467, 10.1%; the next value below the cutoff holds 1 room, 0.00172 under it — imported by 589 files, 343 (58%) of them test files (710 import edges; a file importing several of these rooms is one file here) (293 of these files import lib/rule-tester/rule-tester.js, 292 of them test files; without it 51 of 296, 17%) |
| high-centrality node (import graph) | hub | maintainability | 49 (6 tied at the cutoff) | conf 2, lib 44, packages 3 (all in packages/js/package.json) | lib/shared 12 of its 19 (under a third of the feature's rooms) | ⊃ corridor (by its predicate; 26 of these rooms outside it) | `centrality >= p90` — rooms at or above this repository's p90 on centrality (here 0.0013): 49 of 467, 10.5%; the next value below the cutoff holds 3 rooms, 0.0001 under it — imported by 593 files, 341 (58%) of them test files (702 import edges; a file importing several of these rooms is one file here) (293 of these files import lib/rule-tester/rule-tester.js, 292 of them test files; without it 49 of 300, 16%) |
| recently-touched room (clock) | lit_room | maintainability | 47 | (root) 1, lib 40, messages 1, tools 5 | lib/rules 24 of its 293 | no identity or containment | `last_touched_days <= p10` — rooms at or below this repository's p10 on last_touched_days (here 48.0352 days): 47 of 467, 10.1%; the next value above the cutoff holds 1 room (none of them last touched by a commit the repository's .git-blame-ignore-revs disowns), 0.1814 days over it — caveat: reads the newest commit that touched the file, whatever the commit did (D-069): a repository-wide formatting or chore commit is a touch, so a room can read as recently touched on a chore alone (0 of 47 here were last touched by a commit the repository's .git-blame-ignore-revs disowns) |
| test-imported room (test graph) | scaffolding · test-imported room | maintainability | 372 (of the 95 rooms without a test importer, 21 are imported by a room that has one — the most test-imported of them lib/rule-tester/rule-tester.js, 292 test importers) | conf 2, docs 2 (docs/_examples/custom-rule-tutorial-code/package.json 1, docs/_examples/integration-tutorial-code/package.json 1), lib 360, packages 2 (packages/eslint-config-eslint/package.json 1, packages/js/package.json 1), tools 6 | lib/rules 293 of its 293 | no identity or containment | `reinforcement_index >= 0.5` — caveat: reads whether any file under the test convention imports the room (test_fan_in > 0, D-011): the index is 0 with no test importer and 0.5 or more with one, so 0.5 is its floor, not a midpoint; a helper or fixture under the test paths counts as a test importer, and one import suffices; a room that tests reach only through a re-exporting barrel, or load by a path computed at run time, has no test importer of its own |
| high-centrality junction with fan-out at or above the median (import graph) | corridor | onboarding | 23 | lib 22, packages 1 (all in packages/js/package.json) | lib/config 4 of its 5 (under a third of the feature's rooms) | ⊂ hub (by its predicate; 26 hub rooms outside this set) | `centrality >= p90 and fan_out >= p50` — here p90 on centrality is 0.0013 (6 of 467 rooms at that value), p50 on fan_out is 1 (207 of 467 rooms at that value) — imported by 557 files, 324 (58%) of them test files (597 import edges; a file importing several of these rooms is one file here) (293 of these files import lib/rule-tester/rule-tester.js, 292 of them test files; without it 32 of 264, 12%) |
| import-graph root with fan-out at or above the upper quartile (import graph) — here p75 on fan_out resolves to 1, which 270 of 467 rooms reach (58%) | import_root | onboarding | 16 (3 are declared package entries) | (root) 1, bin 1, docs 2 (all in docs/package.json), lib 2, messages 2, packages 1 (all in packages/eslint-config-eslint/package.json), tools 7 | tools (as parent, not the wing) 5 of its 13 (under a third of the feature's rooms) | no identity or containment | `fan_in == 0 and fan_out >= p75` — here p75 on fan_out is 1 (207 of 467 rooms at that value) — caveat: reads no fan-in, not entrance (D-029): a package entry that anything imports — a room or a test file — cannot qualify, and one that nothing imports can (3 of 16 here are declared package entries) |
| imported leaf with fan-in at or above the upper quartile (import graph) — here p75 on fan_in resolves to 2, which 382 of 467 rooms reach (82%) | leaf_utility | onboarding | 147 | conf 2, docs 2 (docs/_examples/custom-rule-tutorial-code/package.json 1, docs/package.json 1), lib 139, messages 1, packages 1 (all in packages/eslint-config-eslint/package.json), tools 2 | lib/rules 98 of its 293 | no identity or containment | `fan_out == 0 and fan_in >= p75` — here p75 on fan_in is 2 (330 of 467 rooms at that value) — imported by 208 files, 141 (68%) of them test files (357 import edges; a file importing several of these rooms is one file here) |
| declared package entry (package manifest) | package_entry | onboarding | 11 | bin 1, docs 1 (all in docs/_examples/custom-rule-tutorial-code/package.json), lib 4, packages 5 (packages/eslint-config-eslint/package.json 4, packages/js/package.json 1) | lib (as parent, not the wing) 4 of its 6 (tied with packages/eslint-config-eslint 4 of its 5) | no identity or containment | `is_package_entry == 1` |
| high edit-pressure node (clock and edit record) | ◌ crack (excluded) · high edit-pressure node | maintainability | 47 | (root) 1, bin 1, lib 44, packages 1 (all in packages/js/package.json) | lib/rules 35 of its 293 | ⊃ ◌ toothpick_wing (by its predicate; 46 of these rooms outside it) | `bug_pressure_index >= p90` — rooms at or above this repository's p90 on bug_pressure_index (here 0.72974): 47 of 467, 10.1%; the next value below the cutoff holds 1 room, 0.00004 under it — ◌ excluded from the diagnosis: bug_pressure_index is unvalidated on the pre-registered test set (D-015): its tuned weights assign zero to fix history and it does not beat busyness by the margin. |
| unreinforced high-load node with high edit pressure (import graph and clock and test graph and edit record and size) | ◌ toothpick_wing (excluded) · unreinforced high-load node with high edit pressure | maintainability | 1 (1 of these 1 is imported by a room that has a test importer — the most test-imported of them lib/cli.js, 1 test importer) | lib 1 | too few rooms to place (1): lib/eslint/eslint-helpers.js | ⊂ ◌ crack (by its predicate; 46 crack rooms outside this set); ⊂ maintainability/foundation = onboarding/foundation (by its predicate; 46 maintainability/foundation rooms outside this set); too few rooms for any other relation (1) | `load_index >= p90 and bug_pressure_index >= p90 and reinforcement_index <= 0.0` — here p90 on load_index is 0.78632, p90 on bug_pressure_index is 0.72974 — ◌ excluded from the diagnosis: bug_pressure_index is unvalidated on the pre-registered test set (D-015): its tuned weights assign zero to fix history and it does not beat busyness by the margin. The fragility half of this feature has no forecast to stand on; it renders as decorative, counted, and excluded from diagnostic claims until a reference repo validates the index. — imported by 3 files, 0 (0%) of them test files (3 import edges; a file importing several of these rooms is one file here) — caveat: reads test_fan_in == 0 as the graph resolved it (D-011): a room whose tests import it through an alias the resolver did not place, or only through a re-exporting barrel, or only by a path computed at run time, reads as unreinforced |

### Rooms at the most positions (6)

**Every room at the most positions, in path order — a set, not a ranking and not a severity (D-004 Q3).**

**All 12 rooms here are long-untouched (dark_room) — the position at which the stance places the norm's known false positive (see Stance).**

*Every room under the most distinct diagnostic sets (6), by path — 12 rooms. A set is a distinct set of rooms a diagnostic feature draws (a feature under two profiles, or two features drawing one set, is one set). Rooms at fewer positions are not listed; 250 rooms carry two or more distinct sets. Size is the room's non-blank line count; importers is the room's fan_in, with the test files among them in parentheses.*

| room | lines | importers (from test files) | diagnostic features that mark it |
|---|---|---|---|
| lib/config/default-config.js | 66 | 4 (1) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, scaffolding · test-imported room |
| lib/languages/js/source-code/index.js | 5 | 8 (4) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, scaffolding · test-imported room |
| lib/linter/code-path-analysis/code-path-segment.js | 228 | 4 (1) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, scaffolding · test-imported room |
| lib/linter/index.js | 8 | 9 (5) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, scaffolding · test-imported room |
| lib/rule-tester/index.js | 5 | 6 (5) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, scaffolding · test-imported room |
| lib/rules/utils/regular-expressions.js | 50 | 4 (1) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| lib/shared/ast-utils.js | 27 | 7 (2) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| lib/shared/deep-merge-arrays.js | 52 | 3 (1) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| lib/shared/severity.js | 45 | 4 (1) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| lib/shared/string-utils.js | 47 | 8 (1) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| lib/shared/traverser.js | 177 | 7 (3) | dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, foundation · high-load node (2 profiles), hub, leaf_utility, scaffolding · test-imported room |
| packages/js/src/index.js | 19 | 6 (3) | corridor, dark_room · long-untouched room, flooded_basement · long-untouched room above the load floor, hub, package_entry, scaffolding · test-imported room |

### Shared rooms

*Rooms both features mark, for every pair of diagnostic features (features drawing one set of rooms share one row, named with each name); the diagonal is the feature's own count. A shared count equal to the smaller of the two features' own counts is containment, and equal to both is identity; the relation column above draws those, and only between features with 3 or more rooms or where the predicates guarantee the containment — any other pair under that floor is read here and not there.*

| shared rooms | dark_room | flooded_basement | maintainability/foundation = onboarding/foundation | hub | lit_room | scaffolding | corridor | import_root | leaf_utility | package_entry |
|---|---|---|---|---|---|---|---|---|---|---|
| dark_room | **79** | 73 | 16 | 20 | 0 | 24 | 8 | 3 | 21 | 6 |
| flooded_basement | 73 | **73** | 16 | 20 | 0 | 23 | 8 | 0 | 21 | 3 |
| maintainability/foundation = onboarding/foundation | 16 | 16 | **47** | 35 | 8 | 42 | 16 | 0 | 26 | 2 |
| hub | 20 | 20 | 35 | **49** | 9 | 36 | 23 | 0 | 23 | 2 |
| lit_room | 0 | 0 | 8 | 9 | **47** | 37 | 7 | 2 | 6 | 0 |
| scaffolding | 24 | 23 | 42 | 36 | 37 | **372** | 19 | 0 | 135 | 5 |
| corridor | 8 | 8 | 16 | 23 | 7 | 19 | **23** | 0 | 0 | 2 |
| import_root | 3 | 0 | 0 | 0 | 2 | 0 | 0 | **16** | 0 | 3 |
| leaf_utility | 21 | 21 | 26 | 23 | 6 | 135 | 0 | 0 | **147** | 1 |
| package_entry | 6 | 3 | 2 | 2 | 0 | 5 | 2 | 3 | 1 | **11** |

## Marks excluded from the diagnosis (◌)

◌ crack · high edit-pressure node 47; ◌ toothpick_wing · unreinforced high-load node with high edit pressure 1 — 48 marks computed and counted, excluded from the diagnosis: the signal they read (bug_pressure_index) is unvalidated; each row carries the reason.

## Stance

The diagnosis presupposes a maintenance norm — that the positions it marks are worth a visit — which the reader may reject; it is stated as an ought, not a fact (system spec, stance disclosure). The norm's known false positive, in the system spec's words (stance disclosure): a finished, correct, stable utility that nobody has touched in three years. The page cannot tell finished from abandoned; the case sits at the long-untouched position — here dark_room, 79 rooms. A narrowing that is this page's, not the spec's (D-070): the long-untouched rooms that are also high-load (foundation) — 16 rooms — lib/cli-engine/hash.js (27 lines, 3 importers, 1 from test files); lib/config/default-config.js (66 lines, 4 importers, 1 from test files); lib/languages/js/source-code/index.js (5 lines, 8 importers, 4 from test files); lib/languages/js/source-code/token-store/cursor.js (69 lines, 5 importers, 0 from test files); lib/linter/code-path-analysis/code-path-segment.js (228 lines, 4 importers, 1 from test files); lib/linter/index.js (8 lines, 9 importers, 5 from test files); lib/rule-tester/index.js (5 lines, 6 importers, 5 from test files); lib/rules/utils/keywords.js (66 lines, 3 importers, 0 from test files); lib/rules/utils/regular-expressions.js (50 lines, 4 importers, 1 from test files); lib/shared/assert.js (18 lines, 5 importers, 0 from test files); lib/shared/ast-utils.js (27 lines, 7 importers, 2 from test files); lib/shared/deep-merge-arrays.js (52 lines, 3 importers, 1 from test files); lib/shared/logging.js (33 lines, 3 importers, 1 from test files); lib/shared/severity.js (45 lines, 4 importers, 1 from test files); lib/shared/string-utils.js (47 lines, 8 importers, 1 from test files); lib/shared/traverser.js (177 lines, 7 importers, 3 from test files) — which the page places and does not judge.

## Provenance

- brief_version: `0.41.0`
- facts_hash: `e6c1f73164203a10f9e4eba0a52e147fca4f7afb7d8888b9eeaa030f05b90c0c`
- generator: `code`
