# mcp-secure-server — architect's brief

*Rendered from the facts sheet by code; no model wrote any of it. Every cell is a field, and every fixed text on the page has a test that asserts it against the sheet it is rendered from (`tests/test_brief.py`); the tests run on a fixture, not on this page. Profile maintainability 0.3.0 + onboarding 0.3.0, geometry age, skeleton `696ebaf89e10…`, facts `1e24c1701213…`. As of 2026-09-10, commit `5348b3ef614d`. Calibration: in-repo, self-relative (system spec §5.3), 15 package scopes pooled — per-package calibration is an open question of the mapper (architect-brief spec §5, mapper §7 Q7); one frame — stability is the `substrate timelapse` run under gate `bae5f62948e7`, not this page. Brief 0.33.0.*

## Register

**A position names where a room sits in a record — the import graph, the clock, the test graph, the edit record, size — and is not a claim about the room's condition (D-004 Q3). Every pNN ranks this repository's own 187 rooms, so a `>= p90` row holds a tenth of them — or more where rooms tie at the cutoff, and the rooms column says how many are tied; a single-rank row states its share, and every ranked term states the value its rank resolved to here — and no count on this page compares across repositories. A room is a source file outside the test convention with computed signals, and not a config or migration or placeholder file (the ruleset excludes those kinds): 187 of the 264 files with a source extension the substrate reads (manifests, documents and the rest of the tree are not counted); the 61 test files are nodes of the import graph and not rooms; the 16 files of an excluded kind (config 14, placeholder 2) are in the graph and not a room.**

*187 rooms in 2 wings (cookbook 122 · src 65); 285 diagnostic marks across all profiles (200 in the base profile), one mark per feature per room; identical pairs of diagnostic features mark 19 rooms twice (19 under one predicate in two profiles, 0 where two predicates draw one set because a conjunct excludes nothing, 0 under both); the diagnostic features name 10 distinct sets of rooms; 23 excluded marks ◌ (crack 20, toothpick_wing 3); 87 rooms carry two or more distinct diagnostic sets; gate `bae5f62948e7`, 7 of 8 signals asserted, none validated — asserted is a description that held under the stability budget and the corroboration its grounding class requires (validation spec §2.4), validated a forecast confirmed by a temporal holdout (validation spec §3).*

- **wing** — a directory at depth 1 of the tree (the ruleset's wing_depth), not a package; the population spans 15 package scopes, pooled (rooms per scope, largest first, each scope named by the manifest that holds it: package.json 65 · cookbook/image-gen-server/package.json 16 · cookbook/transaction-server/package.json 15 · cookbook/advanced-validation-server/package.json 12 · cookbook/cli-wrapper-server/package.json 12 · cookbook/database-server/package.json 12 · cookbook/monitoring-server/package.json 11 · cookbook/api-wrapper-server/package.json 8 · cookbook/filesystem-server/package.json 8 · cookbook/multi-endpoint-server/package.json 8 · cookbook/kenpom-server/package.json 7 · cookbook/nba-server/package.json 7 · cookbook/http-server/package.json 4 · cookbook/package.json 1 · cookbook/tool-policies-server/package.json 1); 20 of 447 imports cross a package scope.
- **◌** — an excluded feature (the ruleset's word is decorative): computed and counted, excluded from the diagnosis because a signal it reads is unvalidated.
- **largest parent directory** — the immediate parent (non-recursive) holding the most of a feature's rooms, shown only when it holds a 3rd or more of them and the feature has 6 or more rooms; a parent that shares a wing's name is marked as the parent.
- **relation to** — identity and containment, and only those, between features, diagnostic or decorative, with 3 or more rooms — and a containment the predicates guarantee at any count; two sets that overlap without one containing the other are not related here, and 'no identity or containment' says exactly that. 'By its predicate': the inner predicate conjoins every term of the outer. Otherwise the cell says which raw signals the two predicates read in common (a blend or index expanded through its declared inputs), or 'no raw signal in common' — a signal, not an instrument.
- **the import graph and the test graph** — one edge set read twice: a test file is a node whose imports count in fan_in and centrality, and test_fan_in counts those importers alone; an import-graph row says how many of its rooms' importers are test files. Centrality is PageRank over that graph: a room's rank rises with the rank of its importers, not only with their number, so a room with four well-placed importers can outrank one with thirteen. The graph is resolved statically: 0 imports in the tree did not resolve to a file, and 256 are external packages; an import the resolver did not place, or one computed at run time, is not an edge, so a room reached only that way reads as unimported there. The resolver was given the 2 path-alias patterns tsconfig.json declares. 19 imports name a package this repository declares and resolve to its entry.
- **caveat** — the ruleset's own limit on what a predicate reads, never a claim about this repository; the count in it ('N of M here') is this repository's, beside the clause it counts.

| position (the record it reads) | feature | profile | rooms (tied at the cutoff) | by wing | largest parent directory n / rooms in it | relation to (= one set · ⊂ inside · ⊃ contains) | predicate (with the cutoffs resolved here); caveat, a limit on the predicate; or reason |
|---|---|---|---|---|---|---|---|
| long-untouched room (clock) | dark_room | maintainability | 34 (27 tied at the cutoff) | cookbook 27 (in 6 of the wing's 14 scopes), src 7 | none holds a third | ⊃ flooded_basement (by its predicate; 5 of these rooms outside it) | `last_touched_days >= p90` — rooms at or above this repository's p90 on last_touched_days (here 270.365 days): 34 of 187, 18.2%; the next value below the cutoff holds 24 rooms, 0.0195 days under it |
| long-untouched room above the load floor (import graph and clock and size) | flooded_basement | maintainability | 29 | cookbook 22 (in 4 of the wing's 14 scopes), src 7 | none holds a third | ⊂ dark_room (by its predicate; 5 dark_room rooms outside this set) | `last_touched_days >= p90 and load_index >= 0.10` — here p90 on last_touched_days is 270.365 days — importers of these rooms: 58, 18 (31%) from test files — caveat: reads a floor on the load blend, not an importer (D-048): a room that nothing imports can clear the floor on inverse fan-out and size alone (0 of 29 here have no importer) |
| high-load node (import graph and size) | foundation | maintainability + onboarding (one predicate, two profiles) | 19 | cookbook 2 (in 2 of the wing's 14 scopes), src 17 | none holds a third | ⊃ ◌ toothpick_wing (by its predicate; 16 of these rooms outside it) | `load_index >= p90` — rooms at or above this repository's p90 on load_index (here 0.77128): 19 of 187, 10.2%; the next value below the cutoff holds 3 rooms, 0.00088 under it — importers of these rooms: 145, 29 (20%) from test files |
| high-centrality node (import graph) | hub | maintainability | 19 | cookbook 8 (in 2 of the wing's 14 scopes), src 11 | none holds a third | ⊃ corridor (by its predicate; 3 of these rooms outside it) | `centrality >= p90` — rooms at or above this repository's p90 on centrality (here 0.01022): 19 of 187, 10.2%; the next value below the cutoff holds 1 room, 0.00052 under it — importers of these rooms: 103, 10 (10%) from test files |
| recently-touched room (clock) | lit_room | maintainability | 27 (11 tied at the cutoff) | src 27 | none holds a third | ⊃ ◌ toothpick_wing (reads last_touched_days with it; 24 of these rooms outside it) | `last_touched_days <= p10` — rooms at or below this repository's p10 on last_touched_days (here 55.0336 days): 27 of 187, 14.4%; the next value above the cutoff holds 1 room, 26.05 days over it |
| test-imported room (test graph) | scaffolding | maintainability | 72 | cookbook 36 (in 11 of the wing's 14 scopes), src 36 | none holds a third | no identity or containment | `reinforcement_index >= 0.5` — caveat: reads whether any file under the test convention imports the room (test_fan_in > 0, D-011): the index is 0 with no test importer and 0.5 or more with one, so 0.5 is its floor, not a midpoint; a helper or fixture under the test paths counts as a test importer, and one import suffices; a room that tests reach only through a re-exporting barrel has no test importer of its own |
| high-centrality junction with fan-out at or above the median (import graph) | corridor | onboarding | 16 | cookbook 7 (in 2 of the wing's 14 scopes), src 9 | cookbook/image-gen-server/src/providers 6 / 6 | ⊂ hub (by its predicate; 3 hub rooms outside this set) | `centrality >= p90 and fan_out >= p50` — here p90 on centrality is 0.01022, p50 on fan_out is 1 — importers of these rooms: 96, 10 (10%) from test files |
| import-graph root with fan-out at or above the upper quartile (import graph) | import_root | onboarding | 15 | cookbook 15 (in 10 of the wing's 14 scopes) | none holds a third | no identity or containment | `fan_in == 0 and fan_out >= p75` — here p75 on fan_out is 3 — caveat: reads no fan-in, not entrance (D-029): a package entry that anything imports — a room or a test file — cannot qualify, and one that nothing imports can (10 of 15 here are declared package entries) |
| imported leaf with fan-in at or above the upper quartile (import graph) | leaf_utility | onboarding | 19 | cookbook 13 (in 5 of the wing's 14 scopes), src 6 | none holds a third | no identity or containment | `fan_out == 0 and fan_in >= p75` — here p75 on fan_in is 3 — importers of these rooms: 68, 23 (34%) from test files |
| declared package entry (package manifest) | package_entry | onboarding | 16 | cookbook 13 (in 13 of the wing's 14 scopes), src 3 | none holds a third | no identity or containment | `is_package_entry == 1` |
| high edit-pressure node (clock and edit record) | ◌ crack | maintainability | 20 (2 tied at the cutoff) | src 20 | none holds a third | ⊃ ◌ toothpick_wing (by its predicate; 17 of these rooms outside it) | `bug_pressure_index >= p90` — rooms at or above this repository's p90 on bug_pressure_index (here 0.7761): 20 of 187, 10.7%; the next value below the cutoff holds 1 room, 0.0039 under it — ◌ excluded from the diagnosis: bug_pressure_index is unvalidated on the pre-registered test set (D-015): its tuned weights assign zero to fix history and it does not beat busyness by the margin. |
| unreinforced high-load node with high edit pressure (import graph and clock and test graph and edit record and size) | ◌ toothpick_wing | maintainability | 3 | src 3 | too few rooms to place (3) | ⊂ ◌ crack (by its predicate; 17 crack rooms outside this set); ⊂ maintainability/foundation = onboarding/foundation (by its predicate; 16 maintainability/foundation rooms outside this set); ⊂ lit_room (reads last_touched_days with it; 24 lit_room rooms outside this set) | `load_index >= p90 and bug_pressure_index >= p90 and reinforcement_index <= 0.0` — here p90 on load_index is 0.77128, p90 on bug_pressure_index is 0.7761 — ◌ excluded from the diagnosis: bug_pressure_index is unvalidated on the pre-registered test set (D-015): its tuned weights assign zero to fix history and it does not beat busyness by the margin. The fragility half of this feature has no forecast to stand on; it renders as decorative, counted, and excluded from diagnostic claims until a reference repo validates the index. — importers of these rooms: 17, 0 (0%) from test files — caveat: reads test_fan_in == 0 as the graph resolved it (D-011): a room whose tests import it through an alias the resolver did not place, or only through a re-exporting barrel, reads as unreinforced |

### Rooms at the most positions (5)

**Every room at the most positions, in path order — a set, not a ranking and not a severity (D-004 Q3).**

*Every room under the most distinct diagnostic sets (5), by path — 5 rooms. A set is a distinct set of rooms a diagnostic feature draws (a feature under two profiles, or two features drawing one set, is one set). Rooms at fewer positions are not listed; 87 rooms carry two or more distinct sets. Size is the room's line count; importers is the room's fan_in, with the test files among them in parentheses.*

| room | lines | importers (from test files) | diagnostic features that mark it |
|---|---|---|---|
| src/index.ts | 169 | 21 (4) | corridor, foundation (2 profiles), hub, package_entry, scaffolding |
| src/security/config/tool-policies-config.ts | 301 | 6 (2) | corridor, foundation (2 profiles), hub, lit_room, scaffolding |
| src/security/config/tool-policies.ts | 111 | 6 (2) | corridor, foundation (2 profiles), hub, lit_room, scaffolding |
| src/security/layers/layer-utils/semantics/semantic-quotas.ts | 159 | 4 (1) | dark_room, flooded_basement, foundation (2 profiles), leaf_utility, scaffolding |
| src/security/utils/error-sanitizer.ts | 485 | 6 (1) | corridor, foundation (2 profiles), hub, lit_room, scaffolding |

### Shared rooms

*Rooms both features mark, for every pair of diagnostic features (features drawing one set of rooms share one row, named with each name); the diagonal is the feature's own count. A shared count equal to the smaller of the two features' own counts is containment, and equal to both is identity; the relation column above draws those, and only between features with 3 or more rooms or where the predicates guarantee the containment — any other pair under that floor is read here and not there.*

| shared rooms | dark_room | flooded_basement | maintainability/foundation = onboarding/foundation | hub | lit_room | scaffolding | corridor | import_root | leaf_utility | package_entry |
|---|---|---|---|---|---|---|---|---|---|---|
| dark_room | **34** | 29 | 1 | 3 | 0 | 14 | 3 | 3 | 2 | 0 |
| flooded_basement | 29 | **29** | 1 | 3 | 0 | 14 | 3 | 0 | 2 | 0 |
| maintainability/foundation = onboarding/foundation | 1 | 1 | **19** | 10 | 12 | 13 | 9 | 0 | 3 | 1 |
| hub | 3 | 3 | 10 | **19** | 6 | 5 | 16 | 0 | 1 | 1 |
| lit_room | 0 | 0 | 12 | 6 | **27** | 19 | 6 | 0 | 1 | 1 |
| scaffolding | 14 | 14 | 13 | 5 | 19 | **72** | 5 | 0 | 15 | 2 |
| corridor | 3 | 3 | 9 | 16 | 6 | 5 | **16** | 0 | 0 | 1 |
| import_root | 3 | 0 | 0 | 0 | 0 | 0 | 0 | **15** | 0 | 10 |
| leaf_utility | 2 | 2 | 3 | 1 | 1 | 15 | 0 | 0 | **19** | 0 |
| package_entry | 0 | 0 | 1 | 1 | 1 | 2 | 1 | 10 | 0 | **16** |

## Excluded marks (◌)

◌ crack 20 · ◌ toothpick_wing 3 — 23 marks computed and counted, excluded from the diagnosis: the signal they read (bug_pressure_index) is unvalidated; each row carries the reason.

## Stance

The diagnosis presupposes a maintenance norm — that the positions it marks are worth a visit — which the reader may reject; it is stated as an ought, not a fact (system spec, stance disclosure).

## Provenance

- brief_version: `0.33.0`
- facts_hash: `1e24c1701213685407aeb9f98b1bcb579b6e7b0fa6bb8a41bb5474c9fe0d5a7a`
- generator: `code`
