# Decision log — codebase-as-structure

*Append-only. Each entry records a decision, the reasoning at the time, the assumptions it rests on, and what breaks if those assumptions change. Entries are never edited after the fact; a reversal is a new entry that cites the old one. This is the stratum a future reader digs through to learn why the artifact is shaped the way it is.*

*Format: `D-NNN · date · title` / Decision / Why / Assumes / Breaks if / Supersedes.*

---

## D-001 · 2026-09-04 · Claude runs point; Alex steps in for guidance, questions, and audit pipelines

**Decision.** Claude makes the core product decisions (features added, removed, modified; scope; sequencing; tooling) and executes. Alex provides occasional guidance, answers questions, and proposes audit pipelines. Decisions are logged here before or alongside the work they govern.

**Why.** Alex proposed it as an experiment in how far the project can be taken under delegated ownership. The project had stalled for eleven weeks on a single unsigned proposal; the cost of waiting exceeded the cost of a wrong call that can be reversed.

**Assumes.** Every decision is reversible by a later entry; nothing outward-facing (publishing, pushing to a remote, external services) is done without Alex's explicit go.

**Breaks if.** Alex wants decision rights back — he says so and this entry is superseded.

## D-002 · 2026-09-04 · Repository initialized; the June 20 corpus is the baseline commit

**Decision.** The project directory becomes a git repo. The first commit is the spec corpus exactly as found on 2026-09-04 (last modified 2026-06-20), including the tribunal report and the unsigned proposal.

**Why.** A project whose subject is git history was not itself under version control. Every later decision needs a diffable baseline to be attributable.

## D-003 · 2026-09-04 · "Completion" is defined in three milestones; M1 is the product

**Decision.**
- **M1 — repo-substrate, validated.** The C1 CLI emits `substrate.json` and the markdown report; the validation gate runs the temporal holdout on the reference set and emits `validation.json` and the holdout report; the C1, validation, and integration checklists are green. This is the standalone product the substrate spec already claims must be valuable even if the renderer never ships.
- **M2 — the diagnosis.** C3 built as a minimal authored ruleset with the anti-horoscope gate enforced, producing `skeleton.json`, plus a deterministic 2D cutaway render (C6) with C4 folded into it. No LLM in the path.
- **M3 — the narrative.** C5 architect brief under the adversarial lens, with the register lint. Optional; built only if M2 proves the skeleton is worth narrating.

**Why.** The system spec's six components span from a deterministic extractor to a diffusion renderer. Treating all six as one deliverable is how the project stalled. M1 is where every falsifiable claim lives; M2 is where the thesis (metaphor as faithful function of signal) gets its first real test; M3 is decoration until M2 exists.

**Assumes.** The substrate spec §1 framing — the substrate is the product — still holds.

**Breaks if.** Alex wants the render first. Then M2's cutaway gets pulled forward against unvalidated signals, which the anti-horoscope contract forbids; that would need a `decorative`-only render mode as a new decision.

## D-004 · 2026-09-04 · The asserted-tier proposal: adopted with one substitution

Resolves the four questions the June 19 proposal left open. Referenced by the tribunal as remediation items #5, #6, #9.

**Q1 — Walk back the parity claim.** *Adopted as proposed.* Keep "different kind," drop "not a weaker one / neither subsumes." The min-over-signals operator in the mapper stays; it was right. The six places in the live specs that assert parity are rewritten.

**Q2 — Blind pre-registered recognition as the `asserted` correctness bar.** *Adopted in principle, protocol substituted.* The proposal requires three or more developers who know the repo to rank files blind, on two or more repos. UluOps has one developer. As written, 2b cannot run, and a correctness gate that cannot run is `untested` forever, which makes the whole `asserted` tier unreachable. The substitute keeps what 2b was for (a falsifier a constant cannot pass, produced without seeing the metric's output) and drops the panel:

  - **2b′ — cross-modal corroboration of the description.** A descriptive signal earns its correctness half by rank-correlating (Kendall τ-b, lower CI bound ≥ a config floor, default `0.30`) with a measurement of the *same present-tense property* from a *different modality that is not in its formula*. For `load_index` (static import topology) the second modality is co-change coupling from history (`cochange_degree`, substrate §5.2 Tier 2, promoted to Tier 1 by this decision). For `neglect_index` it is blame-age of surviving lines. For `reinforcement_index` it is coverage data when present, else `untested`. For `complexity_proxy_index` it is real cyclomatic complexity on the languages where a parser is cheap. This is the §3A pattern (a second, separately-derived label) applied one tier down. It can return negative.
  - **2b″ — recorded n=1 blind ranking.** Alex produces a blind top-10 "load-bearing" and top-10 "I distrust this file" list per reference repo *before* seeing any substrate output; it is sealed (committed by hash before the run). It is reported as a recognition check, not gated, because n=1 cannot carry a gate.
  - Stability (2a) stays exactly as proposed and remains mandatory. `asserted` ≝ 2a ∧ 2b′.

**Q3 — Feature-name leak.** *Disclosure plus lint now; position-names in the C3 ontology.* The C5 brief must state that a name denotes structural position, not a breakage forecast, until `blast_radius_index` is validated. A deterministic post-hoc lint over the brief flags consequence vocabulary sourced from `asserted` signals. The C3 ontology prefers position names ("hub", "junction") where the consequence name adds nothing.

**Q4 — Numbers.** *Placeholders accepted* (ε=0.05, δ=0.15, K=5, τ≥0.30 for 2b′, M≥2). Tuned after the first real holdout, recorded as config diffs.

**Why the substitution.** The tribunal's deepest point was that recognition is confirmation-biased by construction. A human panel fixes the bias but is unavailable. A second modality that was not in the formula fixes the bias *and* is available. It is weaker than a panel in one way (both modalities are machine-derived from the same repo) and stronger in another (it is reproducible and cannot be flattered).

**Assumes.** `cochange_degree` is computable from the history miner without a new extraction engine (it is: pairwise co-occurrence over `nodes_touched`).

**Breaks if.** Co-change and import topology turn out to be so correlated on every repo that 2b′ cannot fail. Then it is not a falsifier and needs a third modality. The first holdout report will show the τ distribution and answer this.

**Supersedes.** The parity wording in validation §1, §2.1, §3.8; substrate §10; system §3; mapper §3.

## D-005 · 2026-09-04 · Scope cuts for M1

- **C2 (Narrative Reader) removed from v0 entirely**, not merely optional. Nothing in M1 or M2 reads `signal.json`. Mapper open question #4 closes as "no C2 coupling in v0."
- **C4 folds into C6.** For the 2D cutaway, massing is vertical strata plus feature placement; a separate `massing.json` artifact is ceremony. System open question #1 closes.
- **§3A corroboration stays specced but is not built in M1.** It is secondary by design, blocked on the independence precondition, and depends on a tracker corpus. Built after the holdout runs and only if the holdout leaves a `validated` signal for it to corroborate.
- **Reference set grows from two to four repos.** The two UluOps repos (registry-api, mcp-secure-server) are same-org, which the tribunal flagged as near n=1 generalization. Two public JS/TS repos with real fix history are added. Candidates chosen in a later entry once cloned and sized.

## D-006 · 2026-09-04 · Dependency extractor backend: dependency-cruiser, pinned

**Decision.** The v0 JS/TS `DependencyExtractor` shells out to `dependency-cruiser` (pinned exact version, recorded in `toolchain_versions`) and consumes its JSON output. The substrate owns the edge contract (self-loops dropped, duplicates collapsed, external vs unresolved split) over that output.

**Why.** It resolves `tsconfig` path aliases and reports unresolved modules distinctly from external packages, which is exactly the split substrate §6.3 needs and is the one piece a hand-rolled resolver would get wrong first. `tree-sitter` gives a parse, not a resolution; `madge` conflates unresolved with external. Cost: a Node toolchain dependency inside a Python project. Accepted, because the tool version is inside the fingerprint by construction.

**Assumes.** dependency-cruiser's output is deterministic for a fixed version and config. Verified by golden test before trust.

**Breaks if.** Alias-heavy repos produce confident mis-resolution (substrate open question #8). The spot-validation planned there is the check.

## D-007 · 2026-09-04 · Tracker reconciliation before building

**Decision.** The tracker project carries 50 open issues; roughly a dozen were resolved in the specs on June 19–20 without being closed. Each is closed with a note citing the spec section that resolved it before any new issues are logged. The tracker is the work queue only once it is true.

## D-008 · 2026-09-04 · Grounding classes for descriptive signals; the load family gets a second instrument, not a second modality

**Context.** The first run of the gate on `mcp-secure-server` and `uluops-registry-api` returned: `age_days` ↔ `blame_age_median` τ 0.77 / 0.78 (a genuine cross-modal confirmation); `neglect_index` ↔ blame τ 0.43 / 0.65; but the load family ↔ `cochange_degree` τ 0.24 / 0.44 with lower CI bounds below the 0.30 floor on the smaller repo, and `last_touched_days` ↔ blame τ 0.23 / 0.34. Several stability failures traced to the protocol counting files the removed commits had directly edited.

**Decision.**
1. **Stability compares only nodes untouched by the K removed commits.** A file edited in those commits has its recency, churn, and co-change legitimately move; that is the signal reporting the edit. The budget measures whether editing *other* files moves *this* file's value. `p95` is reported beside `max`.
2. **Descriptive signals are grounded by class**, recorded in `validation.json` as `grounding_class`:
   - **G1 measurement** — direct git or file facts (`commit_count`, `churn_lines`, `fix_count`, `revert_count`, `author_count`, `last_touched_days`, `size_loc`, `nesting_proxy`, `cochange_degree`, `blame_age_median`). Bar: stability. The instrument is git or the file; the metric's name is literal, so no meaning rides on the label. Popper's constant objection targets models, not measurements.
   - **G2 instrument-checked** — resolver-dependent facts checked against an independent second instrument measuring the *same property*: `fan_in`, `fan_in_nonzero`, `fan_out` ↔ `fan_in_alt` / `fan_out_alt` from a regex scanner with its own resolver (`altdeps.py`, no shared code with dependency-cruiser); `has_sibling_test` ↔ `test_fan_in` (importers that are test files). Bar: stability + τ-b lower CI ≥ `tau_instrument` = 0.60, set now, before the numbers are seen. Same property measured twice should agree strongly; a low τ means the instruments disagree on alias-heavy or dynamic-import code, which is substrate open question #8 made measurable.
   - **G3 cross-modal** — a different modality of the same property: `age_days`, `neglect_index` ↔ `blame_age_median`. Bar: stability + τ-b lower CI ≥ `tau_asserted` = 0.30.
   - **G4 derived** — fixed-weight blends and graph functions of asserted inputs: `centrality` (from `fan_in`), `load_index`, `complexity_proxy_index`, `reinforcement_index`. Bar: stability + every input asserted. The composite's name carries no claim beyond its inputs (D-004 Q3 handles the name); `cochange_degree` and `test_fan_in` are computed and **reported as correlates**, never gated.
3. `last_touched_days` moves from G3 to G1. Blame age measures how old the surviving text is; last-touched measures when the file was last edited. They are different properties, and the low τ was the counterpart being wrong, not the signal.

**Why.** D-004 asked for a counterpart "not in the formula" and named co-change for load. The run showed co-change is *change* coupling, and a foundation is precisely the file that is imported by many and changed by few, so the pairing was conceptually mismatched, not merely under-correlated. There is no independent measurement of "load-bearing" in v0 beyond the import graph; pretending otherwise would be the horoscope move in reverse. The honest position is that `load_index` is a named blend of independently checked measurements and claims nothing more. Lowering the τ floor until co-change passed was the deadline bypass the tribunal warned about and was not considered.

**Assumes.** The second scanner is genuinely independent (it is: separate module, no shared resolution code) and that τ ≥ 0.60 is the right agreement bar for two instruments on one property. Both are checked by the τ distribution in the report.

**Breaks if.** G4 is read as a loophole — a ruleset naming `load_index ≥ p90` "foundation" and C5 voicing "if this breaks, much falls." That is forbidden by §2.1.1 and D-004 Q3 regardless of G4; the register lint (M3) is the enforcement. **Flagged for audit**: this is the decision most worth a lens pass.

## D-009 · 2026-09-04 · Weight tuning is pre-registered: tune on two repos, verdict from the other two

**Context.** Both predictive indices failed the holdout on both small repos. `bug_pressure_index` ranked *below* the recency and busyness baselines (ROC −0.21 and −0.04). `change_pressure_index` beat the best baseline on ROC by +0.03 and +0.05 against a +0.05 margin, and on PR-AUC by ×1.69 and ×1.00 against ×1.20; it is, by construction, a blend of the two baselines and so cannot easily beat them by margin.

**Decision.** The §6.2.1 weights are tuned once, under this protocol, before any test-set number is seen:
- **Tuning set:** `uluops-registry-api` (956 commits, 132 holdout positives) and `eslint` (11,008 commits). One internal, one external, both with hundreds of positives.
- **Test set:** `typeorm` and `mcp-secure-server`. The `validated` verdict is computed **only** from the test set; tuning-set passes are reported but do not count. `min_repos` = 2 therefore means both test repos must pass.
- **Search:** a grid at 0.1 steps over each index's inputs (weights summing to 1), inputs for `bug_pressure_index` widened to {`fix_count` pctile, `fix_count_nonzero`, `revert_count`, `fix_ratio`, recency, `commit_count` pctile}. Objective: the **minimum** over tuning repos of (index ROC-AUC − best-baseline ROC-AUC), tie-broken by the minimum PR-AUC ratio. The minimum, not the mean, so one repo cannot carry the other.
- **Frozen** into `config/tuned.toml` and committed before the test set is run. The commit hash of the frozen config is recorded in the holdout report.
- The pass margins (+0.05, ×1.20) are **not** tuned. The interaction between the multiplicative PR margin and high base rates is noted in the report as a limitation, not adjusted away.

**Why.** The spec always said the weights are placeholders tuned against the holdout, held out not in sample. Tuning without pre-registration is the researcher-degrees-of-freedom leak the corroboration section already closes for its own thresholds; the same discipline applies here.

**Breaks if.** The tuned indices still fail the test set. Then the finding ships as `unvalidated`, the report says by how much, and C3 may not name a feature over either index. That outcome is acceptable; hiding it is not.

## D-010 · 2026-09-04 · The sealed recognition rankings are model-assisted priors, accepted as the n=1 substitute

**Context.** Alex had a separate Claude (Fable 5.1) session fill `blind/uluops-registry-api.md` and `blind/mcp-secure-server.md` from its session memory of those repos plus `git ls-files` (to make paths valid), and reviewed and endorsed the result. Each file states its own provenance: no `git log`, no `git blame`, no import tracing, no file contents, and no substrate output read. Substrate reports for both repos existed on disk in `out/` at the time; the provenance statements say they were not read, and the rankings contain items (`CHANGELOG.md`, `package.json`) that the substrate does not even emit, which is consistent with that.

**Decision.** Accept both files as the §2.4.3 recognition record, sealed at this commit, with the provenance carried into the holdout report verbatim: "n = 1, model-assisted, maintainer-endorsed." They remain non-gating, as §2.4.3 already requires. The next repos' rankings, if any, follow the same rule: provenance stated in the file, sealed before that repo's first gate run.

**Why.** The alternative was no recognition record at all. A model's priors endorsed by the maintainer are a weaker witness than the maintainer's own blind list, and a stronger one than nothing; since the record never gates, the cost of accepting it is bounded to the report's honesty, which the provenance note preserves. Section 5 ("the one structural fact") and section 6 ("where the metrics will lie") of each file are the more valuable content: they are pre-registered predictions about where the substrate will mislead, and the report should check them.

**Breaks if.** A future ranking is produced after reading substrate output. Then it is not a recognition check and must not be sealed; the file's own provenance line is the control.

## D-011 · 2026-09-04 · What the four-repo baseline and the second instrument found; reinforcement re-grounded on the import graph

**Context.** The first full run (four repos, placeholder weights) and the first review pass (python-validator, code-auditor, test-architect, software-architecture-expert-validator, popper-validator, circumvention-forecaster; tracker runs 4–9) landed together. Findings that changed the design:

1. **The second import instrument caught the primary one on its first outing.** On typeorm, `fan_in` ↔ `fan_in_alt` was τ 0.37 against 0.84–0.98 elsewhere. Cause: dependency-cruiser drops `import type` edges unless told to analyze pre-compilation dependencies; `src/data-source/DataSource.ts` had 9 importers on the primary instrument and 621 on the scanner. For a structural map a type import is a compile-time dependency, so `--ts-pre-compilation-deps` is now on, as a fingerprinted config value. This is substrate open question #8 (extractor mis-resolution) answered by measurement rather than spot-checking.
2. **Every substrate run now carries its own instrument-agreement check.** `summary.fan_in_instrument_tau` and `graph_instruments_disagree` (floor 0.60, config) are computed at extraction, and disagreement degrades the graph exactly as a low resolution rate does. The reference-set verdict is global; this makes the G2 check local to the repo being rendered, which is where C3 needs it.
3. **Reinforcement is re-grounded on the import graph.** `has_sibling_test` (path convention) disagreed with `test_fan_in` (a test imports the file) on three of four repos; on typeorm, whose tests are named by feature, the convention is simply wrong. `reinforcement_index` is now `0` when no test imports the file, else `0.5 + 0.5 · percentile among imported files`; its G2 counterpart is `test_fan_in_alt` from the independent scanner (Popper C-2B: the earlier pairing shared the primary resolver). `has_sibling_test` stays as a G1 measurement with a literal name and a declared heuristic, and is reported as a correlate.
4. **`fan_in_nonzero` is G4 over `fan_in`** (a monotone re-ranking, not a second measurement); the June-style lookup bug that made it permanently `too_few_pairs` is fixed.
5. **G1 carries declared heuristics and a degeneracy check** (Popper C-1). `fix_count`, `revert_count`, `author_count`, `nesting_proxy`, `blame_age_median`, `has_sibling_test` state their heuristic in the grounding table and the report; any signal with fewer than 3 distinct values across the population is `degenerate`, never `asserted`. The G1 membership is pinned by test to the spec table (circumvention A5).
6. **Stability has floors** (Popper C-5): `untested` (`insufficient_stability_population`) when fewer than 30 untouched nodes are compared or more than half the population was touched by the removed commits.
7. **A retirement criterion for corroboration pairs** (Popper C-2A): a G2/G3 pair whose bootstrap lower-CI τ is ≥ 0.85 on every reference repo is flagged `non_discriminating` in the report; an adversarial fixture is then required before it counts. On this run no pair trips it: `fan_in` τ ranges 0.37–0.98, `age_days` 0.38–0.78.
8. **Recognition leaves the verdict table** (Popper C-8) and lives in its own section with its provenance and non-gating status stated.

**Assumes.** The scanner is independent enough of dependency-cruiser to be a check on it (they share a JSONC parser for `tsconfig.json` and no resolution code).

**Breaks if.** A repo's tests reach the code only through runtime string paths or a bundler alias neither instrument resolves; then `reinforcement_index` reads zero for a well-tested repo and the `graph_instruments_disagree` flag is the only warning. On such a repo the gate withholds `asserted` rather than guessing, which is the intended failure.

## D-012 · 2026-09-04 · The gate's floors are validated, its inputs attested, and its preimages published

**Context.** The circumvention forecast found that D-008/D-009/D-010 had moved the June exploits out of the specification and into configuration and operator discipline, and that the report omitted exactly the knobs an operator under deadline would turn. The code audit found five reproducible correctness bugs, two of which (rename onto a reused path; case-insensitive blob reads) corrupt values the seed and fingerprint cannot see.

**Decision.** Each is fixed in code and, where the design changed, on the page:

- **`ValidationConfig.validate()`** range-checks every floor; the spec defaults are minimums — a config may tighten a bar, never loosen it below spec (A1, A8). The report prints every floor under "Gate configuration."
- **Tuning/test roles are in the artifact** (A3; Popper C-6; the architecture validator's DIVERGENT). `run` takes `--test-repo` and `--tuning-repo`; only test-role repos count; `reference_repos[].role` and `expected_role` (from the pre-registered D-009 sets, mismatch flagged) and `tuned_config_commit` are recorded.
- **Cache integrity** (A2; audit): atomic writes, a corrupt or foreign-fingerprint entry is a miss, `truncate` is in the key, and every scored document is attested by its seed and the sha256 of its bytes in `validation.json`.
- **Preimage published** (A7): `substrate_effective_config` — the weights, regexes, and toolchain the fingerprint hashes — is embedded verbatim beside the validation config.
- **Counterpart independence is tested** (A4): `METRIC_INSTRUMENT` maps every metric to the instrument that produced it; a test asserts every G2/G3 counterpart comes from a different instrument and is not one of the signal's own formula inputs. `WEIGHT_KEY_SIGNAL` maps weight keys to signals and a test asserts G4 input lists equal the formulas (A6).
- **The holdout label is derived from a frozen validation-side regex** (`label_subject_regex`, A9), re-classifying commit subjects rather than reading the substrate's feature-side `type`. Narrowing `fix_subject_regex` cannot move a label; the report shows both and flags a difference.
- **Sealed rankings carry their git identity** (A10): committed blob, working-tree blob, first-commit hash and time are recorded per repo; a mismatch is visible.
- **The substrate report is stamped UNGATED** (A12) until C3 exists: it consults no `validation.json` and says so in its second line.
- **Audit fixes:** rename-target alias breaking (fix counts no longer land on the old lineage); blobs read by SHA via `git cat-file --batch`, not by worktree path; the split index guarded against wraparound; NaN objectives and one-class holdouts are `degenerate`, never a false FAIL or a frozen weight; a `None` index is `index_unavailable`, never a score of zero; blame failures and unreadable files are counted as instrument faults, not absences; UTF-8 pinned on every subprocess and file call; timeouts on every subprocess; the worktree temp dir cannot leak; the one unsorted output list is sorted; `pyproject` and `__version__` agree.

**Not done, logged to the tracker:** an adversarial synthetic repo for G2 (alias-heavy, dynamic imports); the edge-dropout sweep that would show whether `centrality` inherits `fan_in`'s certification; the population-drift variant of the stability check; spec §4/§6.1 schema blocks rewritten to the emitted shape; the wall-time split option removed from §3.1; checklists re-derived.

**Why.** The circumvention pass's summary is the reason: "nothing here requires expertise, because the gate's floors are literals in files the operator owns." The fingerprint was the right detector and was correctly built over config-space; it covered no bytes on disk and published no preimage. Both are now covered. What remains social is the choice of reference repos, which is pre-registered and flagged, and the honesty of a blind-ranking's provenance line, which is now cross-checked against git.

## D-013 · 2026-09-04 · Standing rules from the first review cycle; the review roster as an experiment; the name

**Context.** Alex had a Claude Desktop session read the tracker runs side by side and write `docs/notes-for-fable.md`. Three of its seven points were already in the D-012 batch; the rest are adopted here.

**Rule 1 — a decision that closes an exploit names the code that enforces it.** In the decision itself. A decision that cannot name code has not closed anything; it has moved the exploit from the specification into configuration or discipline, which is exactly what the circumvention forecast found D-008/D-009/D-010 had done. D-012 follows this rule; every later decision must.

**Rule 2 — the review roster is recorded with its rationale, so the roster choice can be scored later.** Alex delegated lens selection, which makes each review cycle a dataset: a model-chosen validator roster against a real corpus, scorable by RAH for whether the picks were the reliable agents or the relevant-sounding ones. First cycle (tracker runs 4–9, workflow `m1-review`):

| Agent | Why chosen | Decision / score | Verdict on the choice |
|---|---|---|---|
| python-validator | mechanical hygiene | PASS 77 | correct, low information |
| code-auditor | runtime correctness; subprocess/worktree/NaN paths | UNSOUND 68 | **highest yield** — five reproduced bugs, two invisible to the fingerprint |
| test-architect | the golden test is what determinism rests on | IMPROVE 74 (auto-fail) | correct — mutation testing proved the verdict path was unguarded |
| software-architecture-expert-validator | four specs claim things about the code nobody had checked | IMPRECISE 74 | correct — caught networkx, toolchain roles, §6.1 shape drift |
| popper-validator | the June root finding was Popper's; closure test | UNCORROBORATED 87 | **correct** — predicted the `fan_in_nonzero` bug before the run confirmed it |
| circumvention-forecaster | the June highest-severity lens; scored 38 then | VULNERABLE 88 | **most informative number on the project** — the "exploits moved, didn't close" pattern |

Not chosen: Husserl (fulfillment audit fits M2, when there is a rendered experience), Hume (re-run at M2, when the stance becomes visible to a viewer), Wittgenstein (deferred to the C3 ruleset, where the naming lives).

Definition versions and token counts were not attached to runs 4–9 at save time; attaching them is a tracker follow-up, and future saves carry them.

**Rule 3 — a minimal consumer of `validation.json` exists from early M2.** Nobody shares a `validation.json`; the cutaway is the distribution mechanism for the measurement, and a consumer, even a throwaway one, is what makes the gate real (circumvention A12). M2 starts with a skeleton-schema sketch, strata plus one or two features, no C5.

**Rule 4 — the novelty claim is exact.** README states the prior art (CodeCity; CodeScene; Nagappan & Zimmermann; Google 2011) and claims only the temporal-holdout gate between metric and picture, citing only results the report has produced.

**Name (system-spec open question #5, substrate open question #9).** *Resolved.* The system is **codebase-as-structure** (the repository name, the parent spec, the tracker project). The standalone product — C1 plus the gate — is **repo-substrate** (the Python package and the CLI). Two names because they are two things: the second must be valuable even if the first never renders (`repo-substrate-spec.md` §1). No further naming work.

**Addendum to D-013 (2026-09-06, D-035).** Rule 5: every brief version bump, and every gate change that regenerates the briefs, seats a hostile reader on one brief and records which marks the reader judged unsupported; the lint is the authority for what it checks, the reading for whether the checks are the right ones.

## D-014 · 2026-09-04 · The tuned weights are frozen; two gate corrections from the placeholder run

**Context.** The four-repo run under the D-011/D-012 gate (out/validation-placeholder) and the pre-registered tuning (config/tuning.json) landed together.

**Tuning outcome (D-009 protocol, tuning set = uluops-registry-api + eslint, grid step 0.1, objective = min-over-repos ΔROC then min PR ratio).**

| index | best weights found | min ΔROC (bar +0.05) | min PR ratio (bar ×1.20) | spec placeholder |
|---|---|---:|---:|---|
| `bug_pressure_index` | commit_count 0.5, recency 0.2, revert_count 0.3 | **+0.014** | **1.00** | ΔROC −0.041 / −0.119 |
| `change_pressure_index` | churn_lines 0.5, commit_count 0.2, recency 0.3 | **+0.030** | **1.00** | ΔROC +0.052 / +0.030 |

No weight vector in either grid reaches the pre-registered bar on the tuning set itself. The best `bug_pressure_index` drops `fix_count` and `fix_ratio` to zero weight: on these repos, historical fix concentration carries no forecast of future fix activity beyond busyness and recency. That is the substantive finding of M1 so far, and it is what the spec said the gate exists to find ("write down where the indices lie"). The weights are frozen in `config/tuned.toml` at this commit, before the test set is scored; the margins are not touched (D-009). The expected verdict on the test set is `unvalidated` for both, and if so C3 may not name a feature over either.

**Correction 1 — degeneracy is a modal-value share, not a distinct-value count.** The D-011 check marked `has_sibling_test` degenerate on every repo because it is binary. A two-valued signal with a real minority class is not a constant. `degenerate_max_modal_share` = 0.97 replaces `degenerate_min_distinct`; a constant (share 1.0) still fails. Enforced in `validation/asserted.py::run_stability`; floor validated in `ValidationConfig.validate()`.

**Correction 2 — the retirement criterion is satisfied by a committed fixture, and says so.** After the `import type` fix the two import instruments agree at τ 0.98–1.00 on every reference repo, which trips the D-011 criterion for `fan_in`, `fan_out`, `test_fan_in`. The criterion's purpose was to refuse a counterpart that *cannot* fail; `tests/test_instruments.py` shows these can (the scanner over-counts an import-shaped string; the primary drops type edges with the flag off; the fingerprint moves with the flag). The grounding table now records `adversarial_fixture` per G2 pair, `validation.json` carries `retirement_backed`, and the report prints the fixture name or "NO FIXTURE." A pair that trips the criterion without a fixture would be reported as such; none does. Enforced in `validation/config.py` (`GROUNDING`), `validation/gate.py::descriptive_verdict`, `validation/report.py`.

**Observed and left as is.** `neglect_index` is unstable on `mcp-secure-server` (max Δ 0.207, p95 0.025 — one node) and passes on the other three; the `max` clause is deliberately strict and the signal is `asserted` at M = 2. `has_sibling_test` under the corrected check is expected to be `asserted` (G1) with its heuristic declared; it feeds nothing.

**Breaks if.** The test-set verdict comes back `validated` for either index despite the tuning set failing the bar — that would be a sign the split leaked, and the first thing to check is `substrate_attestations` and the split SHAs.

## D-015 · 2026-09-04 · M1 verdict: both predictive indices `unvalidated`; twenty-one descriptive signals `asserted`; what C3 may name

**The test-set verdict** (out/validation, tuned config commit 24c087a, test repos typeorm and mcp-secure-server, tuning repos in-sample for the record):

| index | repo | role | ROC vs best baseline | PR-AUC ratio | passed |
|---|---|---|---:|---:|---|
| `bug_pressure_index` | typeorm | test | 0.770 vs 0.728 (+0.042) | ×1.06 | no |
| `bug_pressure_index` | mcp-secure-server | test | 0.828 vs 0.825 (+0.004) | ×1.55 | no |
| `change_pressure_index` | typeorm | test | 0.878 vs 0.728 (+0.150) | ×1.21 | **yes** |
| `change_pressure_index` | mcp-secure-server | test | 0.856 vs 0.825 (+0.031) | ×1.68 | no |

`bug_pressure_index`: **unvalidated**, 0 of 2. `change_pressure_index`: **unvalidated**, 1 of 2 (the bar is both test repos). Tuning-set rows fail the same clauses, as the tuning already showed.

**What the numbers say.** With the tuned weights `bug_pressure_index` rank-correlates with the baseline it must beat at τ 0.72–0.83: it *is* busyness plus recency with a revert term, which is what a grid search over a space that cannot beat busyness will find. The June concept — "historical fix concentration forecasts future fix activity" — is not supported on these four repos beyond what busyness already carries. `change_pressure_index` carries real signal on the large TypeScript repo (+0.150 ROC, the only clean pass in the whole run) and beats the baselines everywhere but never by the margin on the small one, whose holdout is 28 commits and 22 positives.

**Consequences, per the anti-horoscope contract (system spec §3, mapper §3).**
- C3 may not name a feature over `bug_pressure_index` or `change_pressure_index` except as `decorative: true` with a `decorative_reason`. The toothpick's fragility half and the crack/fault-line features have no validated forecast to rest on in v0.
- C3 may name features over the twenty-one `asserted` signals, in the descriptive register only (§2.1.1): `load_index`, `centrality`, `fan_in`, `fan_in_nonzero`, `fan_out`, `neglect_index`, `age_days`, `last_touched_days`, `blame_age_median`, `reinforcement_index`, `test_fan_in`, `has_sibling_test`, `complexity_proxy_index`, `nesting_proxy`, `size_loc`, `commit_count`, `churn_lines`, `fix_count`, `author_count`, `cochange_degree`, `recent_commit_share`. Foundation, strata, corridor (position), flooded basement (load-bearing ∧ neglected), scaffolding (reinforcement), and lighting/material (recency/age) are all reachable as descriptions.
- `revert_count` is `untested (degenerate)`: on every reference repo the modal value (zero) covers more than 97% of files. It stays a raw metric and feeds nothing named.
- The G2 pairs are `non_discriminating` on the reference set and fixture-backed (D-014); `age_days` and `neglect_index` ↔ blame are discriminating on their own (τ 0.38–0.78).

**What M1 delivered.** A substrate that is byte-identical on re-run, attested by seed and content hash, with every value-affecting choice in a published fingerprint preimage; a gate whose floors cannot be loosened below spec, whose labels are frozen on the validation side, whose test/tuning roles are in the artifact, and which returned a negative result on the first thing it was built to test. That is the product the substrate spec §1 said must be valuable even if nothing renders. **M1 is complete** in the sense the spec defines; the checklists' remaining unchecked items are the known deferrals (§3A corroboration, the population-drift stability variant, the edge-dropout sweep for `centrality`, a generic degradation flag for the non-graph indices).

**What changes for M2.** The ruleset is authored over `asserted` signals in the descriptive register. The recognition record for `load_index` on registry-api is overlap 0.40 / τ 0.44 against a model-assisted n=1 ranking — reported, not evidence. The cutaway's first consumer of `validation.json` (D-013 rule 3) is the next thing built.

**Breaks if.** A future reference repo with a long, fix-tagged history and a stable busyness profile lets `bug_pressure_index` clear the bar. Then the verdict moves, the report shows which repo moved it, and the toothpick's fragility half becomes voiceable. Nothing about the design has to change for that; only the evidence.

## D-016 · 2026-09-04 · M2 begins with the consumer: C3 as a gated ruleset engine, C6 as a deterministic SVG cutaway

**Decision.** M2's first deliverable is the thing D-013 rule 3 asked for — something downstream that reads `validation.json` — built as the real C3 rather than a throwaway, because the gate logic is small and the throwaway would have had to implement it anyway.

- **C3 (`repo_substrate/mapper/`).** A ruleset is a versioned **TOML** document (`rulesets/<profile>.toml`): a header and `[[feature]]` entries, each a conjunction of `signal op value` terms where `value` is `pNN` (a percentile of that signal across the population, resolved at map time) or an absolute number. No `or`, no `not`: a feature is a conjunction and a different feature is a different conjunction. The spec said YAML; TOML parses with the standard library and carries no new dependency, and both are versioned diffable text, which is the property the spec wanted. **The gate runs before any predicate is evaluated**: every signal a non-decorative feature reads must be `validated` or `asserted` in `validation.json`, a missing key is `untested`, and any violation refuses the whole skeleton (`GateError`). A `decorative` feature must carry `decorative_reason` or the ruleset does not load. Feature status is the min-operator of mapper §5. `graph_dependent` features degrade (emitted, flagged, not diagnostic) when the substrate's graph is degraded or the node's `load_index_degraded` is set. Every feature instance carries its predicate, the resolved thresholds, the evidence values, and each signal's status. `skeleton.json` records the substrate seed, both fingerprints, the ruleset name and version, a content hash of itself, and a `summary` with `diagnostic_count`, `decorative_count`, `degraded_count`, and `decorative_features`. The archetype is `null`: mapper §7 Q1 is unresolved and therefore not claimed.
- **The maintainability ruleset v0.1.0.** Six diagnostic features over `asserted` signals — `foundation` (load p90; `name_implies_consequence`, position name "high-load hub"), `hub` (centrality p90), `flooded_basement` (neglect p90 ∧ load ≥ 0.10, the substrate §9 query), `scaffolding` (reinforcement ≥ 0.5), `dark_room` / `lit_room` (last-touched p90 / p10) — and two **decorative** ones, `toothpick_wing` and `crack`, both over `bug_pressure_index`, each with a `decorative_reason` citing D-015. They are kept so that their absence from the diagnosis is visible on the picture rather than silent.
- **C6 (`repo_substrate/cutaway.py`), C4 folded in.** A deterministic SVG: wings are top-level directories (columns), strata are five `age_days` percentile bands with the oldest at the bottom, rooms are files with width ∝ √lines, material is age, lighting is recency, and features are overlays with distinct styles. The banner prints the diagnostic count, the decorative count in a warning colour, the degraded count, the gate's per-signal statuses, and every fingerprint. Decorative features are drawn dashed in a colour no diagnostic feature uses. Each room's tooltip carries its path, its raw numbers, and every feature that fired on it with its position name. No randomness: same skeleton and substrate, same bytes. The seed is recorded for the stochastic form grammar a later phase may add.
- **Geometry is profile-independent** (mapper §6): strata and wings come from the substrate; the ruleset only decides overlays. `overlays[]` is present and empty; the second profile is the next ruleset.

**First run, all four repos, over the D-015 verdict:** every non-decorative feature admitted; `crack` fires 22–59 times per repo and `toothpick_wing` 0–3 times, all decorative and counted; nothing degraded. On registry-api the toothpick never fires because every top-decile-load file is imported by a test.

**Assumes.** Five age bands and directory wings are an adequate first geometry. They are the cheapest geometry that makes the age-strata metaphor literal; a dependency-layering geometry (topological rank) is the alternative the system spec names and is deferred.

**Breaks if.** A reader takes `foundation` as a forecast. The feature carries `name_implies_consequence: true` and a position name, the banner says every feature denotes present position, and the tooltip prints the position name; the C5 lint (M3) is the last line. On this run no feature over a `validated` signal exists, so no forecast register is available to anything.

**Seen on the first pictures (D-016 addendum).** On `mcp-secure-server` the whole `cookbook` wing sits in the flooded-basement band — old, untouched, still imported — which is exactly what the sealed ranking's section 6 pre-registered ("cookbook/** will dominate … leaf consumers"). On `uluops-registry-api` the foundation outlines land on `src/utils/errors.ts`, `src/config/index.ts`, `src/db/connection.ts`, three of the ranking's top four load-bearing files, and the flooded basement is the bottom band of `src/utils`. Reported here as recognition, not evidence (n = 1, model-assisted); the pictures are in `reports/2026-09-04-m2/`.

## D-017 · 2026-09-05 · Overlays, two geometries, and a skeleton diff; geometry leaves the profile

**Decision.**
- **Overlays are implemented as mapper §6 specifies: layering, never unification.** `substrate map` takes one base ruleset and any number of `--overlay` rulesets. Each is gated on its own signals; each produces its own feature list and summary under `overlays[]`; duplicate profiles are refused. Geometry (strata, wings) is computed once from the substrate and is byte-identical whether or not overlays are present (tested). Nodes flagged by more than one profile are listed in `co_located_nodes` and counted — shown, never averaged. In the cutaway the base profile styles the room and each overlay adds a corner badge in its own hue; the HTML wrapper (`--html`) toggles overlay layers, and the base cannot be turned off because it is the building.
- **Geometry is a substrate property, chosen at map time, not a profile property.** `strata_signal` is removed from the ruleset. `--geometry age` (five `age_days` bands, oldest at the bottom) or `--geometry layer` (dependency layering: longest import path to a leaf over the SCC condensation, leaves at the bottom — the system spec §5.1's primary strata definition). The chosen geometry is recorded in `skeleton.json` and captioned on the picture. Cycles collapse to one layer; the condensation is Tarjan's SCC, iterative.
- **Second profile: onboarding v0.1.0** — `entrance` (fan_in 0 ∧ fan_out ≥ p75), `corridor` (centrality ≥ p90 ∧ fan_out ≥ p50), `foundation` (shared with maintainability, so co-location is visible), `leaf_utility` (fan_out 0 ∧ fan_in ≥ p75). All over asserted G2/G4 signals, all position names.
- **Skeleton diff** (`substrate skeleton-diff before after --renames <substrate>`): per (profile, feature) added/removed nodes and Jaccard over nodes present in both skeletons, a summary `feature_churn` (symmetric difference over union), and `strata_moved`. Born and deleted nodes are excluded from churn and counted separately — the same discipline as the signal-level budget (validation §2.4.1). This is the instrument for mapper §7 Q3; the budget number itself is not yet set. It is also the diff the Phase 1 time-lapse will consume.

**Why geometry left the profile.** If a profile could choose the strata, two lenses over one repo would draw two buildings, and mapper §6's whole point — the same room, marked twice — would be lost. A geometry is a way of drawing the substrate; a profile is a way of reading it.

**Assumes.** Corner badges at 7px read at the room sizes in use; on very small rooms (10px wide) two badges touch. Acceptable for v0.

**Breaks if.** A third overlay is added: corners run out at four. The badge scheme is a v0 encoding; a fifth profile needs a different mark.

**First skeleton-diff reading (D-017 addendum).** `uluops-registry-api`, age geometry, maintainability + onboarding, HEAD−5 commits → HEAD: 267 common nodes, 0 born, 0 deleted; `feature_churn` 0.012; 3 nodes changed stratum (1.1%); per feature, `dark_room` +1 and `flooded_basement` +2/−1 (Jaccard 0.96 and 0.90), everything else identical. Five commits moved about one percent of the named structure. A budget in the region of "≤ 5% feature churn and ≤ 3% strata movement per K = 5 commits" would pass this repo comfortably; it is not pinned until the other three repos are measured the same way.

**Predicate semantics (D-017 addendum).** The first layer-geometry render showed the onboarding `entrance` and `leaf_utility` features never firing: a bare metric name in a predicate resolved to its *percentile*, and an ECDF percentile is never 0, so `fan_out == 0` could not be true. Rule, now enforced in `mapper/engine.py::_node_value` and stated in the ruleset grammar: a predicate name denotes an index by its index name, otherwise the raw metric; `pNN` thresholds are ranked over those raw values; only `*_nonzero` names resolve to percentiles. Absolute thresholds therefore mean what they say on the signal's own scale, and percentile thresholds are unchanged in effect.

## D-018 · 2026-09-05 · Skeleton-level stability budget pinned; judged over the untouched population

**Decision.** Mapper §7 Q3 is resolved. Between two skeletons of one repository, **untouched feature churn ≤ 0.05 and untouched strata movement ≤ 0.05** — one room in twenty — with the floors of the signal-level budget carried over: at least 30 untouched common nodes, and no more than half of the common population touched. The budget is per comparison and per geometry; the caller chooses K and the diff records `commits_between`. Enforcement: `src/repo_substrate/mapper/diff.py::skeleton_diff` computes the split and emits `budget.verdict ∈ {within_budget, over_budget, untested}` with a reason; `SKELETON_BUDGET` holds the numbers; the CLI derives the touched set from the after-substrate's timeline (`touched_since`, commits after the before skeleton's `head_sha`, through the renames map) and reports `untested: touched_set_unavailable` when the two skeletons are not on one recorded line. Tests: `test_skeleton_budget_is_judged_over_the_untouched_population`, `test_skeleton_budget_floors_refuse_to_get_easier`, `test_touched_since_reads_the_after_timeline_through_renames`.

**Why the untouched population.** Same reasoning as D-008 for the signal budget. A node the intervening commits edited may legitimately change feature or stratum — that is the skeleton reporting the edit, and a time-lapse that hid it would be lying. A node they did not edit that changes anyway moved by ripple: a percentile threshold shifted under it, or a dependency's layer depth changed. Ripple is the jitter that makes adjacent frames flicker, and it is the only thing a *stability* budget should bound. The whole-population numbers stay in the output because they are what a reader sees.

**Why 0.05.** The four-repo reading (`reports/2026-09-05-m2/skeleton-budget.md`, K = 5, both geometries) puts untouched feature churn at 0.005–0.014 and untouched strata movement at 0–0.032. A ceiling fitted to the data would be no test; a ceiling far above it would never fail. 0.05 is above every reading, within a factor of two of the tightest (mcp-secure-server, layer, strata), and has a plain meaning on the picture: under a five-commit edit elsewhere, no more than one room in twenty may change its mark or its floor. A ruleset whose threshold sits in a dense part of a signal's distribution would exceed it at once; that is the failure it is built to catch.

**Assumes.** K = 5 is the perturbation the reference set was measured at; a budget claimed at K = 50 has not been measured. The touched set is what the extractor's timeline records as `nodes_touched`, so a file the extractor does not model (docs, lockfiles) cannot be "touched" — correct, since it is not a room either.

**Breaks if.** A geometry with finer strata (more than five bands) is added: strata movement scales with band count and the ceiling was read at five. Or a feature is written over a raw-count threshold near the mode of its distribution (`fan_in >= 2` on a repo where most files have fan_in 1 or 2): churn would be real and the budget would correctly refuse the ruleset, which is the point.

**Observed on the way (recorded, not gating).** Feature churn is geometry-invariant, as it must be. `uluops-registry-api`'s window touched no source node and the skeleton still moved 1.1%: the age signals are measured from HEAD's timestamp, so removing commits moves the clock — age geometry drifts under time alone. Layer geometry is the less stable geometry (longest-path depth propagates to every importer); whether it needs its own ceiling is deferred to Phase 1, where the time-lapse will read it at every checkpoint.

## D-019 · 2026-09-05 · No archetype in v0; the label is a corpus-relative claim

**Decision.** Mapper §7 Q1 is resolved by not claiming. `skeleton.json` carries `archetype: null`; the mapper spec's scope no longer promises "a single archetype resolution"; the cutaway banner names the repository, the profile, and the geometry, never a kind of building. Enforcement: `mapper/engine.py` emits `None`, and `mapper/ruleset.py::load_ruleset` now refuses any top-level table other than `[ruleset]` and `[[feature]]`, so an `[archetype]` table cannot ride along unread (test `test_archetype_is_not_claimed_and_cannot_be_smuggled_in`). Reopens with Phase 3.

**Why.** Three reasons, in order of weight.
1. *Calibration.* "Cathedral," "shantytown," "bunker" are claims about this repository relative to other buildings. v0 calibrates in-repo (system spec §5.3): within one repo every signal has a top decile, and an aggregate of per-node features ("40% of rooms are scaffolding") has no reference class to be read against. The reference class is what Phase 3's corpus provides. This is not a hard problem deferred; it is a claim whose grounding does not exist yet.
2. *The asserted bar.* A repo-level label has no second modality (validation §2.4.2) — nothing in v0 measures "what kind of building" independently of the features it would aggregate. It could therefore never reach `asserted`, only `decorative`, and a decorative one-word label on the banner is precisely the horoscope the gate refuses (system spec §3). The substrate spec already forbids a quality grade (§1); an archetype is a grade with a metaphor on it.
3. *Position over consequence (D-004 Q3).* Archetype names are consequence names by construction. The ruleset's discipline of preferring position names has no analogue at the whole-building level: there is no "position" a building occupies.

**Assumes.** The cutaway does not need a headline to be readable; the four reference renders (D-016, D-017) were read without one. If a headline is wanted, `summary.feature_counts` is the honest one — counts, not a noun.

**Breaks if.** Phase 3 lands and the corpus is too small or too homogeneous to give the label a stable reference class; then the archetype stays `null` and the reason moves from "no corpus" to "corpus insufficient," recorded in `validation.json`.

## D-020 · 2026-09-05 · M2 closed; Phase 1 (time-lapse) precedes the optional M3

**Decision.** M2 as defined in D-003 — a minimal authored ruleset under the anti-horoscope gate producing `skeleton.json`, plus a deterministic 2D cutaway with C4 folded in, no LLM in the path — is complete. The two open questions the mapper spec carried into M2 are resolved (Q3 by D-018, Q1 by D-019); overlays, two geometries, and the diff instrument (D-017) exceed the definition. Ordering of what follows: **Phase 1, the evolution time-lapse (system spec §8), is built next, before M3.**

**Why Phase 1 before M3.** D-003 makes M3 conditional: "built only if M2 proves the skeleton is worth narrating." A single frame cannot prove that; four reference renders were read (D-016 addendum) and read well, but reading well is recognition, n = 1, and the corpus has already refused recognition as a gate (D-004 Q2). The time-lapse is the test that can fail. If the named structure over a repository's history is mostly the budget's jitter, there is nothing to narrate and M3 is not built. If foundations are poured, wings bolted on, and basements flood at identifiable commits, the skeleton is worth a brief. Phase 1 is also deterministic, consumes only what M2 emits, and is the first real consumer of the D-018 budget — the same logic as D-013 rule 3: a consumer makes the instrument real. M3 puts an LLM in the path; every audit so far has pushed weight from the render toward the instrument, and this ordering keeps it there one phase longer.

**Shape of Phase 1 (to be specified, not yet designed).** Checkpoints along the timeline (by commit count, the same reasoning as validation §3.1's split); a substrate and skeleton per checkpoint, cached by sha; adjacent-checkpoint `skeleton-diff` with the budget verdict per frame; a frame sequence the cutaway renders with stable node identity through renames; and the question the phase must answer in a report — what fraction of movement across the history is over-budget ripple versus structural change. The validation gate is not re-run per checkpoint; `validation.json` at HEAD governs every frame, and that limitation goes on the page.

**Assumes.** Extraction cost scales: a full four-repo gate run is ~10 minutes cold, so twenty checkpoints on eslint is not free; the cache and `--truncate-at` carry it. Alex has not asked for the render first (D-003 "breaks if"), and can reverse this ordering.

**Breaks if.** The time-lapse shows every repo's history as under-budget stasis with a few discontinuities at large refactors — then the budget is loose or the features are coarse, and that is a finding against M2, not a reason to proceed to M3.

## D-021 · 2026-09-05 · Phase 1 built: the time-lapse; ripple split into clock and rank; first reading across the reference set

**Decision.** `substrate timelapse` (`src/repo_substrate/timelapse.py`, `time-lapse-spec.md`, `checklists/time-lapse.checklist.md`) replays a repository at checkpoints on its first-parent trunk, extracts each checkpoint truncated at its own commit (so `as_of` is that checkpoint's clock), maps it under HEAD's `validation.json` with the same ruleset, overlays, and geometry, renders the cutaway, and diffs adjacent frames with the D-018 budget using the exact touched set (`touched_between`: commits in the later timeline absent from the earlier). Design choices, each with its reason in the spec: trunk-only checkpoints (§2); spacing by commit count (§2, validation §3.1's reasoning); the HEAD gate governing every frame, stated as a limitation rather than re-derived per frame (§3.2, §6.1); a frame under `n_min` skipped explicitly rather than mapped as an empty building (§3.3); a frame whose substrate fingerprint differs from the gate's refused (§3.2, `TimelapseError`) — the seam the cache's own fingerprint check does not see.

**Ripple is two things.** The first registry run showed untouched-population movement of 9–24% per transition at K ≈ 75, which read as a budget failure until the feature table was read beside it: `lit_room`, `dark_room`, `flooded_basement`, and the age strata were moving on rooms nobody touched because the checkpoint's clock had advanced. That is the skeleton reporting time, and the budget at K = 5 never saw it because five commits are minutes. The time-lapse therefore classifies each feature by its predicate's signals (`CLOCK_SIGNALS`: `age_days`, `last_touched_days`, `blame_age_median`, `recent_commit_share`, `neglect_index`, `change_pressure_index`) and reports ripple as **clock** (those, plus age-geometry strata) and **rank** (everything else, plus layer-geometry strata). Only rank ripple is jitter. This is the one-step-back that makes the phase's question answerable.

**First reading** (`reports/2026-09-05-phase1/phase1-reading.md`; twelve frames per repository, both geometries). Rank jitter as a share of all movement: eslint 0.02 / 0.03, typeorm 0.02 / 0.04, mcp-secure-server 0.05 / 0.09, uluops-registry-api 0.05 / 0.15 (age / layer). Edits dominate the mature repositories (eslint 0.80, typeorm 0.63); clock dominates the non-edit movement on the young ones (registry 0.37, mcp 0.61 under age geometry). Structural change (born + deleted) is 0.12–0.24 everywhere. The budget's floors refuse most transitions on the large repositories, correctly: with 350–1,500 commits between frames the intervening commits touch most rooms, and there is no untouched population to read ripple from. Where a verdict is rendered it is `over_budget`, and it is clock ripple accumulated over K, not the K = 5 reading — the report says so on every run.

**What this decides.** The named structure of a repository over its history is edits, time, and births; jitter is a few percent. That is the condition D-003 set for M3 ("only if M2 proves the skeleton is worth narrating"), met as far as a mechanical test can meet it. Whether to *build* M3 is a separate product judgment and is not made here; Alex may want to read the scrubber pages first.

**Assumes.** Twelve frames is a reading, not a schedule; the events the frames catch (typeorm's 2022 mass edit, its HEAD restructuring with 180 rooms deleted and 80 born) are the ones that happened to fall between checkpoints. Layer-geometry strata ripple is classified as rank; an argument exists that dependency-layer propagation is the graph reporting a structural change elsewhere, not jitter. It is recorded as rank because D-018 defined ripple as movement of what did not itself change, and that definition is kept until the per-geometry ceiling question (time-lapse §7 Q2) is settled with more than one reading.

**Breaks if.** A ruleset adds a feature over a clock signal that also reads a rank signal — the feature is classified clock and its rank component hides in the clock share. The classification is per feature, not per signal, because a feature fires or not as a whole; a per-signal attribution would need the predicate evaluated term by term, which the diff does not do. Or `--every 5` on a reference repository shows rank ripple far above the twelve-frame reading — then the coarse schedule was averaging jitter away and the budget must be read at its own K.

## D-022 · 2026-09-05 · Age and recency are fractional days; the integer-day tie artefact; the gate is re-run

**Finding (Phase 1, `substrate timelapse --every 5` on mcp-secure-server).** Between two frames five commits apart with no file born, deleted, or edited among them, 53 rooms changed floor. The population's distinct `age_days` values went from six to seven: a cohort of files committed together had `age_days = 15` at one checkpoint and split into 18 and 19 at the next, because `age_days` was `(as_of − first_seen).days` — integer truncation — and the checkpoint's clock had crossed a day boundary for some of the cohort's files and not others. Under average-rank ECDF percentiles (substrate §7) the cohort's percentile fell from 0.503 to 0.243 and its age band flipped. The same mechanism moves `last_touched_days`, and through them `neglect_index`, `change_pressure_index`, `dark_room`, `lit_room`, `flooded_basement`, and the age geometry — every clock-relative signal — by amounts the K = 5 stability budget (validation §2.4.1, delta 0.15) would refuse, on any repository whose files were born in cohorts. The M1 gate recorded `max |Δ| = 0.0025` for `age_days` on mcp-secure-server because its one perturbation window did not straddle such a boundary. That pass was luck. The registry's `max |Δ| = 0.107` for the same signal was the artefact showing, unrecognized.

**Decision.** `age_days` and `last_touched_days` are **fractional days** (`history.py::_days`: `total_seconds() / 86400`, floored at zero, rounded to 1e-5 of a day). Files tie only when their `author_date` is identical — born or last touched in the same commit — and such ties never split under any advance of `as_of`. The change is to the substrate's definition of two metrics, so the extractor version and the config fingerprint change (`repo-substrate@0.3.0`), every cached substrate is invalid, and the D-009 protocol is re-run from tuning: `substrate-validate tune` on the pre-registered tuning repos, the tuned config committed, then `run` with the pre-registered test repos, into `reports/2026-09-05-m1b/`. D-015's verdict stands as the record for substrate 0.2.x; the new run is a new verdict on the corrected instrument, not a revision of the old one. M2's readings (D-017 addendum, D-018) and Phase 1's (D-021) are regenerated under the new gate; where they change, the change is logged as an addendum here.

**Why in the substrate, not the geometry.** A geometry-only fix (rank strata by exact birth order) would leave the percentiles of the clock signals, and every index over them, carrying the artefact into `flooded_basement` and `dark_room`. The percentile is where the damage is done; the input to it is where the fix goes. Test: `test_age_ranks_do_not_move_when_the_clock_crosses_a_day_boundary`.

**What the finding says about the method.** The signal-level budget measures one window. A window can miss a discontinuity that a time-lapse of eighty windows cannot. This is the argument for the per-frame gate (time-lapse spec §7 Q3) in its strongest form, and it is why the fine-schedule falsifier (D-021 breaks-if) is worth its cost: it was run to check that jitter was not being averaged away (it was not — rank ripple 5–6% at K = 5 and K = 10, as at twelve frames) and found something else.

**Assumes.** `author_date` resolution is seconds; files born in the same commit share it exactly. Rounding to 1e-5 day (under a second) keeps cross-platform JSON identical without recreating ties.

**Breaks if.** A repository's history was rewritten so that a cohort's `author_date`s differ by seconds while the files were, in fact, born together — then the cohort is a near-tie, and a new file inserting into it moves each member's rank by a small amount. That is rank ripple of the ordinary kind and is bounded by the budget.

**Gate re-run under substrate 0.3.0 (D-022 addendum, `reports/2026-09-05-m1b/`).** Re-tuning on the pre-registered tuning repos chose the same weights (`config/tuned.toml` byte-identical; `tuned_config_commit` unchanged at 24c087a); the objective values moved in the third decimal (`tuning.json`). The D-009 run on the pre-registered test repos gives the same verdict on every signal: both predictive indices `unvalidated`, twenty-one descriptive signals `asserted`, `revert_count` degenerate. The stability numbers show the artefact leaving: `age_days` max |Δ| on uluops-registry-api 0.107 → 0.000, on mcp-secure-server 0.0025 → 0.000, on typeorm 0.0086 → 0.000; `last_touched_days` on the registry 0.036 → 0.000. New substrate fingerprint `5f5554e36d4a`. The skeleton-level budget re-read (`reports/2026-09-05-m2b/skeleton-budget.md`): the registry's five-commit ripple, 0.011 in D-018, is now 0.000 in both churn and strata — it was entirely the tie artefact; mcp-secure-server's untouched churn rose 0.014 → 0.038, within budget, because fractional recency lets `lit_room` membership re-rank file by file where integer ties held cohorts together. That is ordinary rank ripple and the ceiling of 0.05 stands. D-021's time-lapse readings are regenerated under this gate; the first reading is superseded where the numbers differ.

## D-023 · 2026-09-05 · The change sheet: one drawing per transition, on the after frame's layout

**Decision.** `cutaway.py::render_change_sheet(before, after, substrate_after, diff, kinds, substrate_before)` draws a transition between two skeletons as one sheet. The layout is the after frame's cutaway layout (`_layout`, extracted from `render_cutaway` byte-for-byte), so a reader toggling between the cutaway and the change sheet sees every room in the same place. Each room is marked by what happened to it between the frames — `born`; `edit` (a commit touched it and a feature or its floor changed); `clock` (untouched, moved through a clock-relative feature or the age band: time reported); `rank` (untouched, the percentile or dependency layer moved beneath it: jitter); `unchanged` (ghost) — and rooms deleted between the frames are drawn dashed in a demolished strip under each wing. Each rect carries `data-change` and a title listing the feature and floor changes. The time-lapse writes one per transition (`fNNN-<sha8>.change.svg`, `change_stem` in the manifest). `skeleton_diff` now lists `touched.nodes`, so a diff document is sufficient input.

**Why.** System spec §8 promises "animate the construction." Two adjacent cutaways ask the eye to do the diff; the decomposition of D-021 says what kind of movement happened but not where. The change sheet is the picture of the decomposition: the same numbers, placed on the rooms. It stays inside the anti-horoscope contract because every mark is a recorded fact about two skeletons — born, deleted, touched, feature added or removed, floor changed — and none is a forecast. It is also where the D-022 artefact would have been visible at a glance: 53 amber rooms in one cohort, no grey among them.

**Assumes.** The demolished strip is one row per wing; a wing that loses more rooms than fit in the row shows the count and the first rooms. Overlay badges are not drawn on the change sheet; it is about movement, not the lenses.

**Breaks if.** A geometry moves rooms between wings (none does: wings are directories). Then the after layout would misplace a room that changed wing, and the sheet would need a "moved" mark.

**Phase 1 reading regenerated under substrate 0.3.0 (D-021 addendum, `reports/2026-09-05-phase1b/`).** Same schedules, same ruleset and overlay, gate `reports/2026-09-05-m1b/`. Rank jitter as a share of movement: eslint 0.02 / 0.03, typeorm 0.02 / 0.04, mcp-secure-server 0.07 / 0.11 (0.10 at every 5 commits), uluops-registry-api 0.06 / 0.16 (age / layer). What changed against the first reading is the clock share on the young repositories (mcp 0.61 → 0.44, registry 0.37 → 0.33) and the total movement (mcp every-5: 1,649 → 1,054): the integer-day tie artefact was clock ripple, and it is gone. The conclusion of D-021 stands with the numbers corrected: over a history the named structure is edits, time, and births; jitter is a few percent, most of it dependency-layer propagation under the layer geometry. The change sheets (D-023) are on every run's page. The first reading is kept as the record of the instrument that found the defect.

## D-024 · 2026-09-05 · Phase 1 review cycle: roster, convergence, and what changed

**Roster and rationale (D-013 rule 2).** Alex asked for Nagarjuna, Heidegger, and Laozi and left three to me. I chose **popper-validator** (the layer makes new quantitative claims — the budget, the jitter shares, the D-022 mechanism — and the M1 Popper run predates them), **wittgenstein-analyst** (queued since D-013 for the ruleset naming, and the time-lapse introduced a vocabulary: ripple, clock, rank, jitter, edit, untouched), and **code-auditor** (new runtime paths: `timelapse.py`, the diff budget block, the change sheet, `_days`). The M1 cycle's roster was all engineering instruments; this one is weighted toward lenses that read categories, because the day's work invented categories. Saved as one tracker run (`phase1-review`), six agents, with a cluster key on each converged finding.

**Outcome.** Scores 83–89; decisions DISCLOSED (Heidegger 86), EFFORTLESS (Laozi 84), REIFIED (Nagarjuna 84), CLEAR (Wittgenstein 83), UNCORROBORATED (Popper 86), UNSOUND (code-auditor 89, an auto-fail on unhandled JSON parsing at the CLI). Every philosophical lens said the layer is unusually disciplined and then found the same seam.

**The convergent finding, five lenses.** The budget's operand and its definition had come apart. D-018 defined ripple and jitter as one thing and `skeleton_diff` tested total untouched movement against 0.05. D-021 narrowed jitter to the rank half and never re-derived the budget (Wittgenstein CGC-2), so on mcp-secure-server the verdict was dominated by the 0.44 clock share the definition excludes. The verdict token carried no K, so `over_budget` at K = 790 was the same datum as at K = 5 and the manifest tallied them together (Nagarjuna SV-3, Laozi F-003 "the verdict inverted at transferred K", Heidegger B-4). `CLOCK_SIGNALS` was an unpinned literal whose any-signal rule booked every move of `flooded_basement` — which reads `neglect_index` and `load_index` — as time, so the rank share the M3 condition turns on was a lower bound in the conclusion's favour; D-021 had recorded that as a hypothetical breaks-if while the shipped ruleset already satisfied it (Popper C-2, Laozi F-005, Wittgenstein FR-1). The rank share was quoted per repository without K although the artifact's own rows showed it moving with the schedule (Nagarjuna SV-1, Popper C-3/C-4).

**What changed, in code.**
- *Operand.* `skeleton_diff` classifies every feature `clock | rank | mixed` from the signals its fired instances record as evidence (`feature_kinds_from_skeleton`, `classify_signals`; a diff needs no ruleset). The budget judges **jitter**: rank and mixed feature churn, and strata moves, over untouched rooms; clock churn is reported beside it and never judged. Mixed counts as jitter — the conservative side, since the rank component cannot be separated without evaluating predicate terms both frames do not carry. Strata moves of untouched rooms are rank under both geometries: after D-022 a floor moves only because the population re-ranked around the room.
- *K.* `SKELETON_BUDGET` carries `pinned_k = 5` and `max_k = 10`; a comparison with `commits_between > max_k` is `untested: beyond_pinned_k`. The verdict token no longer travels to a K it was not pinned at; the numbers still do, labelled with their K. `budget.k` and `budget.operand` are in the datum.
- *Pinned list.* `CLOCK_SIGNALS` moved to `mapper/diff.py` and is pinned by test against the substrate spec's list (`test_clock_signals_are_pinned_to_the_substrate_spec_list`), the way D-011 pinned G1 membership.
- *Change sheet.* A room takes its strongest mark, rank > mixed > clock, and the sheet says so; each mark's tooltip now carries the fired feature's evidence values (Heidegger C-1's disclosure path, the part the skeletons can supply). A test pins that every room sits at the cutaway's x and width (Popper C-6).
- *Bugs (code-auditor).* Renamed rooms that changed floor were dropped from `strata_moved` (C1, a published-number bug; fixed, tested). A `validation.json` without a fingerprint key disabled the gate check instead of raising (C4; now raises, and a mismatch names the differing config keys — Heidegger C-2). The scrubber page threw when every frame was skipped (C2). CLI JSON and git errors now exit with the file named (C3). `skeleton-diff` refuses skeletons that differ in repo, geometry, or ruleset (P2). Stale frames of an earlier schedule are removed from the output directory (P5). `--frames N` on a short trunk records the realized count and says so (C6). `untested` distinguishes `renames_not_given` from `touched_set_unavailable` (Heidegger B-3).
- *Register hook (Wittgenstein FR-2).* `name_implies_consequence = true` now requires a `position_name`, and a `position_name` that repeats the feature name is refused; `hub` lost its self-referential one. This is the grammar the M3 register lint will read.

**Pre-registered before the regenerated reading (Popper C-3, C-4).** "Jitter is a few percent" is refuted if the jitter share (rank + mixed) exceeds 0.20 on any (repository, geometry) at twelve frames, or exceeds 0.10 on two or more repositories. "The twelve-frame schedule is not averaging jitter away" is refuted if the every-5 jitter share exceeds the twelve-frame share by more than 2× or by more than 0.10 absolute on the repository where both were run. These are written here before `reports/2026-09-05-phase1b/` is regenerated under the new operand, and the regenerated reading is judged against them in the addendum below.

**Adversarial ruleset (Popper C-1; `reports/2026-09-05-m2b/adversarial-ruleset.md`).** A ruleset built to break the budget — absolute thresholds in the dense part of `fan_in`, `size_loc`, `commit_count`, and `load_index >= p50` — run by the D-018 procedure at K = 5 on all four repositories: jitter churn 0.000–0.002, every verdict `within_budget`. The budget at K = 5 has still never fired. The reading is not that the budget is sound; it is that five commits rarely move an untouched room's rank signal at all — a raw count on an untouched file changes only through edges from touched files — so a K = 5 perturbation cannot produce the flicker the ceiling was set to catch. The budget's K is the wrong K for its purpose, and the per-K reading (time-lapse §7 Q2) is now the question that matters: pin the ceiling at a K where it can fail. Not re-pinned here; that needs the every-K readings.

**Not done, recorded.** Popper's frozen-clock test (re-run a transition with `as_of` held at the earlier checkpoint) needs an extractor override and would attribute mixed features per term; deferred with the mixed class as the conservative stand-in. Popper's sliding-window test of D-022's "luck" claim (forty K = 5 windows under substrate 0.2.x) is the severe test of that word; deferred, cost forty extractions. Nagarjuna's SV-2 — edits to files the extractor does not model (tsconfig, lockfiles) move `fan_in` on rooms that count as untouched — is a real leak into the jitter class and is recorded as a limitation in time-lapse §6, unquantified. Nagarjuna's SV-4 (movement sums four incommensurable counts) stands as a bookkeeping convention, stated in the report. Heidegger's Z7/Z8 (four-command path threading; a cost-blind schedule surface) are usability debts, not taken up.

**Regenerated reading under the D-024 operand (D-024 addendum; `reports/2026-09-05-phase1b/`).** Judged against the ceilings pre-registered above. Jitter share (rank + mixed), age / layer: eslint 0.05 / 0.05, typeorm 0.12 / 0.08, mcp-secure-server 0.32 / 0.19 (0.33 at every 5 commits), uluops-registry-api 0.30 / 0.20. **"Jitter is a few percent" is refuted for the young repositories** — above 0.20 on two, above 0.10 on three — and stands for the mature ones. The schedule-invariance claim stands (0.33 vs 0.32 on mcp). What moved the number: floor moves of untouched rooms, booked as clock in D-021, are rank under D-024 (after D-022 a floor moves only by re-ranking), and on a population growing by a fifth between frames the age bands re-rank wholesale. That is jitter by D-018's definition and it is caused by births: structural change propagating through in-repo percentiles, the limitation time-lapse §6.2 stated and the reading now measures. D-021's conclusion is restated: over a mature history the named structure is edits and births with jitter at 5–12%; over a young, fast-growing one the picture re-ranks by a fifth to a third between frames, and a narrative over it would narrate the calibration. Corpus-relative calibration (Phase 3) is where that changes. The first two readings are kept: the one that found the artefact, and the one that carried the one-sided classifier this cycle caught.

## D-025 · 2026-09-05 · M3 deferred; the K study is next

**Decision.** M3 (D-003: the C5 architect brief under an adversarial lens with the register lint) is not built next. Next is the **K study**: `substrate timelapse --every K` on all four reference repositories at several spacings, to pin the skeleton budget at a K where it can fail (D-024, time-lapse §7 Q2) and to settle whether layer geometry needs its own ceiling. M3 reopens after that, scoped to the descriptive register over `asserted` signals and, on the D-024 reading, to mature repositories.

**Why.** (1) The instrument's stability reading moved twice today under its own corrections — jitter on the young repositories went from "a few percent" to 0.30 in one cycle. A brief narrates the skeleton; a skeleton whose stability reading is still settling is a moving target, and the register lint's hook only acquired a grammar today (D-024). (2) The condition D-003 set for M3 rests on "stable under small changes," and D-024 showed the budget is pinned at a K where nothing fires it, on the reference set or on a ruleset built to break it. Until the ceiling is read at a K where it can fail, the stability claim is asserted, not shown. (3) The young-repository result is a property of in-repo percentiles on small growing populations; corpus-relative calibration (Phase 3) is the fix, and a narrative would narrate the calibration. (4) Every audit since June has pushed weight from the render toward the instrument; this is the same call one more time, and it is the delegated call, not a deferral to Alex.

**Design of the K study.** Spacings 5, 10, 25, 50, and 100 where the trunk allows, age and layer geometry, maintainability + onboarding, gate `reports/2026-09-05-m1b/`. Per (repository, geometry, K): jitter churn and untouched strata movement per transition and their distributions; the number of transitions the budget could judge (K ≤ max_k) and the verdicts. The ceiling is re-pinned at the smallest K at which the reference set's own distribution has a tail the ceiling can cut — a ceiling that never fires is not a ceiling — and `max_k` is raised to match. Per-geometry: if layer jitter sits above age's by a stable factor across K, two ceilings. Pre-registered refutation of the study's premise: if jitter churn at K = 25 and K = 50 is still below 0.05 on every repository, the budget cannot be pinned on this reference set at all and the reading says so.

**Assumes.** Extraction cost: registry every 10 (82 frames) and every 25 (33), mcp every 5/10, typeorm every 50 (76 frames) and every 100 (38), eslint every 100 (87 frames) and every 250 (35); several hours cold, cached thereafter.

**Breaks if.** Alex wants the render or the brief first. Then M3 is built on the mature repositories with the budget's K left open and stated on the page.

## D-026 · 2026-09-05 · The budget is pinned at K = 25, applied to 50; the calibration rule; age floors are the unstable element

**Decision.** `SKELETON_BUDGET` (`mapper/diff.py`): `pinned_k = 25`, `max_k = 50`, ceilings unchanged at 0.05 (jitter churn) and 0.05 (untouched strata), and a new floor `min_jitter_union = 20` (`untested: insufficient_jitter_population` below it). Test: `test_budget_refuses_a_tiny_jitter_population`. The K study that pins it is `reports/2026-09-05-kstudy/kstudy.md` (D-025's design, run 2026-09-05 evening).

**The calibration rule.** The ceiling is not moved; the K is chosen so that the ceiling sits between the reference set's per-transition median and its 90th percentile of jitter churn. At K = 5 nothing reaches it (D-024's adversarial ruleset included). At K = 25 on the registry the median is 0.034 and the p90 0.143; at K = 10 on mcp 0.022 / 0.066; at K = 50 on typeorm 0.027 / 0.235; at K = 100 on eslint 0.028 / 0.125. Twenty-five is the smallest K at which the rule holds on every repository where it was read, and fifty is the largest at which the median is still at or under the ceiling on the fastest-growing one. A ceiling that separates typical from tail can fail, and this one does: 53% of the registry's K = 25 transitions exceed it. D-018's "headroom, not a fit" stands; the K was the missing parameter, which is what D-024's adversarial run showed.

**Measured, and worth stating.** Jitter share is a property of the repository, not the schedule — within a repository it holds to within a few points across a fivefold change in K (registry 0.25–0.28, typeorm 0.15–0.16, eslint 0.16 at both K). Nagarjuna's SV-1 is answered: quote it with K, and it will hold. Jitter churn per transition grows with K at a rate set by growth, and the median crosses the ceiling at K ≈ 25 (mcp), 50 (registry), 100 (typeorm), 200 (eslint).

**Per geometry (time-lapse §7 Q2, resolved by not adding a ceiling).** Age-geometry floors are the least stable element of the whole skeleton: untouched strata movement p90 0.13–0.43 under age against ≤ 0.13 under layer, medians up to 0.10 against 0.000. Age bands are quantiles of `age_days`; a growing population re-ranks them wholesale between frames, while layer depth moves only when the graph does. A second ceiling for age strata would be tuned to pass. Instead: one ceiling, and the reading that layer is the stable geometry for a time-lapse and that percentile age bands should become absolute-era bands if the age geometry is to hold a floor still — D-019's calibration argument applied to strata. Opened as mapper §7 Q6; not built.

**Assumes.** `commits_between` counts every commit in the timeline between checkpoints, merges' side branches included, so an `--every 25` schedule produces K in the twenties to sixties; `max_k = 50` admits most of them and refuses the merge-heavy tail as `beyond_pinned_k`, which is the honest outcome.

**Breaks if.** A reference repository is added whose K = 25 median exceeds 0.05 — a repository growing faster than mcp-secure-server did in its first month. Then the pinned K must fall, or the rule is restated per growth rate rather than per K. Or if absolute-era strata are built: then the strata ceiling is re-read under them.

## D-027 · 2026-09-06 · M3 built: the architect brief with the register lint

**Decision.** `substrate brief skeleton.json [substrate.json] -o brief.md` (`src/repo_substrate/brief.py`, `architect-brief-spec.md`, `checklists/architect-brief.checklist.md`). Three parts: a deterministic **facts sheet** (the closed set of sayable things, hashed), one **generation** call to `claude-opus-5` under the condemnation-surveyor stance with the register rules in the system prompt, and a deterministic **register lint** of seven rules that runs after the model. A brief that fails is written marked FAILED and the command exits 1; the violations are fed back once for a regeneration. `--draft` lints a hand-written brief with no model in the path, which is how the tests run and how a brief can be written by anyone.

**Why this shape.** The anti-horoscope contract cannot be enforced inside a model; it can be enforced on what the model wrote. So the lint is the product and the generator is replaceable: the model writes, the lint refuses, the page shows both. D-003's "adversarially-framed Generator lens" is read in v0 as the stance plus the lint as hostile reader; a second model pass acting as hostile reader is open (spec §5 Q1) because it would be another model, and the reader can trust the deterministic part.

**The rules and where each comes from.** R1 consequence vocabulary — validation §2.1.1, the A2 boundary; the lexicon (`CONSEQUENCE_WORDS`) is the audit surface and errs toward false positives. R2 provenance — the C5 → mapper seam in the system-integration checklist: every claim resolves to a feature and a room. R3 numbers — the facts sheet only. R4/R7 decorative — mapper §3: excluded from diagnosis, count stated. R5 disclosure — D-004 Q3 via the grammar D-024 gave `name_implies_consequence`. R6 no label — D-019.

**Model and settings.** `claude-opus-5`, adaptive thinking, effort `high`, streaming, server-side refusal fallback enabled (`fallbacks: "default"`), model served and request id recorded on the page. Non-deterministic by nature; the facts hash and the lint status are what make two briefs comparable.

**Scope (D-025).** Mature repositories. The brief is generated for all four reference skeletons under the M1b gate (`reports/2026-09-06-m3/`); the young repositories' briefs are read as briefs over a skeleton that re-ranks between frames, and say so through the skeleton hash and the time-lapse beside them.

**Assumes.** The lexicon covers the consequence words a model reaches for under this system prompt; a consequence voiced without a listed word passes (spec §4.1 — the known false-negative surface). Room metrics are withheld from the prompt to keep it small; the lint still checks any number against them.

**Breaks if.** A predictive index becomes `validated`: then the lint must admit consequence vocabulary sourced from that index alone, and the flat lexicon becomes a per-signal one (spec §5 Q3). Or a reader finds the briefs unreadable because every sentence carries a bracket — the citation form is the cheapest provenance that a lint can check, and a renderer that folds citations into footnotes is a presentation change, not a contract change.

**First briefs (D-027 addendum, `reports/2026-09-06-m3/`).** Four briefs over the four reference skeletons (M1b gate, age geometry, maintainability + onboarding), `claude-opus-5`, about 11k input and 2k output tokens each, 530–610 words. Every brief needed the second attempt: the lint refused the first drafts for a consequence word ("risk"), for citations written with the literal word `feature` (an ambiguous example in the system prompt, since fixed), and for a decorative count written in words. Three forms the writer reached for that the lint now accepts because they are honest: `[feature ×N: rooms]`, several `feature: rooms` clauses in one bracket separated by semicolons, and a decorative-disclosure paragraph that carries no citation because it may not cite decorative features. Three the lint now refuses that it did not at first: a consequence-implying name used anywhere in the prose without its position name (plural-tolerant), a bare `[feature]` with neither room nor count, and number words. A `--relint` path re-judges an existing brief under the current lint without a model call and keeps its provenance; a test pins that generation-time lint and relint agree on the same prose. All four pass. What the briefs read like: the surveyor's voice holds — wings and counts first, then the marks by where they sit, the decorative marks disclosed and kept out, the stance verbatim — and every sentence that mattered was cited. The cost of the discipline is on the page: a bracket in every paragraph.

## D-028 · 2026-09-06 · M3 review cycle: the lint checked paragraphs while the header promised sentences

**Roster and rationale (D-013 rule 2).** hume-analyst on the four briefs' text against their facts sheets (does each sentence resolve to evidence; where does habit or the stance pass as observation), wittgenstein-analyst on the lexicon and the rules (does a word list draw the line validation §2.1.1 means), hostile-reader on the typeorm brief from an interest-rival's seat. Chosen because M3 is prose over an instrument: the two questions are whether the prose says what the sheet says and whether the checker checks what it claims. Saved as tracker run 12 (`m3-review`), 16 findings, statuses updated below.

**Outcome.** Hume UNGROUNDED 88, Wittgenstein BEWITCHED 86, hostile-reader HATER_EXPOSED 87 — each narrowly, each on the same seam. All three said the arithmetic holds (every count reconciles; four-fifths of the prose resolves exactly; the decorative exclusion is real integrity) and then found the place where it does not.

**The convergent finding.** The register lint checked that a *paragraph* contained a resolving citation, while the header on every brief promised that *every claim* cited the feature and room it rested on. In the typeorm brief a sentence named two rooms as holding both the dark and lit readings — impossible, since one recency value cannot be at once above p90 and below p10 — under a citation of a third room, and the lint passed it (hostile-reader critical, Hume F1, Wittgenstein "declared grammar sentence-level, operational grammar paragraph-level"). Two more holes Wittgenstein found that the spec had not disclosed: the disclosure phrase ("not a claim about what breaks") exempted the whole sentence from the consequence rule, an amnesty a regeneration loop would learn to reach for; and the rule scanned inside citation brackets, where a room called `no-unsafe-negation.js` is a name, not a claim. Hume: the number the briefs called the diagnostic total was the base profile's count with the overlay's marks left out; "against that" set two independent sets in a compensatory relation without a listed word; `flooded_basement` and `dark_room` carry consequence in their names while the ruleset flagged them as not implying one.

**What changed.**
- *R8 attribution.* A room named in a sentence must be covered by a feature cited in that sentence, and that feature must have fired on it. This is the sentence-level rule the header had promised. The header now states what the lint checked and what it cannot: a consequence voiced without a listed word, a computed number that happens to match, a room's function inferred from its name.
- *R1.* Citations are stripped before the scan; the disclosure clause is struck from the sentence and the remainder is read, so the clause covers only itself. Phrase patterns for consequence voiced as a relation between sets (`against that`, `offsets`, `compensates`, `if … changed`) join the lexicon.
- *R5.* The position name must appear in the paragraph where the consequence-implying name is first used, not anywhere in the document.
- *R4.* A decorative feature is cited by count only; naming its rooms hands a reader the salience the exclusion was meant to withhold.
- *R7.* The facts sheet's `diagnostic_count` is now the count across all profiles (`diagnostic_count_base` keeps the base profile's), and the brief must state it beside the population.
- *Ruleset.* `flooded_basement` and `dark_room` are `name_implies_consequence = true` with position names ("long-untouched, still-imported room", "long-untouched room"). Hume and Wittgenstein both read the flag as a judgment sharpened by fiat; on the auditors' reading the two most consequence-laden names were the two left unflagged. `foundation`, `crack`, `toothpick_wing` keep theirs; `hub`, `lit_room`, `scaffolding`, and the onboarding names stay unflagged.
- *Header.* "PASS on attempt 2" — retry-to-pass is on the page (hostile-reader #8).
- *Prompt.* Rooms named must be covered in the same sentence; name rooms, do not say what they do; no counts the writer computed; no setting features against each other; disclose at first use; the stance paragraph carries no citation.

Re-judged under the strengthened lint, all four D-027 briefs fail — on exactly the sentences the auditors named — and are regenerated; the addendum records the result.

**Not fixed, recorded.** The entrance predicate cannot find a library's barrel because the barrel is imported internally; on typeorm the front door is a codemod (hostile-reader #3; open on the onboarding ruleset). Percentiles pool across a monorepo's unrelated dependency graphs, so `packages/codemod` files rank as whole-repo foundations (hostile-reader #4; open in the mapper). The flooded basements on typeorm are dominated by finished one-class error files — the stance limitation the system spec discloses in the abstract, shown on the flagship in particular; a per-repository applicability note is open. The cutaway's legend hardcodes feature names without position names (Wittgenstein informational; open). Function inferred from a pathname and a computed count that coincides with a listed number remain outside what a lint can read; both are in the spec's limitations and in the header's "cannot check".

**Assumes.** The disclosure-clause patterns cover the forms a writer under this prompt produces; a new form is an amnesty again until added. `DISCLOSURE_CLAUSES` is an audit surface beside `CONSEQUENCE_WORDS`.

**Breaks if.** R8 is defeated by a room named in one sentence and cited in the next; the rule is per sentence because that is where a reader parses attribution, and a writer who splits them is writing to the lint, which the regeneration loop will show in the attempt count.

**Regenerated under the D-028 lint (addendum, `reports/2026-09-06-m3/`).** All four briefs pass: eslint and typeorm on the first attempt, the registry and mcp-secure-server on the second. Before the regeneration the strengthened lint, re-judging the D-027 briefs, refused every sentence the three auditors had named by hand — typeorm's impossible dark-and-lit rooms, "against that", the named toothpick rooms, the base-profile count called a total — which is the check that the rules now read what the reviewers read. One false positive of the new rule surfaced on the way and is fixed: R8 matched rooms as substrings, so `src/index.ts` was "named" by any sentence mentioning `packages/codemod/src/index.ts`; a room is now a whole path token (`_mentions`, tested). Two residual refusals after the first regeneration were legitimate and were fixed by the second: a decorative name disclosed without its position name, and a count not stated. The lint's attempt counter is on every header; the regeneration loop is visible, not hidden.

## D-029 · 2026-09-06 · Packages in the substrate: the declared entry as a flag; `entrance` becomes `import_root`

**Decision.** Substrate 0.4.0 reads every `package.json` in the tree (`inventory.py::package_facts`) and gives each room two facts: `package`, the nearest ancestor directory holding a `package.json` (`""` at the root; not a signal — a grouping, like the wing), and `is_package_entry`, true for the file a `package.json` names as `main`, `module`, `browser`, `types`, `bin`, or an `exports` target, resolved to source by a declared heuristic (`./index.js` → `index.ts` / `src/index.ts` / `lib/index.js` …, a directory → its index; `_resolve_entry`). `is_package_entry` is a **G1 flag**: the grounding table declares `flag: true`, the stability budget applies, the degeneracy bar does not (it guards ranking, and a flag is never ranked), and the mapper refuses a percentile threshold over it (`engine.py::check_gate`). `validation.json` records `flag` per signal. The onboarding ruleset (0.2.0) renames `entrance` to `import_root` — the position name it already carried — and adds `package_entry` (`is_package_entry == 1`, position "declared package entry"). Tests: `test_package_facts_resolve_entries_and_owners`, `test_mapper_refuses_a_percentile_over_a_flag`, and the G1 pin.

**Why.** The hostile reader's third finding (D-028): on typeorm the onboarding overlay named `packages/codemod/src/index.ts` as the only entrance, because `fan_in == 0 ∧ fan_out ≥ p75` finds roots of the import graph and a library's own entry is imported internally. The predicate was true of what it measured and false of what its name said. Two corrections, both honest: call the root what it is, and add the fact a maintainer would check — what the package declares as its door. That fact is documentary (G1), heuristic where built entries stand in for source, and never worth ranking, which is what "flag" names. The `package` grouping is laid in the substrate now because the fourth finding (monorepo pooling — percentiles pooled across unrelated dependency graphs) needs it; per-package calibration is D-030's question, not this one's.

**Cost.** The extractor version changes the config fingerprint; every cached substrate is invalid and the D-009 protocol is re-run from tuning into `reports/2026-09-06-m1c/`. M2's readings, the K study, Phase 1, and the M3 briefs are regenerated under the new gate; any change in a number is logged as an addendum.

**Assumes.** `package.json` is the package manifest; a Python repository gets `package = ""` everywhere and no entries (a `pyproject.toml` reader is a later addition of the same shape). The entry heuristic errs toward finding a source file for a built entry; a wrongly resolved entry is a false positive on one room, visible in the brief as a `package_entry` mark on a file that is not a door.

**Breaks if.** A repository declares its entries only through a bundler config the manifest does not name; then `package_entry` fires nowhere and `import_root` is the only door the overlay can show, with the rename saying what it is.

## D-030 · 2026-09-06 · First forecast cycle: what the built system makes possible, what it invites, what it normalizes

**Roster and rationale (D-013 rule 2).** Alex's standing recommendation (2026-09-06): the forward-looking lenses for ideas and direction. First cycle: **capability-emergence-forecaster** (what composes from the gate, the diff, the time-lapse, the change sheet, and the linted brief), **perverse-outcome-detector** (the system now has five measurable criteria that people and models will optimize against), **normalization-forecaster** (what its vocabulary does once assumed). Unintended-consequences and future-observer are held for the next cycle so rosters stay comparable. Saved as tracker run 13 (`forecast-cycle`), 18 findings.

**Outcome.** GENERATIVE 89, PERVERSE 86, NEUTRAL 89.

**What the system makes possible (capability emergence).** The reading that changes the project's self-understanding: the largest asset may be the lint, not the cutaway. `facts()` + `lint()` + `--relint` is a domain-general deterministic verifier for model prose over structured facts — per-sentence attribution as a set computation, no model in the loop — coupled to skeleton-shaped dicts only by construction; with a corpus of facts sheets it becomes a verifiable reward signal for bounded-register generation, a task class where the field has no non-model verifier. The skeleton budget is a stability test for any percentile-scored instrument ("how much of my score's movement is the thing moving versus the population re-ranking under it"). The time-lapse is an instrument debugger, with D-022 as the existence proof. The decision log's "Breaks if" clauses are a falsification ledger — D-022 fired D-018's, D-024 fired D-021's, D-029 fired D-028's open finding — and with the tracker a ground-truthed scoring of reviewers. Direction candidates, not built: a facts-sheet protocol with the lexicons in config (E1/E5); a parser over the D-NNN schema (E7); a substrate contract that lets `map | render | diff | brief` leave git (E6).

**What it invites (perverse outcomes), and what changed.** The critical finding: every route to `untested` on the skeleton budget is under the operator's control — a coarse schedule, a large refactor, a small repository, features at p99 — so an adverse verdict is optional, and a run the budget could not judge was never an outcome in itself. *Fixed:* the time-lapse totals carry `budget_judged`, `budget_coverage`, and a `budget_reading` of `no_verdict | within_budget | over_budget`, and the report says that a run with no verdict says nothing about stability. Also fixed in code, each with a test: decorative features were excluded from churn accounting, so the hatch bought unmeasurability — decorative churn is now measured and reported, never judged; the `flag` exemption could launder any near-constant signal — a flag that is constant on a repository is degenerate there; the stance and decorative-disclosure phrases were paragraph-wide amnesties from R2 and R4 — the stance exemption applies only to the paragraph that is the stance, the disclosure exemption from R2 only when every citation is a decorative count, and R4's per sentence; R3 admitted any small integer on a large repository because every room's metrics were allowed everywhere — a room's metrics are admitted only in a paragraph that names it; counts could be satisfied by a citation — they must sit outside brackets; regeneration was unbounded — three attempts at most, and every attempt's refused rules are on the page; `decorative_reason` was a non-empty-string check where mapper §3 asks it to name the ungrounded signal — it must name a signal of the predicate; `name_implies_consequence` was declared by fiat — a feature name containing a consequence word (`NAME_CONSEQUENCE_WORDS`, the audit surface) must declare it. *Recorded, not fixed:* clock-only predicates exit the budget by design and the gradient rewards dropping a rank conjunct — clock churn is reported beside the verdict, and the interaction of the two gates' escapes is now named in the spec; `M_asserted = 2` of four (D-014 exercised it honestly); and the signal-level stability K = 5, left behind when the skeleton budget moved to 25, which is D-031's comparison run.

**What it normalizes.** NEUTRAL because the artifact carries its counter-evidence on its own pages, and the exposure is that the counter-evidence lives in this log while the coined words travel. Watch: the status ladder becoming the ontology of evidence once `validation_status` is a schema key other tools read, with `untested` and `unvalidated` collapsing in transmission; feature names reaching code review and management before the gate does, with D-019's refusal of a building label living in the skeleton and not in a reader's spreadsheet; a disclosed name read as neutral, with the finished-utility false positive as its shadow. No fix beyond the page for any of these yet; they are the reason the vocabulary stays position-first.

**Substrate 0.4.1 on the way.** The 0.4.0 gate asserted `is_package_entry` with a modal share of 1.0 on two repositories: their manifests point at `dist/…`, and the resolver never stripped a build directory. Fixed (`BUILD_DIRS`, a `types/` segment), tested, version bumped so the cache cannot serve the old values; the gate is re-run and the 0.4.1 run is the D-029 baseline. Re-tuning was not repeated: package facts enter no tuned objective, and the 0.4.0 re-tune had already returned byte-identical weights.

**Assumes.** The forecast rosters alternate so they can be scored against what later fires (D-013 rule 2 applied to forecasters). The direction candidates are recorded for the next planning decision, not adopted here.

**Breaks if.** The next cycle's held lenses find the same seam the first three did — then the roster is not orthogonal and the alternation is not buying information.

**Gate under substrate 0.4.1 (D-029 addendum, `reports/2026-09-06-m1c/`, fingerprint `179d8acb7b0c`).** Same tuned weights (`tuned_config_commit` 24c087a, re-tune byte-identical under 0.4.0, not repeated for the resolver fix). Every verdict is as under 0.3.0; the one addition is `is_package_entry`: **asserted**, a flag, with an entry share of typeorm 0.010, mcp-secure-server 0.080, uluops-registry-api 0.004, eslint 0.024 — the 0.4.0 run had found none on the registry and mcp-secure-server because their manifests point at `dist/`, and the build-directory stripping (D-030) is what changed between the two runs. Downstream readings are regenerated under this gate by `scripts/regenerate_readings.py --tag 2026-09-06`.

## D-031 · 2026-09-06 · One K for both budgets: the signal-level stability window moves to 25; `neglect_index` leaves the ruleset

**Decision.** `stability_perturbation_k` defaults to 25 (`validation/config.py`; validation §2.4.1), the K at which D-026 pinned the skeleton budget by the rule that the ceiling must sit where it can fail. The comparison run (`config/validation-k25.toml`, `out/validation-k25`, archived as `reports/2026-09-06-m1c-k25/`) is the canonical gate for the reference set from here: its effective config equals the new default. Under it every verdict holds except two: `recent_commit_share` is `untested: unstable` (max |Δ| 0.50 typeorm, 1.00 registry, 0.33 eslint; degenerate on mcp-secure-server), and `neglect_index`, which blends it, is `untested: input_not_asserted`. `rulesets/maintainability.toml` 0.2.0 rewrites `flooded_basement` as `last_touched_days ≥ p90 ∧ load_index ≥ 0.10` — the position name it already carried, "long-untouched, still-imported room," is exactly that predicate — and no shipped feature reads `neglect_index` any more.

**Why.** The perverse-outcome detector's P4 (D-030): the twenty-one `asserted` verdicts rested on a K = 5 window that the project's own K study had shown cannot fire, while the skeleton budget had moved to 25. Two budgets at two Ks is satisficing by inheritance. Read at 25, the signal set answers plainly: nineteen signals are stable; the one that is not is the one that is a window over commit count by definition — remove 25 commits and the last-fifth window moves 25 commits, so a share over it moves for every file near its edge. That is not an instrument defect; it is what a recency share is. An index that blends it inherits the instability, and a feature over the index inherits it in turn. The feature was never about the share: "flooded basement" meant old, still imported, and untouched, and `last_touched_days` says untouched without a window.

**What this costs.** `neglect_index` was the G3 signal — corroborated against blame age (D-004 Q2, τ 0.77 on mcp) — and the ruleset now reads no G3 signal. The cross-modal bar stays in the gate for the signals that reach it; the maintainability profile's "neglect" reading is now carried by a G1 primitive with a declared clock. `flooded_basement`'s room set changes: the rooms the old predicate marked through the age and recent-share components of the blend and not through last touch fall out, and the readings (D-018 budget, K study, Phase 1, the briefs) are regenerated under the new gate and ruleset; the numbers that move are logged in the addendum. The M1 verdict on the predictive indices is unchanged (they do not depend on K).

**Assumes.** M_asserted stays 2 of 4. The floors held at K = 25 on every repository (no `insufficient_stability_population`); a smaller reference repository than mcp-secure-server would hit them.

**Breaks if.** Phase 3's corpus wants a recency-share signal after all: then it is redefined over wall time with a stated window, gated on its own, and the ruleset may read it again. Or the reference set grows and `last_touched_days` itself proves K-sensitive on a bursty repository — it did not on these four (max |Δ| ≤ 0.064 at K = 25).

**Addendum (2026-09-06 evening) — the readings regenerated under the K = 25 gate (`reports/2026-09-06b-{m2x,phase1x,m3x}`, `out/timelapse-2026-09-06b`, brief 0.2.1).**

*Skeleton budget at K = 25 (D-018 procedure, jitter operand).* Seven of eight readings are `within_budget`; jitter churn is 0.003–0.013 everywhere, clock churn 0.117–0.491 (as it should be: twenty-five commits move the clocks). The eighth is a finding: **registry in layer geometry is `over_budget`** — untouched strata moved 0.090 against the 0.05 ceiling, while the same repository in age geometry moved 0.018. Layer strata are import depths, so a touched room's import edits ripple into the layers of rooms nobody touched; age strata ride the clock and do not. The budget reports it as jitter because that is what it is under D-024's definition: an untouched room's floor moved. It is not a defect in the instrument and it is not acted on here; it is recorded as the first evidence that the layer geometry's floors are less stable than the age geometry's on a repository with a dense import graph (registry: 264 rooms, 42 touched at K = 25), and as the trigger for the mapper's §7 Q6 discussion — a layer floor that moves under an untouched room is a claim the age floor never makes.

| repo | geometry | K | jitter churn | clock churn | strata moved (untouched) | verdict |
|---|---|---|---|---|---|---|
| typeorm | age / layer | 25 | 0.012 | 0.333 | 0.000 / 0.000 | within_budget |
| mcp-secure-server | age / layer | 25 | 0.011 | 0.263 | 0.000 / 0.011 | within_budget |
| uluops-registry-api | age | 25 | 0.013 | 0.491 | 0.018 | within_budget |
| uluops-registry-api | layer | 25 | 0.013 | 0.491 | **0.090** | **over_budget** |
| eslint | age / layer | 25 | 0.003 | 0.117 | 0.002 / 0.000 | within_budget |

*Time-lapses (twelve frames, D-021 schedule).* Jitter shares move little against the D-024 reading under the K = 5 gate: typeorm age 0.12 → 0.09 and layer 0.06 → 0.06 (the `flooded_basement` predicate change of D-031 is the mover), registry 0.29 / 0.19 unchanged, mcp-secure-server 0.30 / 0.16 unchanged, eslint 0.05 / 0.05 unchanged. The budget tally changes shape, not number: every transition on the long histories is `untested: beyond_pinned_k`, and mcp-secure-server's eleven transitions, which under the K = 5 gate had read `over_budget × 4, within_budget × 6` at a median K of 12, are now `untested: below_pinned_k × 11` — D-032 item 4 doing what it was written to do. The phase-1 reading (`reports/2026-09-06b-phase1x/phase1-reading.md`) and the viewer (republished, same URL) say so.

*Briefs.* All four pass the register lint — typeorm and eslint on attempt 1, mcp-secure-server and registry on attempt 2 — but only after brief 0.2.1, and the road there is the substantive result of this addendum. The first regeneration failed three of four. The model, told that every number must come from the sheet, began (a) chaining several counts in one bracket, `[crack ×21, toothpick_wing ×3]`, which the citation grammar did not parse, so every room in that sentence went unattributed (R2, R8 ×5); (b) listing example rooms in a sentence after the count that licensed them; (c) writing the calibration line I had just put on the facts sheet (D-032 item 5) as a paragraph, which no feature cites (R2) and which says "one frame" (R3); (d) spelling numbers out — "two hundred sixty-seven" — which the lint read as three numbers, none on the sheet (R3 ×7 on registry); (e) disclosing decorative features in its own words ("rest on nothing confirmed and enter no part of this diagnosis"), which R4's phrase match did not recognise; and (f) "seventy-fifth percentile" read as seventy. Every one of these is the lint refusing prose the register permits. Brief 0.2.1 (`brief.py`, commits ff247c9 and 901703f): a bracket may chain clauses on `;`, or on `,` between bare counts; `[f ×N: rooms]` is the form for examples under a count and the prompt shows it; the calibration line stays in the header and leaves the model's sheet; numbers in words are parsed as compounds and ordinals are skipped; "one" is a determiner; R4 rests on the citation (decorative features cited by count only, no room named, the sentence negating diagnosis in its own words) rather than on a phrase; `wing_count` is on the sheet; the prompt asks for digits. Tests: `test_lint_reads_chained_brackets_and_the_determiner_one`, `test_lint_reads_compound_spelled_numbers`. What did not change: a number the model computes for itself ("eight of the addresses on that list sit in scripts") is still refused, and a consequence-implying name still discloses where first used, bracket or prose. The lint is the authority (D-028); this addendum records that the authority had six holes through which correct prose fell, and that the fix each time was to make the rule rest on the citation's structure rather than on the sentence's wording.

*Signal-level stability at K = 25 (Dewey (a), tracker a07deb28).* The gate report (`reports/2026-09-06-m1c-k25/holdout-report.md`) is the first per-signal reading at 25: nineteen signals within eps 0.05 / delta 0.15 on every reference repository, `recent_commit_share` unstable by construction, `neglect_index` untested. The thresholds were carried from K = 5 unchanged; whether they sit where D-026's rule would put them (between the reference-set median and p90 of |Δ| at K = 25) is not yet read, because the gate reports maxima, not the distribution. Open.

## D-032 — Theory → practice cycle: the code enacts the specs; the ledger lagged the log (2026-09-06)

**Roster and rationale.** Alex recommended the theory → practice set (Machiavelli, Wang Yangming, Husserl, Dewey, Epictetus) for stated-versus-enacted audits of code and specs. First cycle, three of the five, aimed at three distinct declared surfaces: `machiavelli-validator` on the commitments DECISIONS.md makes under D-013 rule 1 (a decision names the code that enforces it) against `src/` and `tests/`; `wang-yangming-validator` on the principles the specs declare against the code that enacts them; `dewey-validator` on whether the warrants behind the week's decisions still hold after D-026/D-029/D-031. Husserl and Epictetus are held for the second cycle so the roster can be scored against this one (D-013 rule 2). Tracker run 14, `theory-practice-cycle`.

**Outcome.** EFFECTUAL 91, UNIFIED 89, GROWING 84. Convergence: the gap is propagation, not enactment. Eight of the closures claimed since D-018 trace decision → code → a test that fails without it; the checklists, one spec section, the README and the reference reports lag behind the log. Two nominal closures and two code gaps were found:

- *Nominal (Machiavelli):* `stability_perturbation_k` could be set back to 5 by config, so the D-031 pin was a default, not a floor; `decorative_reason` had to name *a* signal of the predicate but not the ungrounded one, because the loader has no statuses.
- *Code gap (Wang Yangming, critical):* validation §3.8's kind/status legality was enforced only as an assertion in the producer. A hand-edited `validation.json` carrying a predictive signal labelled `asserted` passed the C3 gate — the relabel route the spec names as the exploit was open at the consumer.
- *Code gap (Dewey):* `skeleton_diff` emitted `within_budget` at any K ≤ 50, including K = 5, where D-024 showed the ceiling cannot fire.

**Decision.** All four are closed in code, and the ledger is re-derived from the current rules rather than patched:

1. `ValidationConfig.validate()` refuses `stability_perturbation_k < 25` (`validation/config.py`; `test_stability_k_cannot_be_loosened_below_spec`).
2. `check_gate` requires a decorative feature's `decorative_reason` to name a signal of its predicate whose status is *not* validated or asserted (`mapper/engine.py`; `test_decorative_reason_must_name_the_ungrounded_signal_at_the_gate`). The loader's check (D-030) stays as the cheaper first refusal.
3. `check_gate` refuses a `validation.json` in which a predictive signal carries `asserted` or a descriptive one `validated`, before any feature is evaluated (`mapper/engine.py`; `test_gate_refuses_a_relabelled_signal`). The producer's assertion stays; the consumer, which is the component handed the document, now checks it too.
4. The budget verdict is `untested: below_pinned_k` for `commits_between < pinned_k` (`mapper/diff.py`; `test_budget_is_untested_below_its_pinned_k`). The token now travels neither above nor below the K it was pinned at; the numbers still do, labelled with their K.
5. The brief's facts sheet and header carry a calibration line: in-repo, self-relative (system spec §5.3); one frame; stability is read from a time-lapse, not from the page (`brief.py`).
6. Checklists: validation.checklist.md states the modal-share degeneracy rule (D-011/D-014, flag exemption D-030); structural-mapper.checklist.md restates the D-004 Q1 register wording and the jitter operand at K 25–50; time-lapse.checklist.md and time-lapse-spec.md §4 say K = 25, applied 25–50, with both untested bounds; system-integration.checklist.md §C ticks the five gate contracts that are enacted, each with the test that would fail without it. README status brought to D-032.

**Why.** D-013 rule 1 is only as good as the second reading of it. The two nominal closures show the pattern: a decision named code that *existed*, and the code did not *bind* — a default is not a floor, and "names a signal" is not "names the ungrounded signal". The relabel gap is the same pattern one level up: the spec named the gate as the enforcement point, and the enforcement lived somewhere the gate does not run. The rule is therefore sharpened: a decision names the code that enforces it *and the test that fails without it*, and enforcement lives in the component that receives the input, not the one that produced it.

**Assumes.** The three validators read the same commit (41bd2fd) — D-031's regeneration was still running when they read, so "briefs stale after D-029/D-031" is a finding about timing, not about the chain; the K = 25 regeneration (tag `2026-09-06b`) follows.

**Breaks if.** A future ruleset needs a decorative feature whose ungrounded signal is not in its predicate (then `decorative_reason` has nothing legal to name — extend the reason to name an overlay or geometry signal deliberately, do not loosen the check). A perturbation study at a K below 25 is wanted (then it is a study, run with a budget dict that says so in the report, not a change to `SKELETON_BUDGET`).

**Recorded, not fixed (Dewey).** (a) `stability_eps`/`stability_delta` were carried from K = 5 to K = 25 without re-reading the per-signal Δ distribution at 25; the K = 25 gate report is the first reading, and a re-derivation by the D-026 rule over a per-K distribution is open. (b) `tau_asserted` governs no signal the rulesets read since `neglect_index` left (D-031); the cross-modal bar stays in the gate for what reaches it, and the naming load sits with the register lint, which the mapper spec §2.4.2 already assigns. (c) `M_asserted = 2` survived the reference set growing from two to four; revisit with a fifth repository (D-030 P12). (d) `compute_strata` reads `age_days` outside `check_gate`; unrevisited, and harmless while `age_days` is asserted, but a geometry over a non-asserted signal would be the trigger. (e) Tracker runs 10 and 11 are the M1 gate (D-015) and the phase-1 review (D-024); numbered here so rule 2 has its map.

## D-033 · 2026-09-06 · The stability budget re-ranks over the untouched population; the tail operand follows the signal's ripple class; eps and delta placed where they fire; M_asserted is a majority

**Reading that forced it (`reports/2026-09-06-eps-delta/reading.md`, Dewey (a) of D-032).** Applying D-026's rule — a ceiling sits between the reference set's median and p90 of its operand at the pinned K, or it cannot fail — to the signal-level budget at K = 25 as it stood: over 92 (signal, repo) readings, `stability_eps` (0.05 over median |Δ|) sat five times above the set's p90 (0.0095) and could not fail; `stability_delta` (0.15 over max |Δ|) sat between (median 0.008, p90 0.228) and *did* fail — on `reinforcement_index` (max 0.65, 0.61), `test_fan_in` (0.42, 0.40), `centrality` (0.23, 0.19) and `fan_in_nonzero` (0.17, 0.16), each on typeorm and registry, the two repositories with the most touched nodes in the window. Every one of those signals was nonetheless `asserted`, because `M_asserted = 2` of four accepts a signal that fails on half the set. D-031's "nineteen signals stable at K = 25" was a statement about status, not about the per-repository test, and Dewey's item (c) was not backlog.

**Two things were wrong with the instrument, and one with the bar.** (1) The comparison value was each run's *whole-population* percentile. Removing twenty-five commits changes the touched files' values; those files re-rank everyone else; every untouched node's percentile moves a little even when its own value did not. Every *own* signal — size, churn, age, authorship, nesting — carried that residual (max 0.01–0.02), and the residual was read as movement. (2) For a signal whose value depends on other files — an importer, a test, a manifest, the repository's recent window — a single untouched node whose neighbour was edited moves a lot (a test file added in the window flips one node's `reinforcement_index` by 0.65), and `max` is a one-node statistic: the budget was failing a signal for reporting one real edge. (3) `M_asserted = 2` of four is not a bar over the set; it is a bar over its easier half.

**Decision.**
1. *Comparison value* (`validation/asserted.py::_compare_values`): an index or a flag compares as itself; every other signal compares on its percentile **ranked over the untouched population in both runs**. The touched files cannot re-rank the untouched ones.
2. *Ripple class* (`validation/config.py::GROUNDING[sig]["ripple"]`, `own` | `coupled`): a signal is `coupled` when its value can change without the node being edited — `fan_in`, `fan_out`, `fan_in_nonzero`, `centrality`, `load_index`, `test_fan_in`, `reinforcement_index`, `complexity_proxy_index` (through `fan_out`), `has_sibling_test`, `is_package_entry`, `recent_commit_share`, `neglect_index` (through the window). Everything else is `own`.
3. *Tail operand*: `max` for an own signal, `p95` for a coupled one; `max` reported beside either way (`operand` recorded in the stability block and the report). The own signals are the instrument's **null check**: after (1) they must read 0.000, and on all four repositories, all eleven own signals, they do (`test_local_signals_read_zero_after_reranking`; `out/validation-k25b`). The residual the old instrument showed was entirely the re-ranking artefact.
4. *Placement* (`reports/2026-09-06-eps-delta/reading-d033.md`, over the 48 coupled readings): tails median 0.001, p90 0.020, max 0.119 (`neglect_index` on mcp-secure-server); medians p90 0.0043, max 0.016. `stability_delta` moves 0.15 → **0.05**; `stability_eps` 0.05 → **0.01**. Both now fire on the reference set — delta on `neglect_index` (mcp 0.119, registry 0.064), eps on `neglect_index` at registry and typeorm and `centrality` at registry (medians 0.016, 0.010, 0.0135; 3 of 48 readings, delta 2 of 48) — and nothing the rulesets read changes status (grid in the log of this session: at eps 0.01 / delta 0.05 / M 3 the only asserted signal that falls is `neglect_index`, to 2 of 4). D-026's letter would put delta at ≤ 0.020 and eps at ≤ 0.004, where `centrality` falls on two repositories for a p95 of 0.025; the letter is not followed, because a bar at the p90 of a four-repository set is a measurement of the set, not a standard, and 0.05 is the convention the skeleton budget already carries (D-018) — a coupled signal whose 95th-percentile ripple exceeds five percentile points has, by that convention, moved. That the rule's spirit (the bar has fired) is met while its letter (p90) is not is stated here rather than hidden.
5. `M_asserted` 2 → **3**: a majority of the four-repository set. `ValidationConfig.validate()` refuses `m_asserted < 3`, `stability_eps > 0.01`, `stability_delta > 0.05` (D-012 floors; `test_stability_k_cannot_be_loosened_below_spec` pattern).
6. The canonical gate is re-run with these defaults (`out/validation-k25c`, archived `reports/2026-09-06-m1c-k25c/`); readings regenerate under it with tag `2026-09-06c`, briefs relinted rather than regenerated (the prose is unchanged; the provenance pointer is not).

**What changes in the gate's verdicts.** `neglect_index`: `untested` — now by the instrument (unstable on mcp-secure-server and registry, 2 of 4) rather than by D-031's "unstable by construction" argument about its input. `recent_commit_share`: **`asserted`** (3 of 4; degenerate on mcp-secure-server) — D-031's "unstable by construction" was, it turns out, the re-ranking artefact: ranked among the untouched population its p95 ripple at K = 25 is ≤ 0.032. D-031's ruleset decision stands on its second leg — a window over commit count is a definitional weakness on a bursty repository, and the ruleset does not read it until it is redefined over wall time — but the gate no longer says it is unstable, and the log must not either. Every other status is unchanged; the skeletons and the briefs' prose are unchanged.

**Why.** D-026 gave the project one rule for placing a ceiling. Applied to the signal budget it exposed that the budget's operand was measuring the instrument's artefact on the own signals and one node's edge on the coupled ones, and that the majority bar was not a majority. The fix is the one D-008 used for corroboration: set the bar per class, and say what each class's number means. The null check is the part worth keeping: an instrument whose control group reads exactly zero is one whose non-zero readings can be believed.

**Provenance note.** The exploratory run that produced the null-check and placement readings (`out/validation-k25b`) was launched without `--config config/tuned.toml`, so it carries the untuned predictive-index weights and a different substrate fingerprint; the descriptive signals' stability readings do not depend on those weights and are identical in the canonical run, which was made under the tuned config (`tuned_config_commit` 24c087a) and is the one archived.

**Assumes.** Four repositories; the placement is re-read when the set grows (D-030 P12) and when Phase 3 brings a corpus. `p95` over an untouched population of 136–529 nodes is 7–27 nodes' worth of tail; on a repository under `stability_min_n` the floor already refuses.

**Breaks if.** A coupled signal's ripple at K = 25 sits between 0.02 and 0.05 on a new repository — the bar admits it, and D-026's letter would not; that is the case that decides whether 0.05 was a convention or a convenience. Or a signal is added whose value depends on other files without being classed `coupled` — then its max fires on one edge and the log will read a working signal as unstable; the class is part of adding a signal (validation §2.4.1).

**Addendum to D-033 (2026-09-06, from the theory → practice cycle 2, D-035).** The "null check" of item 3 and the "Why" paragraph overclaims. Once percentiles are re-ranked over the same untouched node set in both runs, an own signal's Δ is zero *by construction* — its raw value is a function of the file's own content and history, invariant under truncation of other commits — so the 44 zeros are an identity of the procedure, not a control group, and they do not warrant the coupled readings. What they do check, and the test now says so (`test_local_signals_read_zero_after_reranking`): the class assignment (an own signal that reads above zero is misclassified — `recent_commit_share` read 0.5 and was moved to `coupled` on exactly that evidence) and the re-ranking's implementation. The reading script and the README now call it the class check. The "Why" sentence about a control group reading zero is withdrawn; the placement of eps and delta rests on the coupled readings alone, as item 4 already said. Two further corrections from the same reading: the degenerate branch published assigned zeros (`0.0 / 0.0 / 0.0`) into the measurement columns, and two such rows sat in the 48-reading placement set; it now publishes the deltas it computed with reason `degenerate`, and the placement reading leaves degenerate rows out and marks them *degenerate* rather than **fails** (which had fused a refusal about the distribution with a refusal about movement). The placement re-read without those two rows: 46 coupled readings, medians p90 0.0043, tails p90 0.0203 — unchanged to the printed precision; the numbers in item 4 stand.

## D-034 · 2026-09-06 · Forecast cycle 2: the gate binds to the substrate it was earned under; the skeleton says where its statuses came from; the legend says what a name means

**Roster and rationale.** The second half of Alex's forecaster set (D-013 rule 2 scores it against run 13): `unintended-consequences` on the causal chains the artifact sets off in use, `future-observer-agent` on what becomes invisible, quaint, illegible and load-bearing once its assumptions embed. Tracker run 15. ANTICIPATED 88, LEGIBLE 88.

**Findings that became decisions.**
1. *Gate transfer* (unintended-consequences C1/C2, critical). `substrate map` accepted any `validation.json` for any substrate: `check_gate` read statuses only and `map` compared no fingerprint, while `timelapse` refused the mismatch (`timelapse.py`). A document earned on four repositories under one substrate config carried an unqualified `asserted` onto a substrate extracted under another. Closed: `map_skeleton` refuses a validation document without a `substrate_config_fingerprint` or with one differing from the substrate's (`mapper/engine.py`; `test_mapper_refuses_a_validation_document_from_another_config`). What stays portable, and is meant to: the statuses themselves — they are properties of a signal under a config, earned on the reference set, and a small repository that cannot run the asserted bar (fewer than K + 1 commits or 30 untouched nodes) *is* the intended consumer of the reference gate. That is now on the page rather than implicit: the skeleton's `gate` block carries `reference_set` (the names and roles of the repositories the statuses were earned on), `reasons` (the gate's reason for every non-asserted status — D-035's finding, closed here), and `population_floor` (`n`, `min` = 30, `met`) so a picture over eleven rooms says that its top decile is one room. A `calibration` line ("in-repo, self-relative: every pNN ranks this repository's own population") sits beside them, and `archetype_reason` says why the whole-building label is `null` (D-019) the way `decorative_reason` says why a feature is decorative.
2. *Register surfaces* (C6; Husserl in D-035 found the same). The disclosure discipline of D-004 Q3 lived in the brief prose and the SVG tooltip; the legend and the banner — the surfaces a screenshot carries — printed bare names, and D-028 had recorded it open. Closed for the legend: `render_cutaway` reads each feature's `position_name` from the skeleton and prints it beside the label ("flooded basement — long-untouched, still-imported room"). The banner's count line still prints bare names for width; recorded, not closed.
3. *Pinned numbers* (C4; future-observer L-1, critical). K, eps, delta and M are measurements of four repositories, frozen as floors that `validate()` refuses to loosen, with their derivations in `reports/` directories that regenerate per gate tag. Closed as far as location goes: every constant in `ValidationConfig` now carries the decision and the report it was read from beside it, and the reference-set literals carry the sentence that every floor is a measurement of these four and that Phase 3 re-reads them (D-019). Not closed, and not closable here: the ratchet itself. D-033 already declined D-026's letter for the reason the forecaster gives ("a measurement of the set, not a standard"); what a hundredth repository does to the floors is the Phase 3 question, and the map from corpus calibration to each pinned number is owed there.
4. *In-repo calibration at the point of use* (C3; future-observer I-2). Every `pNN` in a ruleset ranks the mapped repository's own population, so roughly a tenth of rooms are `dark_room` in every repository forever, and a team that fixes every dark room gets the same count with new addresses. Disclosed in the brief header since D-032; now also at the point of use — a header comment in each ruleset, the `calibration` line in the skeleton, and the reason in the ruleset grammar's docstring (`ruleset.py`) beside the D-017 rule that a name denotes the raw metric ("an ECDF percentile is never 0"), which the observer had found as an exception with no rule.

**Recorded, not fixed.** `untested` as the modal outcome read as "failed" (C5; the page says why since D-032, the ladder reading is a reader's, not the page's). The stability budget rewarding edits away from hubs (C8; a gradient, recorded with D-030's). Non-JavaScript repositories getting silence rather than refusal (C9; `graph_degraded` marks half the building; a language gate is a substrate-contract question, listed with the direction candidates). The lint's grammar converging on one model's register (C7; D-035 adds the practice that answers it). The facts sheet, the citation grammar, the conjunction-only ruleset grammar and the two consequence lexicons as load-bearing and one-directionally accommodating (future-observer B-1…B-4): the brief header now names its brief version beside the facts hash so a PASS says which grammar it passed under; versioning the lexicons as a test is open. `archetype: None` as a dam Phase 3 removes (C12): the reason is on the skeleton now; the decision stays D-019's.

**Why.** Both reports scored the log as already naming most of the consequence surface; what it had not named was mechanical (a binding the mapper lacked and the time-lapse had) and locational (a derivation one file away from the constant it justifies). D-032 sharpened rule 1 to "enforcement lives in the component that receives the input"; D-034 applies it to the document the mapper receives, and adds the locational half: a number's derivation lives beside the number, and a picture's provenance travels with the picture.

**Assumes.** The reference set's statuses are the right thing to carry onto a new repository of the same config — the gate is an instrument calibration, not a per-repository audit. **Breaks if.** A signal's status turns out to depend on repository kind (a `coupled` signal stable on four libraries and unstable on a monorepo): then the reference set on the skeleton is the reader's warning and Phase 3's corpus is the fix.

## D-035 · 2026-09-06 · Theory → practice cycle 2: a check performed once and thereafter presented as performed

**Roster and rationale.** The remaining two of Alex's theory → practice set (D-013 rule 2 scores it against run 14): `husserl-analyst` on designed versus delivered experience (brief, cutaway, viewer, readings), `epictetus-analyst` on fact versus interpretation in the system's self-description (log, README, status vocabulary, reports). Tracker run 16. INTENTIONAL 86, FACTUAL 84.

**Convergence.** Both found the same shape in different places: a check performed once, encoded, and thereafter presented as performed. Husserl found it in the reporting layer — the phase-1 reading's provenance header was a literal in `scripts/phase1_reports.py` naming substrate 0.3.0 and the 2026-09-05 gate, printed above a table read live from manifests carrying the 2026-09-06 gate and D-032's `below_pinned_k` token — and in the lint's PASS standing where D-028's hostile reading stood. Epictetus found it in the instrument's account of itself — D-033's "null check" (corrected in the addendum above) — and in the degenerate branch's assigned zeros.

**Decision.**
1. The phase-1 reading's header is generated from the manifests beside it: gate fingerprint, validation-config fingerprint, `validated_at`, and the generation date; the script refuses to write one reading over runs made under different gates (`scripts/phase1_reports.py`). `reports/2026-09-06c-phase1x/phase1-reading.md` is regenerated; the earlier readings keep the headers their instruments wrote.
2. The gate's reason travels: `skeleton.json.gate.reasons` (D-034 item 1). The degenerate branch publishes computed deltas (D-033 addendum). `scaffolding` is a consequence-implying name — a temporary structure erected to be removed — and the lexicon (`NAME_CONSEQUENCE_WORDS`) now says so; maintainability 0.2.1 gives it a position name ("test-imported room at or above the median reinforcement"), so R5 requires its disclosure where first used, as it does for its neighbours. The four briefs regenerate under 0.2.1 (a relint cannot add a disclosure the prose lacks).
3. The brief header names its brief version beside the facts hash and points the reader at the time-lapse for the skeleton by gate fingerprint (Husserl PARTIALLY FULFILLED-3): a PASS says which grammar it passed under, and "stability is read from a time-lapse" says which one.
4. A standing rule, added to D-013: **every brief version bump, and every gate change that regenerates the briefs, seats a hostile reader on one brief** — the practice that produced R8 (D-028) — and records which marks the reader judged unsupported. The lint is the authority for what it checks; the reading is the authority for whether the checks are the right ones. First seating: the 0.2.1 regeneration of this decision, next cycle.

**Recorded, not fixed.** The stance paragraph is frozen where applicability is per-repository (typeorm's finished one-class error files, D-028; spec §4.4): a per-repository stance is a model-written sentence the lint cannot check, so it stays generic and disclosed. The holdout report prints the global status on rows whose own cells read `unstable` (Epictetus): the cells are the observation and stay; a per-row verdict column is a rendering change listed for the next report revision. D-033's "the bar has fired" is a criterion authored on 2026-09-06, not D-026's recovered intent; the addendum above and the placement reading's "(letter)" column say so.

**Why.** The instrument layer re-tethers itself — pre-registered ceilings fired (D-024), the lint was strengthened by a reading (D-028), the bars were re-placed by a reading (D-033). The reporting layer had begun to substitute regeneration for reading: a sentence written once about a run and reprinted over every later run. The rule is the one D-032 wrote for code, applied to prose: a header states what its rows state, or it states nothing; and a checker's PASS is re-read by a person each time the checker changes.

**Assumes.** One hostile reading per version is enough to catch a grammar drifting toward one model's habits (D-034's C7). **Breaks if.** Two consecutive readings find nothing the lint missed — then the seat is ceremony and the rule is retired with that evidence; or a reading finds something on every brief — then the lint's authority is the ceremony and D-028's "the lint is the hostile reader" is revisited.

## D-036 · 2026-09-06 · The first seating under D-013 rule 5: the brackets hold, the connective prose does not; brief 0.3.0 reads the sentences between the citations

**Seating.** `hostile-reader` on `reports/2026-09-06d-m3x/typeorm.brief.md` (brief 0.2.1) against its facts sheet, the register (architect-brief-spec §2, §4) and the mapper's §3. Tracker run 17. HATER_EXPOSED 87. The reading's own summary is the result: every bracketed citation resolves, every number checks, R4/R5/R7 hold — "the bracket layer is load-bearing and hostile attention cannot move it" — and every substantive finding lives in the prose between the brackets. Three classes: **scope** (a base-profile count bound to an all-profile total), **quantity** (distributional adverbs with no share on the sheet; "every wing that carries source" over two wings of seven), **sample** (two marks with identical room sets presented as two findings; exemplars chosen away from the false positive §4.4 documents).

**What was found, and what it was.** (1) `dark_room` and `flooded_basement` fire on the identical seventy rooms on typeorm — the `load_index ≥ 0.10` conjunct filters nothing there — and the brief gave them separate paragraphs, separate exemplars and the word "also"; a reader finishes with 140 marked positions where there are 70. Computable from the sheet; no rule stated it. (2) `co_located_count` was lifted from the base profile's summary while `diagnostic_count` spans every profile (D-028's fix for the same asymmetry on the diagnostic count did not reach the co-location count); the sentence "619 marks across all profiles, 84 of them co-located" bound one to the other. A facts-sheet bug. (3) "33 imported leaves sit mostly in src/driver/types and src/metadata/types" — 12 of 33. "Concentrated in", "the bulk of", "spread across" applied to 16/60, 39/70 and no shared threshold. No rule, and no share on the sheet for the prose to have used. (4) A directory named in a sentence needed no cited room inside it (R8 covers rooms; a directory is not a room), so a distribution over four directories was asserted on the warrant of two, and "the two eslint configurations" cited one. (5) "two sit outside src entirely" passed R3 because 2 was `toothpick_wing`'s count — the header's declared hole ("a computed number that happens to match"), demonstrated. (6) The header's time-lapse pointer printed "gate `?`": `facts()` read the fingerprint from a key the skeleton does not have. (7) The exemplars under `flooded_basement` were three files that are not error classes, while roughly half the set is `src/error/*Error.ts`, the population §4.4 records as the mark's known false positive on typeorm. Not checkable; recorded.

**Decision (brief 0.3.0, `brief.py`; `test_lint_reads_the_connective_prose`, `test_facts_sheet_carries_overlaps_wing_counts_and_all_profile_co_location`).**
1. The facts sheet carries `overlaps` — every pair of diagnostic features whose room sets are identical or nested, with the relation and the size — `by_wing` counts per feature, `co_located_count` across every profile with `co_located_count_base` beside it, and the gate fingerprint read from the skeleton's own key (`facts()` refuses a skeleton without one).
2. **R9** — for every overlap on the sheet, some sentence names both features. **R10** — a directory named in a sentence contains a room cited in that sentence (R8 extended from rooms to prefixes; wing names exempt). **R11** — the distributional lexicon (mostly, most of, largely, predominantly, mainly, chiefly, concentrated in, the bulk, spread across, scattered, throughout, every wing, all wings, reaches into every) is refused in prose; the sheet carries the counts per wing the prose may state instead. **R3 tightened** — a feature's numbers (its count, its by-wing counts) are admitted in the sentence that cites the feature, not anywhere in the paragraph; building-level counts are admitted anywhere.
3. The prompt says each of these in the model's terms; the header's list of what the lint checked says them in the reader's. The four briefs regenerate under 0.3.0 (tag `2026-09-06e`), and rule 5 seats the next reader on one of them.

**Why.** D-028 built the lint to stand where a hostile reading stood, and D-035 wrote the rule that a reading re-checks the lint each time it changes. The first reading under that rule found that the lint's authority had been complete over the layer it was written for — citations, numbers, words — and absent over the layer that carries the reader's impression: scope, quantity and sample. Each new rule is again structural (a relation on the sheet; a prefix relation; a lexicon), not a phrase match, and each was found by a person reading one brief. That is the rule working, and it is also the measure of how much a PASS meant before it: the marks the reader judged unsupported — `flooded_basement ×70`, the co-location gloss, "mostly" on `leaf_utility`, "every wing" on `scaffolding` — had all passed.

**Recorded, not fixed.** Exemplar selection (7): a rule that a feature's exemplars be drawn in a stated way (first three by path, say) would make the sample checkable at the cost of the prose choosing nothing; deferred to the next seating, with the question of whether the reader still finds it once R9 has told them the sets coincide. Rooms described by what they do ("the query-builder surface"): the declared hole, instantiated; stays declared. `import_root ×1` without §5.5's caveat: the sheet carries no hedge for a predicate the spec calls wrong for libraries; a `caveat` field on the ruleset feature, surfaced on the sheet and required by R5-style disclosure, is the shape of the fix and is listed for the next ruleset revision.

**Assumes.** The connective-prose rules are the next layer, not the last: the reading named scope, quantity and sample as the classes, and R9–R11 cover the instances found, not the classes. **Breaks if.** The second seating finds the same classes again with new instances the rules do not reach — then the rules are chasing the prose and the register needs a structural change (a brief that is a table with one sentence per row, say) rather than another rule.

**Addendum to D-036 (same day).** The four briefs regenerated under 0.3.0 (`reports/2026-09-06e-m3x/`): typeorm and mcp-secure-server pass on attempt 1, eslint on 2, registry on 3. The typeorm brief now opens its age paragraph with "Age marks two sets that coincide", states the by-wing counts in place of the adverbs, and — unprompted — names `src/error/TypeORMError.ts` as the first `dark_room` exemplar, the population the previous brief's sample avoided. Whether that survives a reader is the second seating's question.

## D-037 · 2026-09-06 · The second seating: the overclaims moved out of the verbs into arithmetic and implicature; the sheet is typed rather than the prose tabled

**Seating.** `hostile-reader` on `reports/2026-09-06e-m3x/typeorm.brief.md` (brief 0.3.0), told what the first seating found and what 0.3.0 did about it, and asked D-036's breaks-if question directly. Tracker run 18. HATER_EXPOSED 86. Citation layer: two seatings, zero defects.

**Answer to D-036's breaks-if, as the reader gave it.** *Quantity* is closed — R11 held, no adverb, no unsourced share. *Scope* recurred in a form the D-036 fix created: `co_located_count` counts rooms carrying two or more marks, the prose wrote "160 of those marks", and the sheet carried two co-location numbers of different kinds under one name (the all-profile room count and the base summary's more-than-one-profile count); the fix moved the error from provenance to type. *Sample* recurred unreached and now slightly disincentivised: the `dark_room` exemplars were `TypeORMError.ts` and `ObjectLiteral.ts`, the two members of the seventy that also carry `foundation`, `hub` and `scaffolding`, in a set of which thirty-three are `src/error/*Error.ts`; and R10 made naming `src/error` cost a cited room, so the safe prose never named it. Two findings were outside the three classes and of one shape — a sentence literally true of the sheet carrying a comparison the sheet does not license: "Age marks two sets that coincide" (age and load draw the sets; the identity means the load conjunct excluded nothing, and R9's forced pairing made the convergence reading stronger), and "neither rests on a validated measure" said of the decorative pair when no signal in the gate holds `validated`, which manufactures the contrast D-015 and D-035 refused. Also: "the widest single set" ranks a fixed-threshold feature against p90 sets whose size is fixed by construction; the `STANCE` string listed "fixes should not concentrate", the norm behind the decorative `bug_pressure_index`, among the diagnosis's norms; and a compliant disclosure ("a location in the import graph … not a claim about damage") matched none of R1's amnesty clauses and survived by the lexicon's luck. The reader's recommendation, from the position of someone who wants the brief to keep failing: "do the sheet work and you close three; ship a fifth rule instead and I will be back next version with a sixth instance."

**Decision (brief 0.4.0; `test_lint_types_the_sheet`).** The sheet is typed and the rules read the types; the table remains the fallback.
1. *Units.* `co_located_count` becomes `co_located_rooms`, with `co_located_rooms_base_profile` beside it, and the sheet carries `units` for every building-level number (population and wings in rooms; diagnostic and decorative counts in marks; a feature's count in rooms, one mark per room). **R12**: a number followed by "marks" or "rooms" wears the unit the sheet gives it; "160 of those marks" is refused when 160 counts rooms.
2. *Inert conjuncts.* An identical overlap carries `inert_terms` — the conjuncts one predicate has and the other lacks. **R13**: the sentence that names the pair (R9) names the inert signal too, so the identity is read as a conjunct that did no work, not as two measurements agreeing.
3. *Status vocabulary.* **R14**: "validated" is refused unless some signal in the gate holds that status. Every feature in v0 rests on an asserted signal; a sentence that contrasts a decorative feature with "a validated measure" describes a gate that does not exist.
4. *Composition.* Each feature carries `dominant_dir` — the largest single directory among its rooms and how many. **R15**: a diagnostic feature of six or more rooms with a third or more in one directory names that directory in a sentence that cites the feature; R10 then requires a cited room inside it. The exemplars can no longer be drawn away from the set's composition, and naming the directory is the cheapest sentence rather than the most expensive.
5. *Rankings.* R11 gains the comparative and superlative lexicon (widest, largest, fewest, wider than, …): the prompt's "do not set two features against each other" is now enforced.
6. *Register.* The `STANCE` string names only the norms behind asserted features (reinforce load; visit old load-bearing code); the fixes norm leaves with the decorative index it belongs to. R1's disclosure clauses are widened to the clause rather than the phrase ("not a claim about …", "a location in the import graph").
7. The four briefs regenerate under 0.4.0 (tag `2026-09-06f`). Rule 5 seats the third reader on one of them.

**Why.** The two seatings drew a line the project had not drawn: the citation layer is settled — brackets, counts, rooms, disclosures — and every remaining overclaim is a true sentence carrying a comparison or a unit the sheet did not license. D-036 asked whether that meant the register needed a structural change. The reader's answer, which is adopted here, is that the structural change is to the *sheet*: a number without a unit invites the wrong unit; an identity without its inert conjunct invites a corroboration reading; a status scale the page never shows invites a contrast against its missing top. Each of R12–R15 rests on a field the sheet now carries, which is the pattern of every lint rule that has held (D-028, D-031 addendum, D-036): the rule reads a structure, not a phrase. The table stays the fallback for the case a rule cannot reach — an exemplar list that is representative — and R15 is the last attempt before it.

**Assumes.** Three seatings on one repository's brief are comparable (D-013 rule 2); the third is seated on a different repository's brief to test that. **Breaks if.** The third seating finds the same shape — a true sentence with an unlicensed comparison — through a field the sheet does not carry. Then the sheet is not the fix, the prose is, and the tabular brief is built as a prototype first (the `prototype` skill) to see whether a row per feature reads at all.

## D-038 · 2026-09-06 · The third seating: a nominal closure in the project's own hand, a rule that generated prose, and the tabular brief goes to a prototype

**Seating.** `hostile-reader` on `reports/2026-09-06f-m3x/eslint.brief.md` (brief 0.4.0), a different repository by design (D-037 "Assumes"). Tracker run 19. HATER_EXPOSED 88. Three seatings, zero citation defects; R11, R14 and R15's sample fix held — the `dark_room` exemplars were drawn from `lib/shared`, its largest directory, which is what D-036 and D-037 could not make happen.

**What was found.** (1) **A closure that was never written.** `scaffolding` fires on 371 of eslint's 473 rooms and carried no disclosure, because `rulesets/maintainability.toml` was still 0.2.0: D-035 item 2 ("maintainability 0.2.1 gives it a position name"; "'scaffold' joins `NAME_CONSEQUENCE_WORDS`") was logged, its tracker issue closed, and neither edit reached the file — the patch that carried them aborted on an earlier line and its "ok" was read as the whole. That is Machiavelli's class from D-032 (a decision naming code that does not bind), recurring in the project's own hand two decisions later, and found only because a reader looked at the sheet the ruleset produced. Written now, as `maintainability.toml` 0.2.1 and the lexicon entry, with the D-035 tracker issue reopened and re-closed against this commit. The rule this adds to D-013 rule 1: a closure names the *test that fails without it*, and a ruleset change names the regenerated sheet that shows it — the 0.2.1 briefs of tag `d` showed `scaffolding` undisclosed on every page and nobody read the sheet. (2) **A nesting stated as an identity.** `flooded_basement ×75` sits within `dark_room ×83`; the brief wrote "one set of rooms: two marks … are one finding", and `corridor ×22` within `hub ×50` the same — the extra conjunct removed 8 and 28 rooms and the prose said it removed none. R13 guarded `identical` relations only. (3) **R13's guard inverted.** `foundation` carries the literal predicate `load_index >= p90` in both profiles; `inert_terms` is empty; R13 fired nothing; "the maintainability set and the onboarding set are the same 48 rooms" reads as two profiles agreeing — D-037's own finding on another repository, through a field the sheet carries and the rule read backwards. (4) **R15 generated a base rate.** `lib/rules` holds 305 of eslint's 473 rooms; R15 compelled "98 of the 148 sit in lib/rules" with no denominator on the page or the sheet, so the base rate printed as composition. A new class the 0.4.0 fix created: a rule that generates a sentence rather than constraining one. (5) The `co_located_rooms_base_profile` unit gloss was a predicate, not a unit, and the prose dropped its qualifier. (6) R12 was evaded by a spelled numeral, and "one finding, two marks" counted features, a unit the sheet does not carry. (7) The decorative disclaimer never said why (`decorative_reason` on the sheet, off the page). (8) `import_root ×19` without §5.5's caveat, recorded again.

**Decision (brief 0.5.0; `test_lint_reads_nestings_shared_predicates_and_the_decorative_reason`).**
1. A `within` overlap carries `n_outside`; R13 requires the sentence naming the pair to state it and refuses the identity nouns (one set of rooms, the same N rooms, identical, one finding, coincide). An `identical` overlap carries `shared_predicate`; when true, the sentence naming the pair must say "predicate" — one measurement under two profiles, never two agreeing.
2. `dominant_dir` carries `population`; R15 requires the share and its denominator in the same sentence ("98 of the 148 sit in lib/rules, which holds 305 of the building's 473 rooms"). R3 admits both numbers where the feature is cited.
3. `co_located_rooms_base_profile` leaves the sheet; the all-profile room count is the only co-location number the prose may use.
4. R12 reads spelled numerals and refuses "findings" as a unit.
5. R4b: a sentence citing a decorative feature names the ungrounded signal from its `decorative_reason`.
6. The four briefs regenerate under 0.5.0 and ruleset 0.2.1 (tag `2026-09-06g`).
7. **The tabular brief is prototyped** (the `prototype` skill; D-037 "Breaks if"). The reader's answer to that clause: the sheet was the right layer for the classes it typed and the wrong layer where a rule *generates* prose — and each new field invites the next uncarried denominator (lines, wing share, profile overlap). A row per feature with the columns n · by-wing · dominant dir (n / population) · relation-to · position name states (2)–(4) structurally and leaves no sentence for the implicature to live in. The prototype's question is whether such a page reads at all — whether a reader can take in a building from rows — and it is answered by building one over the eslint and typeorm sheets, not by another rule.

**Why.** Three seatings have drawn the same line three times: the citation layer holds; the overclaims live in the sentences between citations; each rule that reads a structure holds and each rule that generates a sentence creates the next overclaim. R15 is the first rule of the second kind, and it produced (4) on the first monorepo it met. That is the evidence D-037's breaks-if asked for. The rules of 0.5.0 are kept because they constrain (a nesting must state its remainder; a share must state its denominator); the prototype is built because the reader is right that the denominators do not end.

**Assumes.** The tabular page is a rendering of the same facts sheet under the same lint (a row is a sentence with fixed slots); the register survives the change of form. **Breaks if.** The prototype shows that rows do not read — a reader cannot tell a building from a table of fifteen features — in which case the prose brief stays, with its seatings, and the sheet keeps growing denominators as they are found.

**Addendum to D-038 (same night).** The first 0.5.0 regeneration (tag `g`) failed all four briefs, and the failures were the rules contradicting each other: R13 demanded the rooms-outside number in the pairing sentence while R3 refused it (no cited feature carries it), and R12 refused "two marks on one set" because a wing happened to hold two rooms. Both closed: a sentence that names an overlap's pair admits that overlap's numbers; a count of three or fewer "marks" is read as a count of feature names. The briefs regenerate as tag `h`. The prototype of item 7 is built (`scripts/prototype_tabular_brief.py` on the throwaway branch `prototype/tabular-brief`, published at https://claude.ai/code/artifact/1fe479eb-d534-4841-8dc8-46bbb00d6fc6): three shapes over the same sheets — A, a register table with one row per feature and fixed slots; B, a wing ledger with wings as rows and features as columns, composition first; C, a room roll with rooms as rows grouped by directory and marks as dots, where overlaps and composition are patterns rather than claims. Not a decision; the page is the question.

## D-039 · 2026-09-07 · The brief carries a register: the inventory is rendered by code, the prose is the reading

**Prototype and its answer (D-038 item 7).** Three shapes were built over the same 0.5.0 facts sheets with the `prototype` skill and published for judgment (`scripts/prototype_tabular_brief.py` on branch `prototype/tabular-brief`; https://claude.ai/code/artifact/1fe479eb-d534-4841-8dc8-46bbb00d6fc6): **A**, a register table with one row per feature and fixed slots; **B**, a wing ledger with wings as rows and features as columns; **C**, a room roll with rooms as rows grouped by directory and marks as dots. The question was whether a building can be read from rows and where a sentence turns out to be needed. The answer, mine and Alex's: A is the honest successor to the inventory half of the prose — every slot is a field the three seatings asked for, and there is no sentence for an implicature to live in — and it does not replace the reading half, which is the one thing a table cannot do and the one thing the surveyor register exists for. C is a picture and belongs beside the cutaway, not in the brief; B is a dashboard and its content is already A's by-wing column. The prototype stays on its branch as the record; the main branch keeps the decision.

**Decision (brief 0.6.0; `render_register`, `test_register_table_carries_the_inventory_and_the_prose_is_the_reading`).**
1. The brief page is header → **register** → **reading** → provenance → lint. The register is rendered from the facts sheet by code: a building line (rooms and wings, marks across all profiles and in the base profile, decorative marks and their features, rooms with two or more marks, the gate with how many signals are asserted and that none is validated) and one row per feature — position name, rooms, counts per wing, dominant directory as *n / its rooms*, relations to other features (`= x (same predicate, two profiles)`, `= x (load_index >= 0.10 excludes nothing)`, `⊂ x (8 of its rooms outside)`), and the predicate or the decorative reason. Every cell is a field; no cell is a sentence.
2. The prose is the reading: 200–400 words, no inventory. The prompt says the register is on the page and asks for what a reader of the register would still need said — the building's shape, where the marks sit relative to one another, how many distinct sets of rooms the diagnosis actually names, what the decorative marks are excluded for, the stance.
3. The lint keeps every refusal on the prose (R1–R6, R8, R10–R12, R14; the identity-noun and two-profiles-agreeing halves of R13) and moves the inventory *obligations* to the register, where they are met by construction: R7 (counts stated), R9 and the naming half of R13 (pairs named with their remainder or inert conjunct), R15 (dominant directory with its population), R4b (the decorative reason). `lint(text, facts, register=True)` is the mode the page uses; `relint` recovers the prose from a page by dropping the register and its headings.
4. The four briefs regenerate under 0.6.0 (tag `2026-09-06i`). Rule 5 seats the fourth reader on one of them, on a repository the third seating did not read, and asks the narrower question the register leaves: does a shorter prose that owes no inventory have fewer places to overclaim, or does the reading grow its own?

**Why.** Three seatings drew the line between what a citation can carry and what a sentence smuggles. The rules that read a structure held; the one rule that generated a sentence (R15) produced the next overclaim. A table is the form in which every structural obligation is met without a sentence being generated, and D-038's breaks-if named the prototype as the test of whether that form reads. It does, for the inventory. The reading is what the prose was always for, and it is now the only thing the prose is asked to carry — which is also the only thing a hostile reader now has to attack.

**Assumes.** A reader of the page reads the register first; the prose refers to it rather than restating it. **Breaks if.** The fourth seating finds the reading restating the register in sentences (the inventory has moved, not shrunk), or finds the register itself carrying an implicature (a column that reads as a ranking) — in which case the register gets a lint of its own, over its cells, and the prose is cut to the stance and the overlap sentences.

**Rule 6 of the prototype skill, honoured.** The validated decision is folded into `brief.py`; the prototype and its losing variants live on `prototype/tabular-brief`; the answer is recorded here and on tracker run 19's issue for R15.

## D-040 · 2026-09-07 · The fourth seating: the register is a surface too; its cells get tests, the reading gets R16

**Seating.** `hostile-reader` on `reports/2026-09-06i-m3x/mcp-secure-server.brief.md` (brief 0.6.0, the first page with a register; a repository no seating had read). Tracker run 20. HATER_EXPOSED 88. Four seatings, zero citation defects; every register count matched the sheet; `= foundation (same predicate, two profiles)` could not be read as agreement — D-038's shared-predicate cell held.

**D-039's breaks-if, answered.** Both horns fired and a third the decision had not anticipated. (1) *The inventory moved, not shrunk*: about half the reading's words were the rooms and by-wing columns in prose, because no rule refused a number the register already carried. (2) *The reading grew its own overclaims*, three, all of the shape the previous seatings named — "three relations cut the count of distinct sets" (only the identical one merges; nestings leave two sets), "91 rooms … out of 287 diagnostic marks" (a ratio across units R12 did not read), "the graph marks fall on both sides" (an implicature that the age marks do not). (3) *The register's derived cells make claims nothing checks*: the ⊂ cell printed the superset's remainder with the subset's possessive ("5 of its rooms outside" reads as false and contradicts the operator beside it); "dominant directory" printed for every feature with R15's one-third threshold disabled in register mode and ties broken lexicographically in silence (`scaffolding … src/security/utils 4 / 9` with six directories tied at four; `package_entry … 1 / 8`); the directory was the undeclared immediate parent (`src/security/layers` holds nine rooms as a parent and about thirty-one as a tree); the building line's "287 diagnostic marks" counted the identical pair twice while the next column declared it one predicate. Also: the position-name disclosure thinned to one occurrence, four names appearing only as cells; "–" meant two things in two columns. The attempts log showed the shorter prose reaching for a share and a ranking on its first draft and dropping three disclosures on its second: the reading did not stop wanting to overclaim; the lint filed it down.

**Decision (brief 0.7.0; `test_register_cells_are_linted_by_their_tests`).**
1. **The register's lint is its tests.** A cell is rendered by code, so the check that fails without the fix is a test over `render_register` and the sheet, not a rule over prose: the ⊂ cell names whose rooms are outside ("8 dark_room rooms outside this set" on the subset row; "8 of these rooms outside it" on the superset row); a directory prints only when it holds a third or more of the feature's rooms and says "(tied)" when it does not hold it alone; below the third the cell reads "none holds a third", a cell's own answer rather than a gap; the column head says what the directory is ("largest parent directory n / rooms in it") and the register's note says it is the immediate parent, non-recursive; a feature without a consequence-implying name reads "no consequence in the name" rather than "–".
2. **Two numbers join the sheet** so the reading can say them instead of computing them: `distinct_room_sets` (the diagnostic features' sets, an identical pair counted once, a nesting twice) and `marks_on_identical_pairs` (the all-profile marks that fall on the same rooms twice because two profiles carry one predicate). The building line states both beside the mark totals, so "287 marks" is read with "21 of them fall on the same rooms twice" and "the diagnostic features name N distinct sets of rooms".
3. **R16 — the reading does not restate the register.** In register mode a feature's by-wing and directory numbers are refused in the prose; its count and the building's totals remain sayable. The prompt says so and points the model at `distinct_room_sets`.
4. **R12** refuses a ratio across units ("N rooms … out of M marks"). The "graph marks fall on both sides" implicature is recorded, not ruled: a contrast between mark families is the class D-037 named and the sheet carries no field for it.
5. The register's note glosses the position column (a position names where a room sits — in the import graph, on the clock, in the test graph — and is not a claim about its condition, D-004 Q3), which is what the prose used to carry at first use and now carries only when it names a feature.
6. The four briefs regenerate under 0.7.0 (tag `2026-09-06j`). Rule 5 seats the fifth reader on a page of this version.

**Why.** D-039 assumed a code-rendered table was an inert surface. It is a surface: every derived cell — a remainder, a "largest", a total across profiles — is a claim, and moving an obligation from the lint's reach into a cell is not the same as discharging it. The fix the reader recommended is D-039's own breaks-if clause, and the form it takes is the one the project uses for code everywhere else (D-013 rule 1, D-032, D-038): a test that fails without it. The reading, for its part, did what D-038 predicted a shorter prose would do — it reached for the same three shapes — and the two structural answers (a number on the sheet for what it kept computing; a refusal for what it kept repeating) are the pattern that has held since D-036.

**Assumes.** A cell that is a field of the sheet needs no rule; only a *derived* cell (relation remainders, largest directory, cross-profile totals) needs a test, and those are enumerated here. **Breaks if.** The fifth seating finds a derived cell not on that list carrying a claim, or finds the reading still restating the register through the counts R16 leaves sayable — then R16 widens to feature counts and the reading is cut to the relations, the decorative disclosure and the stance.

**Addendum to D-040 (2026-09-08).** The first 0.7.0 regeneration (tag `j`) failed all four briefs on R16, and the failures were mine twice over: R16 refused a number the sentence was otherwise entitled to (a by-wing count that is also a nesting's remainder, "5" on mcp-secure-server; a feature's own count), and the prompt still carried the D-036/D-037 bullets telling the model to name the dominant directory with its population — the sentence R16 now refuses. R16 runs after the entitlements (building totals, cited features' counts, a named pair's numbers) and refuses only what is left; the relation count joins the sheet's numbers ("three relations"); the three bullets are rewritten to say the register carries those cells and the reading does not. Tag `k` is the regeneration that counts.

## D-041 · 2026-09-08 · The fifth seating: a cell's else-branch is where the next overclaim lived; the reading contradicted the register; R16 becomes a binding

**Seating.** `hostile-reader` on `reports/2026-09-06k-m3x/registry.brief.md` (brief 0.7.0; uluops-registry-api, which no seating had read). Tracker run 21. HATER_EXPOSED 88. Every field-backed cell verified — 96 cells, five nestings' remainders, four directories, the two numbers D-040 added — and the identical pair read as one predicate twice, not as corroboration.

**D-040's breaks-if, answered.** (a) *A derived cell outside the list carried a claim* — twice, and the reader's diagnosis is the finding: D-040 tested what a cell says when it has something to say and left untested what it says when it does not. `pos = position_name or "no consequence in the name"` printed a verdict about a feature's name from the absence of a position name, never reading `name_implies_consequence`; under 0.6.0 that gap printed "–" and was visible, under 0.7.0 it printed a denial, and D-038's own failure mode (a consequence-implying name whose position name never landed) would now read as "no consequence". `package_entry`, one room, `src` holding one of one, printed "none holds a third" because the six-room size guard was folded into a flag named for a ratio. A singleton's two `⊂` cells carried remainders of 39 and 132 and read as large findings; "1 dark_room rooms"; a parent directory named `scripts` beside the wing `scripts` at a different population. (b) *The reading restated through what R16 left sayable* — and worse, R16 failed in both directions: it withheld the deflating number ("the scripts wing holds import-graph roots [×11]" when eight of eleven are there; "reaches there too" for one room of twenty-seven) and permitted the bare restatement where a feature's count equals its by-wing count. (c) *The reading grew a new shape*: "the unshared positions stand apart, among them leaf_utility and lit_room" — `lit_room` carries `⊃ package_entry` in its own register row. The first sentence in five seatings that is false against the sheet, and it became possible the moment R9's obligation moved into a cell and left the prose free to deny the cell. Also: "from src/utils/logger.ts to src/middleware/auth.ts" as a span over rooms the page does not order; `import_root ×11` without §5.5's caveat, the third recurrence.

**Decision (brief 0.8.0; onboarding 0.2.1; `test_register_fallbacks_say_the_reason_that_is_the_reason`).**
1. **Every fallback says the reason that is the reason.** `dominant_dir` carries `placeable` (six or more rooms) separately from `holds_third`; the cell reads "too few rooms to place (1)", "none holds a third", or the directory — and a parent that shares a wing's name reads "src (as parent, not the wing)". The position cell reads the field it reports: a position name; else, for a consequence-implying name, "POSITION NAME MISSING (ruleset defect)" in capitals, because that is a ruleset defect and the page says so rather than denying it; else "no consequence word in the name (lexicon)", naming the source of the claim. A remainder of one is singular. No relation is drawn for a set of fewer than three rooms — a set of one is inside anything that contains it.
2. **R17.** A prose construction that denies a relation (unshared, stands apart, no overlap, shares no rooms, independent of, unrelated) naming a feature the sheet lists in an overlap is refused. The prose may say less than the register; it may not contradict it.
3. **R16 is a binding, not a refusal.** A feature's count in a wing or directory is sayable in a sentence that names that wing or directory, and nowhere else. The deflating number is therefore always sayable ("8 of the 11 import_root rooms sit in scripts") and the bare restatement never is. The prompt says so in those words.
4. **R11** refuses "from *room* to *room*": rooms are not ordered and two exemplars are not endpoints.
5. **A ruleset feature may carry a `caveat`** — the ruleset's own warning about a predicate — which travels through the skeleton to the sheet and prints in the register's last column. `import_root` carries §5.5's: an import-graph root in a library is not its entrance; the package's entry has fan-in and cannot qualify. Three seatings asked for it.
6. The four briefs regenerate under 0.8.0 (tag `2026-09-06l`). Rule 5 seats the sixth reader on one of them.

**Why.** Five seatings, and the line moves each time toward the code: the citations held; the connective prose was ruled; the register's computations were tested; now the register's *fallbacks* are tested, and the prose is bound to the register rather than merely relieved of its obligations. The reader's three implications are adopted as rules of construction: enumerate fallbacks, not computations; a prose claim about a relation is checked against the list the register was rendered from; a refusal needs an entitlement per construction, not per number. What has not moved in five seatings is the score band (86–88) and the verdict, which says the seatings are finding what is there rather than running out.

**Assumes.** The fallback branches enumerated here are the register's; a new column adds its fallback to the tests when it is added. **Breaks if.** The sixth seating finds the reading false against the register through a construction R17's lexicon does not name — then the binding is structural (every feature name in the reading carries its register relation alongside, or the reading may not name a feature that has one), not lexical.

## D-042 · 2026-09-08 · The sixth seating: enumerate the explanations, not the numbers; a caveat is a limit on the predicate, never a claim about the repository

**Seating.** `hostile-reader` on `reports/2026-09-06l-m3x/typeorm.brief.md` (brief 0.8.0). Tracker run 22. HATER_EXPOSED 87. Six seatings, zero citation defects; every field-backed cell and every 0.8.0 fallback verified — "too few rooms to place (2)", "none holds a third" where it is true, "src (as parent, not the wing) 4 / 5", singular remainders, the three-room floor, the `dark_room` exemplar drawn from `src/error`.

**D-041's breaks-if, answered.** Yes, twice, and neither through the model. The building line's gloss — "129 of them fall on the same rooms twice *because two profiles carry one predicate*" — was written by D-040 on a repository whose only identical pair was the shared-predicate kind. On typeorm 70 of the 129 are `dark_room`/`flooded_basement`: one profile, two predicates, a conjunct that excludes nothing, which the register's own relation cell three lines down says. The reading transcribed the gloss and cited `foundation ×59` as warrant for 129 — a positive causal clause false against the register, through no word in R17's lexicon. And the caveat three seatings asked for (D-041 item 5) asserted a fact about the repository — "the package's entry has fan-in and cannot qualify" — false of the one room it was printed beside: `packages/codemod/src/index.ts` is both `import_root` and `package_entry` with fan-in 0. The reading then wrote "the register's caveat holds that…" with the falsifying room in the same sentence. Also: R16 as a binding licensed by-wing restatement on a one-wing building, since naming `src` costs nothing when `src` is 85% of the rooms; "accordingly" asserted a proportionality by a part of speech R11 did not list; the header's reader-facing checklist still described a rule the lint no longer applied; "warts and all" in the stance is a condition idiom on a page whose discipline is that a position is not a condition.

**Decision (brief 0.9.0; onboarding 0.2.1 caveat rewritten; `test_explanations_are_split_by_cause_and_the_rule_list_has_one_source`).**
1. **A number with two causes is two numbers.** `marks_on_identical_pairs` keeps its meaning (an identical pair counts each room twice) and the sheet carries `marks_on_shared_predicates` and `marks_on_inert_conjuncts` beside it; the building line states all three with their causes, and the units block glosses each. The rule of construction, from the reader: any cell that says "because" is a claim whose scope is tested against the population it summarises — enumerate the explanations, not the numbers.
2. **A caveat is a limit on the predicate, never a claim about a repository.** The `import_root` caveat now reads: "reads no fan-in, not entrance (§5.5): a package entry that other rooms import cannot qualify, and one that nothing imports can" — true on every repository because it says what the predicate reads, not what the building is. The ruleset field says so; a caveat that asserts a fact about the repo is a ruleset defect the next seating is asked to look for.
3. **R16: the largest wing's count is the register's.** A minority wing's count is sayable where the wing is named — the deflating number stays sayable — and the largest wing's count is not sayable in the reading at all, because naming it is free and its number is the register's row read aloud.
4. **R11** refuses the proportionality words (accordingly, in proportion, proportionally, correspondingly, as one would expect). The stance loses "warts and all".
5. **One source for the rule list.** `RULES` holds each rule's id and its reader-facing sentence; the header's "what the lint checked" and the lint section's list both derive from it, and a test asserts every rule appears on the page — the header can no longer describe a rule the lint no longer applies.
6. The four briefs regenerate under 0.9.0 (tag `2026-09-06m`). Rule 5 seats the seventh reader.

**Why.** The line has now passed through every layer of the page in six steps: citations (held), connective prose (ruled), derived cells (tested), fallback cells (tested), and now the sentences code writes to *explain* a number and the sentences a ruleset writes to *qualify* a predicate. Each was found by a reader and closed by a structure: a split field, a rephrasing rule, a binding, a single source. The seatings are not running out — six scores in a three-point band say the reader is finding what is there — and each layer found is one the project had believed inert. The next layer, by the same logic, is whatever sentence remains that no field backs; the register's note is the last such text on the page, and the seventh reader is pointed at it.

**Assumes.** The explanations on the page are now the building line's causes, the register's note, the caveats and the stance; each is either a field, a limit on a predicate, or a fixed sentence the seatings have read. **Breaks if.** The seventh seating finds the register's note or the stance carrying a claim a repository refutes — then the note is generated from the sheet's fields the way the building line is, and the stance is reduced to the norm's name.

*Note on the record: commit fba825e carries D-042's brief.py changes under this decision's message but not the caveat rewrite, the tests or this entry, which a patch script dropped on an anchor; they follow in the next commit.*

## D-043 · 2026-09-09 · The seventh seating: the under-covering disclosure; the note is generated from the constants, every position names its record, the stance is a norm's name

**Seating.** `hostile-reader` on `reports/2026-09-06n-m3x/eslint.brief.md` (brief 0.9.0), pointed at the register's note and the stance and asked the convergence question. Tracker run 23. HATER_EXPOSED 86. Every field-backed cell and every fallback verified; R16, R13 and the repo-independent caveat held.

**D-042's breaks-if, answered, and convergence.** Not converged, and the reason is a new class the reader named: the *under-covering disclosure*. Six seatings found sentences claiming more than their warrant, each closed by refusing a word, binding a number or testing a cell. This one found the opposite shape — a disclosure whose stated scope is narrower than the surface it covers, with no offending token to refuse. The note said "relations are drawn between sets of three or more rooms" and stated no kind, while the column draws only identity and containment, so `relation to: none` on `import_root` (three rooms shared with `package_entry`) and `crack` (about twenty with `lit_room`) was true only under a definition the note did not state. The note's position gloss named three records — import graph, clock, test graph — and omitted edit pressure and load, the two whose position names most read as condition, which 0.4.0 had named inline per feature. The stance's "drawn as it is" was a fidelity claim the page refutes four ways, and its "load should be reinforced" a norm only the excluded `toothpick_wing` encodes. Also: the reading paraphrased the caveat into a property of rooms — "reading no fan-in and so not an entrance" — false of three `import_root` rooms that are package entries, one the sentence's own exemplar; "sit near these" and "alike" recurred the ruled shapes; and a citation that warranted neither number in its sentence was, in the reader's words, a citation doing decorative work — the hairline breach of the layer seven seatings had called unbreached.

**Decision (brief 0.10.0; `test_note_is_generated_from_the_constants_and_disclosures_cover_the_register`).**
1. **The note is generated from the renderer's constants** (`RELATION_MIN_ROOMS`, `DIRECTORY_MIN_ROOMS`, `DIRECTORY_SHARE`) and states the relation predicate the column computes: identity and containment, and only those, between sets of three or more rooms; two sets that overlap without one containing the other are not related here, and the fallback reads "no identity or containment" — the cell's own answer, in the note's own terms.
2. **Every position names its record.** `record_of(predicate)` derives the records a predicate reads from its signals (import graph, clock, test graph, edit record, size) and the position cell prints it — "high-load hub (import graph)", "high edit-pressure node (edit record)", "long-untouched, still-imported room (import graph and clock)". The note's gloss says a position is a place in the record named beside it; the gloss can no longer under-cover the rows.
3. **The stance is the norm's name**: "The diagnosis presupposes a maintenance norm — that the positions it marks are worth a visit — which the reader may reject; it is stated as an ought, not a fact." No fidelity claim, no norm a decorative feature carries.
4. **R17b.** A negated property said of a cited feature's rooms — "not an entrance", "no hub", "not reinforced" — is refused when another feature marks some of those rooms with that property; the prompt says a caveat is never restated as a property of rooms.
5. **R2b.** A citation warrants something in its sentence — the feature named in prose, one of its rooms, or one of its numbers — or it is refused as decoration. **R11** refuses proximity and likeness (near these, nearby, close to, adjacent, alike, likewise, similarly).
6. The four briefs regenerate under 0.10.0 (tag `2026-09-06o`). Rule 5 seats the eighth reader on the generated note.

**Why.** The attack surface inverted, and the fix inverts with it: an over-claiming sentence is closed by a refusal; an under-covering disclosure is closed by generating the disclosure from the thing it discloses, so its scope cannot fall short of the column's. That is the same move as D-040's tests over derived cells and D-042's single source for the rule list — the page's fixed text is either a field, a constant the renderer also uses, or gone. The stance was the last sentence on the page written by neither, and it is now the norm's name and nothing else.

**Assumes.** The note now contains no claim a repository could refute, since every clause in it is a constant or a definition. **Breaks if.** The eighth seating finds the generated note or the record names carrying a claim a repository refutes — then `_RECORDS` is read from the validation document's grounding table rather than hand-listed here; or finds the reading false against the register through a construction neither R17 nor R17b names — then the reading may not name a feature except beside its register relation, verbatim.

## D-044 · 2026-09-09 · The eighth seating: no new layer; the note was generated from the constants and not from the filter beside them; the stop count stays at zero

**Seating.** `hostile-reader` on `reports/2026-09-06o-m3x/mcp-secure-server.brief.md` (brief 0.10.0; the same repository as the fourth seating, for comparison). Tracker run 24. HATER_EXPOSED 87. Eight seatings, zero citation defects. The reading contradicted neither the register nor the sheet through any construction: no denial, no false cause, no paraphrased caveat — every implicature the fourth seating found on this repository is gone.

**The convergence question, answered.** No finding is a new layer. Every substantive finding is an instance of a layer already ruled, or a defect in a rule as written, or both. But the seat does not retire: D-035's count starts when a seating finds *nothing the lint missed*, and two register defects escaped the register's own tests. (1) The generated note says relations are drawn "between sets of three or more rooms", and the overlaps were computed over *diagnostic* features only — so `toothpick_wing`, three rooms wholly inside `crack`, `foundation` and `lit_room`, read "no identity or containment", which the note says "exactly" means none. D-043 generated the note from three constants and not from the filter beside them; a disclosure inherits the constants it is generated from, not the conditions applied next to them. (2) The position column printed "(lexicon)" on `hub` and `lit_room` — D-041's fallback text — in the slot D-043's note promised would hold the record the predicate reads; two decisions' fixed texts collided in one column. Also: "and others under cookbook/api-wrapper-server/src/tools" carried a quantity outside R11's lexicon; "All 21 lit_room rooms sit in src" restated a row that is all of a minority wing through R16's entitlement (D-041(b) named it, D-042 closed it for the largest wing only); "Two of the relations are nestings" was admitted because a wing has two rooms and "relations" was not a unit — the header's declared hole, third instance; the building line's "21 of them fall on the same rooms twice" read as 21 of 287 marks when the number counts *rooms* an identical pair marks twice; and the cross-family characterisation ("the graph marks sit on the same two wings") recurred a third time.

**Decision (brief 0.11.0; `test_register_relations_cover_every_set_and_positions_always_name_a_record`).**
1. **Relations are computed over every feature with three or more rooms, decorative included**, each flagged `diagnostic` when both its features are; the diagnosis's counts (`distinct_room_sets`, the rooms marked twice) come from diagnostic pairs only, and R9/R13's prose obligations concern diagnostic pairs only (a decorative pair is drawn in the register and never discussed, R4). The test asserts that no row printing the fallback has a superset or an equal among the sets the note promises to relate.
2. **Every position cell ends with its record** — "no consequence word in the name (lexicon); a position in the clock" — so the note's promise holds on every row and the two texts no longer collide. The test asserts it.
3. **The identical-pair number is named for its unit**: `rooms_marked_twice` (with its two causes beside it), and the building line reads "identical pairs of diagnostic features mark N rooms twice (…)".
4. **Relation counts join the sheet** (`relation_counts`: identical, within, total) and R12 reads "relations", "nestings" and "identical pairs" as units checked against the register by kind.
5. **R16** refuses a sentence that names a wing holding all of a feature's rooms together with that count — the row read aloud. **R11** gains the "others" family (and others, the others, the rest, the remainder, others under/in).
6. The four briefs regenerate under 0.11.0 (tag `2026-09-06p`). Rule 5 seats the ninth reader where the eighth pointed: at collisions between fixed texts that different decisions wrote into one column.

**Why.** Eight seatings in a three-point band, and the eighth is the first whose findings are all instances or rule defects — the reader said so, and the classification is adopted as the seat's own measure from here. What keeps the seat is not a new layer but the lesson that closed this one: a generated disclosure must be generated from *everything* that produces the column it describes, filters included, and two decisions writing fixed text into one column is now the live surface. Both are the register's, not the model's; the reading held.

**Assumes.** The register's fixed texts are now: the generated note, the position fallbacks, the directory fallbacks, the relation fallback, the building line and the caveats — each either generated from the constants and filters it describes or tested against the rows it sits beside. **Breaks if.** The ninth seating finds two of those texts contradicting each other on one page — then the register's texts are assembled from one table of clauses, each keyed to the branch that prints it, and a test asserts every branch's clause is in the note.

*Note on the record: commit 581f822 carries this decision's code under its message with two test expectations still stale and without this entry — a patch script aborted before them and the commit chained on the test runner's pipe rather than its exit status; both follow in the next commit.*

## D-045 · 2026-09-09 · The paired reading: the reader changed, the page did not; fixes are made as wide as the shape they close

**Instrument change.** Between the eighth seating and this one Alex installed a revised `hostile-reader` definition and prompt-audited it three times in another session. To separate the reader's contribution from the page's, the new definition was seated on the exact page run 24 read (`reports/2026-09-06o-m3x/mcp-secure-server.brief.md`, brief 0.10.0). Tracker run 25, HATER_EXPOSED 87.

**The comparison.** The new reader recovered five of the old reader's six substance findings and missed only the adjacency framing of one it reached from another side. It added five substance findings the old reader did not make on the same page, and two rhetoric items the old reader did not record. Same verdict, same band; more findings of the same kind, and a sharper diagnosis of where the next ones live. Read as an improvement in degree, not in kind — which is what a prompt revision should produce, and what Alex's own agent-iteration record predicts.

**What it added, and what each was.** (1) A directory share stated in prose with no denominator, on a directory the register had suppressed for holding under a third — R15 "met by the register" while the register printed nothing, and R16's entitlement admitting the share without requiring its rooms. (2) A feature wholly inside the largest wing read aloud with its count in the citation: "import_root fires in the cookbook servers [import_root ×17: …]" — D-044's refusal stripped brackets before looking for the number and exempted the largest wing, so the mandatory citation carried what the prose omitted. (3) "The graph marks sit on the same two wings" and "the onboarding marks land on entries and edges" — families no field defines, false of `import_root` and `foundation` on the distributive reading; the fourth page to carry the shape D-040 recorded and declined to rule. (4) "places 7 of its rooms in src and others under …" leaving 31 of 42 rooms unlocated — and the reader's point that D-044's lexicon entry for "others" was a lexical fix to a non-lexical shape, since "7 in src and 4 under X" carries the same implicature with no listed word. Three of the five are defects in fixes shipped by D-042 and D-044: the live surface is not fixed texts colliding (D-044's guess) but fixes narrower than the shape they close.

**Decision (brief 0.12.0; `test_fixes_are_as_wide_as_the_shape_they_close`).**
1. **A wing that holds all of a feature's rooms is that feature's register row**, and naming it beside the feature is refused whether or not the number is in the prose — the citation carries it. The largest wing is not exempt. A feature of one or two rooms has no share; its place is its room's name.
2. **A located subset carries its total** in the sentence: "7 of the 42 sit in src", never "7 in src". Structural, in place of the lexicon entry.
3. **A named directory carries its share and its rooms together** ("6 of the 22 hub rooms sit in cookbook/x/providers, which holds 6 rooms"); a directory the register suppressed is named with both numbers or not at all. R15 is met by the register only where the register printed.
4. **R18** refuses a family of marks the sheet does not define — built from the sheet's own profile and record names ("the graph marks", "the onboarding marks", "the age marks") — the reading names features. D-040 item 4's recorded shape is ruled on its fourth page, structurally, because `record_of` and the profile names now make the family enumerable.
5. The four briefs regenerate under 0.12.0 (tag `2026-09-06q`). The ninth seating proper is seated under the new definition on a page of this version, with the paired reading as its baseline and the definition change on its record.

**Why.** The reader named the pattern in its own findings: each of the last two decisions closed the instance it was shown and not the shape — the largest wing but not the wing that holds everything, the word "others" but not the partial enumeration, the number in the prose but not the number in the bracket. A rule is as wide as the shape it closes or it is the next finding. That is the rule of construction adopted here, and it is the same one D-032 wrote for enforcement ("a default is not a floor") and D-043 wrote for disclosures ("generated from everything that produces the column").

**Assumes.** The revised reader is the instrument from here; runs 17–24 and 25 onward are two series, and D-013 rule 2 compares within each. **Breaks if.** The ninth seating finds a defect in a fix shipped here — then the fix-narrowness class gets a test of its own: every refusal is exercised on the page where it was found *and* on a page where the shape appears in a different form.

**Addendum to D-045 (2026-09-10) — a refusal wider than its shape, found by the pipeline rather than a reader.** Under 0.12.0 two of the four briefs failed R17b on sentences that quote the `import_root` caveat correctly: "import_root reads fan_in and fan_out, not an entrance declaration (§5.5) — a limit on the predicate". D-042 made a caveat a limit on the predicate and put it on the page; D-043's R17b refuses a negated property said of rooms another feature marks; quoting the caveat therefore tripped the rule that exists to stop the caveat being *paraphrased into* a property of rooms. The distinguishing thing is the inference, not the words: the seventh seating's sentence read "reading no fan-in **and so** not an entrance … holds 7 rooms in tools", which infers from the predicate to the rooms; these do not. R17b now skips a sentence that quotes the cited feature's own caveat (three or more of its content words, a field on the sheet) and carries no inference marker (and so, so not, therefore, thus, hence, which makes, making them, so they are). `test_note_is_generated_from_the_constants_and_disclosures_cover_the_register` gains the case and fails against 931fe5e. Recorded because it is the mirror of the same day's lesson: D-045 said a fix must be as wide as the shape it closes, and this one had been wider — the two failures are one rule of construction, a rule is exactly as wide as its shape.

## D-046 · 2026-09-10 · The converse of D-045: a refusal is no wider than its shape, and every refusal is tested on a sentence it must admit

**How it was found.** Not by a reader. The 0.12.0 regeneration failed three of four briefs, and four of the eight refusals on the eslint page were the lint refusing correct prose: (1) two pages quoting the `import_root` caveat as the limit on the predicate that D-042 made it, refused by D-043's R17b, which exists to stop the caveat being *paraphrased into* a property of rooms — the distinguishing thing is the inference ("reading no fan-in **and so** not an entrance"), not the words (logged as the D-045 addendum, fixed in 0.12.1); (2) "the building holds 473 rooms in 8 wings, lib being the largest of them" refused by R11's comparison clause, which D-037 wrote to stop one *mark* being ranked against another — a wing's size is a field on the sheet and comparing wings is not the shape; (3) "their 49 marks render **without** entering the diagnosis" refused by R4, whose disclosure test read a negation lexicon that had `not/no/nothing/never/neither/nor` and not `without`. Each rule was right about its shape and wrong about its width.

**Decision (brief 0.13.0; `test_every_refusal_admits_the_sentence_it_must_admit`).**
1. R11's comparison clause fires only in a sentence that names or cites a feature; a sentence that names none is comparing wings or the building, which the sheet's own fields describe.
2. The decorative disclosure may negate diagnosis with `without`, `outside`, `apart from`, `excluded from`, `excluding` as well as the plain negations.
3. R16's refusal names the number it refused and the feature's wing counts, so a reader of the violation can see whether the sentence confused a wing with a directory — which is what "5 in tools" was on eslint, where the `tools` wing holds 7 `import_root` rooms and the `tools` *directory* holds 5. That refusal was correct and its message was not.
4. **The practice, which is the point of this entry:** every refusal carries, beside the sentence it must refuse, a sentence it must admit. `test_every_refusal_admits_the_sentence_it_must_admit` holds those cases and grows with each rule.

**Why.** D-045 was written yesterday from the paired reading: a fix must be as wide as the shape it closes. Within a day the pipeline produced three instances of the opposite, and the two halves are one rule of construction — a rule is *exactly* as wide as its shape. The asymmetry worth noting is in who finds each half: a hostile reader finds the narrow fix, because a narrow fix leaves an overclaim on the page; only the generator finds the wide one, because a wide refusal leaves no page at all. That makes the regeneration a second instrument beside the seatings, and its failures evidence rather than noise — a brief that fails three times is telling us about the lint, not only about the model.

**Assumes.** The generator is a fair witness to over-refusal: it writes what the register permits and the refusals it hits are the ones a careful writer would hit. **Breaks if.** A regeneration failure turns out to be the model evading a rule rather than obeying one — then the failure is evidence about the model and the admit-test for that rule must be written by hand from the spec, not from the draft the model produced.

**Addendum to D-046 (same day, third round).** The 0.13.0 regeneration refused the prompt's own canonical decorative sentence — "49 decorative marks render but are not a diagnosis [crack ×48; toothpick_wing ×1]" — because R2b asks every citation to warrant something in its sentence and that sentence's whole discipline (R4, R7) is to cite by count and name no feature or room. It also refused a sentence continuing about a feature the sentence before it had named, where the subject is a pronoun. Both admitted now: the decorative disclosure is recognised by its own form (the decorative count with a negated diagnosis), and a citation is warranted by anaphora when the previous sentence in the paragraph named the feature and this one opens with a pronoun. The admit-test gains both, in the prompt's exact wording. The rule this yields, and the one that should have been written yesterday: **the admit-set is seeded from the prompt's own examples** — the prompt tells the model what to write, so a sentence the prompt shows and the lint refuses is a contradiction in the artifact, whichever side is wrong. Three rounds of over-refusal in one day were three sentences the prompt or the register had licensed.

**Addendum to D-046 (fourth round, same day).** eslint's page reduced to one refusal, and it was the morning's own fix: to stop a parent directory being read as a wing I had exempted wing-named parents from the directory entitlement entirely, which made `import_root`'s "5 of the 19 … in tools" unsayable in every phrasing — the wing `tools` holds 7 of its rooms and the parent directory `tools` holds 5, and the prose had no legal way to say either. A ban where a disambiguation was wanted: the register already prints "tools (as parent, not the wing) 5 / 13", and the reading may now say it too, marked ("the tools directory", "as parent", "not the wing") and carrying the population D-045 requires. Unmarked, the refusal names both numbers so the writer can choose. Four rounds of over-refusal in one day, each narrower than the last, all from the same root: a refusal written against the instance that provoked it rather than the shape it belongs to.

**Addendum to D-046 (the fifth round was a bug, not a rule).** The next eslint regeneration produced twenty-seven refusals, and they were one defect: the directory checks read a variable holding the *last* sentence of the paragraph rather than the sentence being checked, so a directory named in any sentence but the last never entitled its numbers, and R16 and R3 then refused figures the prompt had asked for ("24 of its 48 rooms in lib/rules, which holds 293 rooms"). Introduced this morning when the stripped-sentence variable was hoisted into one sentence loop and read from another. Fixed by reading the loop's own value; the admit-test gains a two-sentence paragraph that fails without it. Recorded because it bears on the evidence: an over-refusal counted after that hoist may be this bug rather than a rule too wide, and the four already logged were found before it or on single-sentence paragraphs. The lesson is narrower than a rule of construction — **a lint whose refusals are evidence is tested on a paragraph, not only on a sentence** — and every case in the admit-test now carries a neighbour.

## D-047 · 2026-09-10 · The ninth seating (series 2): a fixed text on the page is tested against the computation it labels

**Seating.** `hostile-reader` (revised definition; baseline run 25) on `reports/2026-09-06s-m3x/typeorm.brief.md` (brief 0.15.0). Tracker run 26. HATER_EXPOSED 87. Nine seatings, zero citation defects; the relations paragraph, the caveat, the stance and the directory cells held.

**Convergence.** No new layer. Seven findings, every one an instance of a ruled layer where the rule as written was too narrow, plus one refusal wider than its shape that a reader found only because the number coincided. The verdict does not turn and the stop count stays at zero, because two register defects and one ruleset text escaped the register's own tests: (1) `rooms_marked_twice` summed the identical pairs' sizes, and on typeorm six rooms sit in both pairs — 129 stated where 123 rooms are marked twice; D-044 named the field for its unit and never tested that it counted it. (2) `scaffolding`'s position name — "test-imported room at or above the median reinforcement", written in D-035 — claims a median: `reinforcement_index >= 0.5` is `test_fan_in > 0` (the index is 0.0 with no test importer, else 0.5 + 0.5·pct), so the predicate marks every test-imported room and computes no quantile. On every page since 0.2.1, past six readers, on the cell whose job is to make a name a position rather than a verdict. (3) A row under the three-room floor printed "no identity or containment" — the computed negative's text — for a set that was not computed and sits inside four others. Also: the header's rule list omitted R18 (D-042's single source ran one way); "Seven wings hold 583 rooms:" listed six and omitted src at 496 (the model generalised R16's "minority wings or nothing" from features to the building); the reading's first two paragraphs recited the register through R16's entitlement, which cannot tell deflation from recitation; and "(root)" could not be named in prose, so its count was entitled only when it coincided with another.

**Decision (brief 0.16.0; maintainability 0.2.2; `test_fixed_texts_are_tested_against_their_computation`).**
1. `rooms_marked_twice` and its two causes count distinct rooms; `rooms_in_both_kinds` says how many the causes share; the building line states all four.
2. `scaffolding`'s position name is "test-imported room" — what the predicate reads. The ruleset loader refuses a position name with a quantile word (median, top, most, decile, quartile, percentile, upper, lower) unless the predicate carries a pNN.
3. The relation cell under the floor reads "too few rooms to relate (n)".
4. R18 is in `RULES`; the test asserts every rule id the lint can emit is described on the page.
5. "(root)" is nameable as "the root" or "the root wing". A sentence announcing N wings and listing fewer is refused; the prompt says the building's wing counts are all sayable and an enumeration names every wing.
6. Recitation (the reading's first paragraphs as the register read aloud) is recorded, not ruled: a refusal would need the sentence to carry something not on the sheet, which is the hole the lint declares. The reader's own line — the reading has stopped overclaiming and started reciting, and the next target is what the reading fails to say — is the question for the tenth seating.

**Why.** Three of the seven were the same defect in three places: a fixed text — a unit gloss, a position name, a fallback string — rendered beside a computation and never tested against it. D-042 and D-044 each wrote half of this rule (enumerate the explanations; name the unit); the reader wrote the whole: a fixed text on the page is tested against the computation it labels, not only rendered beside it. The position-name defect is the one to remember: it sat on the cell that exists to strip a verdict from a name, and six readers looked at it and read the name instead of the predicate.

**Assumes.** The revised reader's series is comparable within itself (runs 25, 26, …). **Breaks if.** The tenth seating finds the reading reciting again with nothing overclaimed and nothing missed — then the prose is cut to the relations, the decorative disclosure and the stance, and D-039's assumption that a reading is what a table cannot do is revisited with the readings as evidence.


## D-048 · 2026-09-10 · The tenth seating (series 2): the reading is bound to what the register does not print; the tokenizer is the scope of every rule

**Seating.** `hostile-reader` (revised definition) on `reports/2026-09-06t-m3x/eslint.brief.md` (brief 0.16.0, PASS on attempt 2). Tracker run 27. HATER_EXPOSED 88. Ten seatings, zero citation defects; the relations paragraph, D-047's fixes, the disclosure and the stance held. Every finding below was re-verified in-session against the code and the sheet before anything was changed; one came back larger than the reader said.

**D-047's question, answered.** (i) The reading recites: ten of fourteen sentences are register cells re-expressed through R16's entitlement; the four that are not — two conjunct diffs, the distinct-set derivation, the stance — are each fieldable. (ii) Nothing is overclaimed. (iii) Two things are missed that no cell carries: a room under its several marks (254 carry two or more; `lib/rules/utils/ast-utils.js` carries five), and the caveat's live case (`bin/eslint.js` is `import_root` and `package_entry` with fan_in 0). Both are writable under the lint as it stood. **The breaks-if does not fire**: its third conjunct fails on evidence. D-039's premise — a reading is what a table cannot do — is not refuted; it is unmet, because nothing bound the reading to do it. Cutting the prose now would resolve on the hypothesis rather than the evidence; the alternative, keeping it as it is, leaves a reading that owes nothing. The false binary is cut-or-keep, and the third option is to bind the one job and measure it.

**Findings, classed.** Three new layers: (1) the sentence tokenizer admitted only a letter after a full stop, so a sentence opening with a digit — the norm the prompt requires — merged into the one before it, and every per-sentence rule (R1, R2b, R3, R15, R16, R17) bound over two sentences whenever a count opened one; on eslint `[hub ×50]` passed R2b because hub's by-wing 1 and 2 sat in "1 identical and 2 within"; on typeorm the same merge produced an over-refusal (R15 read a directory named for hub as named for corridor), which is the D-046 side of the same defect, found by the generator in the same hour. (2) The model's input is a surface: the prompt licensed "lines, fan-in, fan-out" and `_user_message` strips the rooms that carry them. (3) The reading's positive obligations were unbound: the prompt asked, no rule read whether it was done. Three of D-047's class: `flooded_basement`'s "still-imported" labels `load_index >= 0.10`, a floor on a blend (fan_in_nonzero 0.5, centrality 0.3, inverse fan_out 0.1, size 0.1) that a leaf nothing imports clears on inverse fan-out alone — **27 of the 75 rooms on eslint have fan_in 0** (the reader counted seven), including both dark_room exemplars the reading chose; `corridor`'s "high-fan-out" labels p50 beside "high-centrality" at p90; `record_of(load_index)` printed "import graph" and omitted size on four rows. One of D-046's: the prompt's own canonical sentence ("one finding, not two") is refused by R12, and its "every paragraph cites" contradicts the building-only exemption. Plus one over-refusal the regeneration found: "which holds 61 rooms" refused as marks because a directory's population was not in the rooms unit set and 61 was also a mark total.

**Decision (brief 0.17.0; maintainability 0.2.3; onboarding 0.2.2; `test_the_reading_is_bound_to_what_the_register_does_not_print`).**
1. `SENTENCE` splits before a digit. R15 admits a directory's population from the sentence immediately before when that sentence names the same directory (a name anchors the denominator; the split had turned "which holds 22 rooms. 8 of the 27 … sit there" into a refusal on registry's passing page). The second sentence loop computes its own `prev_sent`; the first loop's was stale there.
2. The sheet carries `most_marked_rooms`: the rooms carrying the most diagnostic marks (at most 5, ties by path), each with every diagnostic feature that marks it, marks counted one per feature per profile. `most_marked()` is a function so the test can run it on a sheet the fixture cannot produce. **The register does not print it** — the note says so — and **R19** binds the reading: one of those rooms named under every feature that marks it, each cited with that room. An obligation, met by a sentence whose every element R2 and R8 already check; no number is generated, so R15's failure mode (D-038) has nowhere to recur. The residual risk the reader named, and the one to weigh at the eleventh seating: exemplar selection is still the unruled class (D-036), and a field the register hides is still a field.
3. Position names say what the record reads: `flooded_basement` is "long-untouched room above the load floor"; `corridor` is "high-centrality junction with fan-out at or above the median". The loader refuses a record word in a position name (imported, root, leaf, reinforced, untouched, edit, load, centrality, fan-out, fan-in, package entry) unless the predicate reads a signal of that record, and refuses "high" worn more times than the predicate carries pNN at or above p75 — the shape of both defects, not their phrases (D-045). `_RECORDS` lists `load_index` under size as well as the import graph.
4. The prompt says what the lint does (the building-only paragraph cites nothing; an identical pair is "one set of rooms carrying two marks") and what the model is given (the counts on the sheet; no room's lines, fan-in or fan-out). A directory's share and population are in the rooms unit set.
5. Everything regenerates under 0.17.0 (tag `2026-09-06u`); typeorm and registry had been regenerated under 0.16.0 first (registry passed on attempt 2; typeorm failed three attempts on the two over-refusals above, so its failures were the lint's, not only the model's — D-046's instrument reading correctly again).

**Why.** The findings sit in two places no test read: the texts that *describe* the lint (the prompt, the input message, the tokenizer) and the texts that *label* computations (position names, record names). The prose itself was the cleanest region on the page. The position column is now the recurring seat of the register's defects (D-041, D-044, D-047, D-048): the cell that exists to strip a verdict from a name is where the next verdict lives. And the tokenizer finding is the series' clearest case of a fix being exactly as wide as its shape: the same character class produced a false pass on one repository and a false refusal on another.

**Assumes.** A sentence naming a room under every feature that marks it is a reading and not a recitation of a hidden cell. **Breaks if.** The eleventh seating finds the R19 sentence recited the way R16's entitlement is — a most-marked room read off the field with nothing said about what its marks make of it — with nothing overclaimed and nothing else missed. Then the cut has its evidence: the prose goes to relations, disclosure and stance, and D-039 is revisited with eleven readings. D-035's stop count stays at zero; the eleventh is the first seating that could start it.

## D-049 · 2026-09-10 · The eleventh seating (series 2): the R19 sentence is the field read aloud; the register's closures land, the cut is put to Alex

**Seating.** `hostile-reader` (revised definition) on `reports/2026-09-06u-m3x/eslint.brief.md` (brief 0.17.0, PASS on attempt 2) — the page D-048's breaks-if named, on the repository the tenth read. Tracker run 28. HATER_EXPOSED 89, fourteen substance findings. Eleven seatings, zero citation defects; every number, bracket, derived cell, fallback, record name and both rewritten position names verified by the reader and re-verified here.

**D-048's breaks-if, answered.** Its first conjunct holds, structurally: the R19 sentence is `most_marked_rooms[0]` read aloud — room, mark count, feature list in the sheet's order — with nothing said about what the marks make of the room; and R19, as an obligation on form, cannot tell that sentence from a reading. Recitation rose from 10 of 14 sentences to 16 of 19; every sentence D-048 had credited as non-recitation on the previous page (the conjunct diffs, the distinct-set derivation) collapsed to its cell on this one. Its second and third conjuncts fail: two non-numeric overclaims remain ("The remaining marks sit differently" — a complement family false for import_root and package_entry on the distributive reading, D-045's shape without the family word; "reads no fan-in *as* entrance" — the caveat's sense inverted against its own register row, past R17b's negation lexicon and through the D-045 addendum's bag-of-words exemption), and three things only prose could say are still unsaid (the caveat's four live rooms — import_root ∩ package_entry — the reduction of six names to four predicates through the relation cells, and the tie at the top). **So the breaks-if does not fire as written.** The reader's argument, which I find sound: the two failed conjuncts do not argue for keeping the prose. The overclaims are what the prose costs (D-040: "the reading grew its own"); the misses are unbound fields, and binding a field produces a row — R16 gave by-wing rows, R19 gave a most-marked row. After eleven readings nothing has been named that the sheet cannot field, and the cut "to relations, disclosure and stance" is a cut to three sentences that are already cells (`relation_counts` and the relation column; `decorative_reason`; the `stance` field).

**Register findings, closed here (brief 0.17.1; maintainability 0.2.4; onboarding 0.2.3; `test_a_position_wears_no_feature_name_and_the_most_marked_list_says_when_it_is_cut`).**
1. `foundation`'s position name was "high-load hub": `hub` is a sibling feature (`centrality >= p90`), 12 of 48 foundation rooms on eslint are not hub rooms, and the row's own relation cell recorded no containment. Now "high-load node" in both profiles; the loader refuses a position name that carries any other feature's name in its ruleset, and the test checks across both shipped rulesets. Fourth consecutive seating with a defect in the position column (D-041, D-044, D-047, D-048, now this) — the cell that exists to strip a verdict from a name is where the next verdict lives.
2. `most_marked_rooms` was capped at five with nothing saying whether the cap bit. It bit: **twelve** rooms carry the maximum seven marks on eslint (the reader said five tied; the recount says twelve). The sheet now carries `rooms_at_most_marks`, a rooms number, so a page can say it.
3. The lint list closed with "(D-027 through D-042)" — a literal beside a single-sourced list carrying R18 (D-045) and R19 (D-048). D-047's rule had not reached it. It now says each rule is dated in the log.

**Recorded, not closed, pending the decision below** (they are prose-side or lint-side and moot under a cut): R19 binds a form and refuses a two-sentence reading; R18 refuses an uncited defined family ("the clock marks") and admits its paraphrase; R17b's quote exemption admits any rearrangement of a caveat's words; `attempts_log` keeps rule ids and drops the refused sentence (D-046's evidence discarded on a passing page); the prompt forbids and requires the opening wing inventory; the loader's "high" check reads no operator; the tokenizer splits after "e.g.". The regeneration under 0.2.4 / 0.2.3 that shows the new position name is likewise pending: relinting the 0.17.0 pages would fail R5 on "high-load hub", and regenerating a reading that may be cut is the wrong spend. The tests assert the rulesets; the sheet follows the decision.

**The decision put to Alex (my recommendation first).** The reading has had eleven seatings to be the thing a table cannot do, and each binding turned the thing into a row. I recommend the cut, in this form: the register prints `most_marked_rooms` with `rooms_at_most_marks`, and a pairwise-intersection row for feature pairs that overlap without containment (the caveat's live case is one); the brief is header → register → decorative disclosure → stance, all rendered by code; the model leaves the pipeline; the lint stays for `--draft` (a hand-written reading is still held to it) and the seating series turns into register tests, which is where its yield already is (five of this seating's findings were test-shaped code defects). The cost: M3's defining sentence — "the one stage with a model in the path" — becomes false, and the architect-brief spec and the system spec's C5 lose their generator. The alternative I do not recommend: keep the prose and give it a job that is not a field. Neither the reader nor I could name one that survives D-004 Q3 (no function inferred from a name, no consequence voiced). If Alex names one, it goes in as R20 and the twelfth seating tests it. Either way this is the decision D-039's breaks-if reserved for the readings-as-evidence, and eleven is the evidence.

**Assumes.** The eleven readings are a fair sample of what a model does under this prompt and lint (the prompt was revised at D-048 and the reader was revised at D-045; the recitation trend held across both). **Breaks if.** Alex names a job for the prose that is not a field — then D-039 stands and the seat continues on that job.

**Addendum to D-049 (2026-09-10, the cut; brief 0.18.0).** Alex chose the cut, as recommended, from three options put to him (cut; keep with a job he names; one more seating on a third repository first). What 0.18.0 is: the page is header → register (the feature table; a *Most-marked rooms* table with `rooms_at_most_marks` in its lead so a capped list says it is cut — "12 rooms carry the most … the first 5 by path are listed" on eslint; a *Shared rooms* matrix, `shared_rooms` on the sheet, one cell per pair of diagnostic features with the feature's own count on the diagonal — identity and containment stay the relation column's, and the note says a shared count is not a relation) → *Decorative marks* (the prompt's canonical disclosure sentence, now `render_disclosure` over `decorative_reason`, tested for every feature, position name and signal it must name) → *Stance* (the field, verbatim) → provenance (`generator: code`). No model writes any of it; the header says so and why. `run_brief(skeleton, substrate, draft=None)`: a hand-written draft is linted (R1–R18; R19 is withdrawn — the field it bound the reading to name is a row) and appended as *Reading (draft)* with its lint section, and only then does the page carry a lint status. `relint` reads the reading under `## Reading` (pages before this) or `## Reading (draft)` and passes an unread page. The CLI drops `--model`, `--effort`, `--max-attempts`; `SYSTEM`, `_user_message` and `anthropic_generator` are removed (history at 0d8f5c0), with a comment where they stood. Four pages under tag `2026-09-06v`; 138 tests. What the matrix says on eslint that eleven readings did not: import_root and package_entry share 4 rooms (the caveat's live case), flooded_basement and import_root share none (the load floor removed every import_root room from dark_room's 83), corridor and leaf_utility share none by construction.

**What this changes on the record.** M3's defining sentence — "the one stage with a model in the path" (architect-brief-spec, system spec C5) — is false from 0.18.0; the spec carries a dated revision note and the system spec's C5 is flagged in the handoff for a deliberate rewrite rather than corrected silently. D-013 rule 5 (a version bump seats a hostile reader) continues on the register page: the reader's yield was already there (four consecutive position-column defects; the cap; the literal), and D-035's stop rule applies to it unchanged. The prose-side issues deferred at D-049 (R17b's exemption, R18's lexicon, R19's form, `attempts_log`, the prompt's opening instruction) close as moot; the loader's operator-blind "high" check and the tokenizer's "e.g." split stay open as latent width defects for the draft path. The `prototype/tabular-brief` branch's variant A is, after all, the whole brief — with C's room roll admitted in the most-marked table, and B's dashboard still declined.

**Assumes.** A reader of the brief reads a matrix. **Breaks if.** The twelfth seating, on a 0.18.0 page, finds the matrix or the most-marked table carrying an implicature a cell should not (a column that reads as a ranking; a listed room that reads as chosen) — then those get the register's treatment: a test over the cells and a note that says what the cell is.

## D-050 · 2026-09-11 · The twelfth seating (series 2), the first on a page with no author: the breaks-if fires on the most-marked table and not on the matrix; brief 0.19.0, onboarding 0.2.4

**Seating.** `hostile-reader` on `reports/2026-09-06v-m3x/typeorm.brief.md` (brief 0.18.0, rendered by code; typeorm, a repository the tenth and eleventh did not read), the register only. Tracker run 29. HATER_EXPOSED 90, eight substance findings, three rhetoric, two observations rejected by the reader's own frame-removal test (the matrix's column order as a ranking — it is the feature table's order; two identical rows as an implicature — the relation column states the identity). Twelve seatings, zero citation defects. Every number recounted here against the sheet before anything was changed; the one correction to the reader is its "at least nine rooms at 6" — eight (it named seven and rounded up).

**D-049 addendum's breaks-if, answered.** *"The twelfth seating, on a 0.18.0 page, finds the matrix or the most-marked table carrying an implicature a cell should not (a column that reads as a ranking; a listed room that reads as chosen)."* The matrix: does not fire — every cell licenses what it says; the one defect is in the note beside it (item 6 below). The most-marked table: fires on both parentheticals. (a) *A listed room that reads as chosen*: `src/data-source/DataSource.ts`, the fifth row, is one of **eight** rooms at six marks, first by path, under a lead that says "4 rooms carry the most diagnostic marks (7 …); all are listed". `most_marked` took the top five by (marks, path) whatever the counts; `render_most_marked` disclosed the tie at the top count only; there was no branch for `rooms_at_most_marks < len(top)`, and the test (D-049) covered `==` and `>` — seeded from eslint's twelve-way tie and the fixture's none, not the third case. D-045's class: the fix was narrower than its shape. (b) *A column that reads as a ranking*: the marks column ordered rooms by a unit the header discounts on the same page ("identical pairs of diagnostic features mark 123 rooms twice"); ObjectLiteral's 7 marks are 5 distinct sets, `src/index.ts`'s 7 are 6, and the tier boundary between rows 4 and 5 was the double-count. D-040 corrected the building line to distinct sets and left the room row in marks.

**Closed here (brief 0.19.0; onboarding 0.2.4; 139 tests; pages under tag `2026-09-06w`).**
1. **The most-marked table counts distinct sets** — the unit the header already uses — and is ordered by sets, then marks, then path; columns *room · distinct sets · marks · rooms at this count · features*. Every row carries `rooms_at_this_count` (rooms under two or more marks at its sets count), so a row below the top tier says it is one of many, first by path; `rooms_at_most_marks` becomes `rooms_at_most_sets`; the lead says the ordering, the tie at the most, and what a row below it is. A feature under two profiles is named with its profile in the features column, so names count marks (seven for seven). What the 0.19.0 pages show that 0.18.0 did not: on typeorm `src/index.ts` leads alone at six sets, and the three seven-mark rooms that led before sit at five beside `DataSource.ts` and four others (eight at five); eslint has thirteen rooms at six sets (0.18.0 said twelve at seven marks); registry two at six; mcp-secure-server one. Test: the third case (`at < len(top)`) in `test_the_page_is_rendered_by_code_and_its_fixed_texts_match_their_fields`, on a constructed sheet — every row prints its own count.
2. **"none validated" was a literal** beside a counted "asserted" (`brief.py`, the register note), on the page whose header says every fixed text is tested against its computation; the test asserted the literal was present. `validated` is a real gate status. Both counts are read from the gate; the test validates one signal and reads "1 validated" in the clause that counts the asserted ones.
3. **Two position names wore one conjunct of two** — the fifth consecutive seating with a defect in the position column (D-041, D-044, D-047, D-048, D-049). "import-graph root" named `fan_in == 0` and not `fan_out >= p75`, beside a count of 1 on a sheet with 23 rooms of no fan-in; "imported leaf" named `fan_out == 0 and fan_in > 0` (110 rooms) beside 33. Now "import-graph root with fan-out at or above the upper quartile" and "imported leaf with fan-in at or above the upper quartile". The loader's checks were one-directional: D-047 and D-048 refuse a quantile word or a "high" the predicate does not carry; nothing refused a pNN the name leaves out. The converse is added (`QUANTILE_PHRASES`: a name wears at least as many quantile phrases as its predicate reads pNN terms; "long-untouched" wears p90 on the clock), and the test checks it across both shipped rulesets and on the seating's own case.
4. **The doubled feature was disambiguated in one place of three.** `short()` stripped the profile, so foundation's relation cell read "= foundation"; `most_marked` deduped names across profiles. `qualified_labels` — the matrix's rule — now names a feature under two profiles with its profile wherever it is named: "= onboarding/foundation (same predicate, two profiles)".
5. **The `import_root` caveat cited "§5.5"** — the system spec's *The mapping ruleset as a versioned artifact*, which resolves and carries no fan-in/entrance limit; the limit is D-028 (hostile-reader #3) and D-029. The caveat now cites D-029. D-035, D-038, D-040 and D-042 say "§5.5's caveat" from the same error and stay as written — the log is a record. The reader's one new layer: a citation inside a cell. R2 checks bracket citations in prose; nothing checked a reference in a ruleset text that reaches the page. The check built is stronger than resolution, because §5.5 resolved: a "§n.n" in a position name, caveat or decorative reason names a section whose body speaks of the feature or a signal its predicate reads, and a "D-nnn" a log entry that does (`test_a_citation_in_a_ruleset_text_that_reaches_the_page_cites_a_section_that_speaks_of_the_feature`; it fails on onboarding 0.2.3 with "§5.5 speaks of none of ['fan_in', 'fan_out', 'import_root']").
6. **The matrix's note said containment is drawn in the relation column "and only those"** while `import_root`/`package_entry` printed 1 = `import_root`'s own count — containment the relation column refuses under its three-room floor (D-041, D-047). The note now says what a shared count equal to a diagonal is (containment; equal to both, identity), that the relation column draws those only between features with three or more rooms, and that a pair under the floor is read here and not there. The caveat's live case on typeorm is that pair.
7. **"no cell is a sentence"** beside `toothpick_wing`'s three-sentence `decorative_reason` cell. Now "every cell is a field, a count, or a fixed text over them; no cell is written".

**Convergence.** Seven of eight findings are instances of ruled layers where the rule was narrower than its shape (D-045's class), now on the code-rendered page, and five of them are fixed texts or leads — removing the author moved the overclaim from sentences to the texts around the cells. D-035's stop count stays at zero: the seat found what the register's tests missed. *What only prose could have said*: the reader named nothing that survives D-004 Q3; the gaps it named are fields (per-room distinct sets — now a column). The twelfth piece of evidence for D-039's revisit, and the first from a page with no author.

**Not built.** A per-room "which features are the same set" mark in the features column (the relation column carries it per feature); the matrix on the viewer (handoff item 4).

**Assumes.** Distinct sets is what "most-marked" means — a count of predicates that fire on a room, not of marks that print — and a reader takes "rooms at this count" as the row's own denominator. **Breaks if.** The thirteenth seating finds the distinct-sets column carrying a severity (six sets read as worse than five — a set is a predicate that fired, not a condition, D-004 Q3), or finds the lead's three clauses read as a paragraph rather than three labels — then the column gets a note that says what a set is, and the lead is split into the cells it labels. D-035's stop rule counts on the register unchanged: two consecutive seatings finding nothing the register's tests missed retire the seat.

## D-051 · 2026-09-11 · What M3 is now: answered, not delivered — the brief stays as the register page, C5 rewritten, the milestone closed on the evidence

**The question.** D-003 named M3 "C5 brief via adversarial lens with the register lint" — the optional milestone, and the one stage with a model in the path. D-049 removed the model. Since then the spec's C5 and the milestone sentence described a stage that did not exist, and the handoff flagged it as drift for a deliberate decision rather than a silent correction (D-013; the documentation rule). Put to Alex this morning with three readings: (1) M3 is the register page — C5 becomes "the skeleton as a readable inventory, no narrative"; (2) M3 was tried and closed — its hypothesis (grounded LLM prose adds something over a table) was tested with twelve readings and refuted, the register is a deliverable in its own right, and the spec keeps the narrative section as the record; (3) M3 is still open — the prose gets a job that is not a field (D-049's alternative, which neither the reader nor I could name).

**Decision (Alex, 2026-09-11): 2 with 1's rewrite.** M3 is recorded as *answered* rather than delivered: the thing D-003 named was the model, and the thing that shipped is a table, so calling the table M3 would be the identity drift the rule exists to catch. The brief stays in the pipeline as C5 — the code-rendered register page — and the lint as the contract on a hand-written draft. The system spec's C5 row, its component description, the data-flow sentence, §6 and the §10 milestone line are rewritten to say this in the present tense, with the June draft's §6 paragraph kept inside the new one, marked, because the strata are the record; the architect-brief spec's opening paragraph says the same and leaves its sections and both revision notes as written. `scripts/regenerate_readings.py` keeps its name deliberately (a stratum; it regenerates no reading).

**What "answered" means on the record.** The M3 hypothesis was falsifiable and was falsified on its own terms: the provenance requirement held (no seating breached the citation layer in twelve), so the failure was not that the model lied — it was that every sentence the lint admitted was a cell read aloud, and every binding meant to make the prose say something the register did not print produced a column (R16 → by-wing; R19 → most-marked; the eleventh's three "only prose could say" → the matrix and D-050's sets column). "A reading is what a table cannot do" (D-039) was exhausted, not refuted by argument. The style directives designed beside the narrative (`style.json`: material, era, the visual metaphor for a signal) were never built and stay a direction candidate — the rendering skin (2026-09-08) — with the constraint that a skin changes how a feature looks and nothing about which rooms it marks.

**Assumes.** The twelve readings are the evidence the milestone can be closed on — one prompt lineage (revised D-048), one reader lineage (revised D-045), four repositories, one register that grew to meet each finding. **Breaks if.** Someone names a job for prose over this sheet that is not a field and survives D-004 Q3 — then M3 reopens as a new milestone with that job as its hypothesis, not by reinstating the cut generator; or a reader of the register page asks a question the page cannot answer that the reading once did — the seatings' "what only prose could have said" sections, all empty since D-049, are the standing check.

## D-052 · 2026-09-11 · The thirteenth seating: the lead read as a paragraph, the record beside the entry, and two counts of sets; brief 0.20.0

**Seating.** `hostile-reader` on `reports/2026-09-06w-m3x/registry.brief.md` (brief 0.19.0, rendered by code; uluops-registry-api, seated once before at D-038 under a model-written reading). Tracker run 30. HATER_EXPOSED 90, seven substance findings, seven rhetoric — every rhetoric item rejected by the reader's own frame-removal test, including the one D-050's first clause named. Thirteen seatings, zero citation defects, and on this page zero wrong numbers: every count recounted here, including the 88 co-located rooms by enumeration.

**D-050's breaks-if, answered.** *"The thirteenth seating finds the distinct-sets column carrying a severity (six sets read as worse than five …), or finds the lead's three clauses read as a paragraph rather than three labels."* Clause 1 does not fire: the column's texts are "ordered by" and "most"; a count of positions inherits D-004 Q3 from the positions it counts; the residual carrier (a superlative title over a descending sort) failed frame removal. Clause 2 fires, on the third clause. "A row below that count is one of the rooms at its count, first by path, and its row says how many there are" was true of the list and false as a label on the second and third rows of a tier — `src/routes/v1/schemas.ts` is second by path at five sets, `src/schemas/enums.ts` third. And the tier of four at five sets was listed as three (`src/terminal/styles.ts` cut by the five-row cap) with a 4 beside three rows and no sentence, while the top tier had words. D-049 item 2's disclosure was narrower than its shape; D-050's lead said in a caption what only a cell could say per row.

**Closed here (brief 0.20.0; 141 tests; pages under tag `2026-09-06x`).**
1. **The lead is split into the cell it labelled.** The fourth column is *listed of rooms at this count* — "3 of 4" is three rows listed of four rooms at the row's sets count, the first by path; `listed_at_this_count` is on the sheet beside `rooms_at_this_count`. The lead says the ordering, the cap and the most, and what the column is; nothing in it is a claim about a row. Tested on a tier listed in part ("5 of 6" on every row of a six-room tier under the cap).
2. **The record beside `package_entry` was "import graph".** `is_package_entry` is read from `package.json` (`inventory.py`, D-029); `_RECORDS` filed it under the import graph, and the register note promises "the record its predicate reads". Now "package manifest"; `record_of("is_package_entry == 1")` is tested. The sixth consecutive seating with a defect in the position column (D-041, D-044, D-047, D-048, D-049, D-050): five names and now a record. The loader checks a name against its predicate; nothing checked the record lexicon against the predicate's source, and the test checked that *a* record was named.
3. **"Distinct sets" was two computations.** The header's `n_diag − len(identical)` subtracted overlaps floored at three rooms; the table counted set identity with no floor. They agreed on every shipped sheet and would have disagreed on three identical features or an identical pair under the floor, under one column name. One computation now — `distinct_sets`, `sets_per_room`, `co_located` — used by the header, the co-location count and the table, and tested on a triple under the floor (two sets, not four or one).
4. **Co-location was counted in marks** beside a table the previous decision moved to sets: a room under `foundation` alone carries two marks from one predicate and was "co-located". Latent on registry (88 either way — the reader enumerated it); live on typeorm, where `dark_room` = `flooded_basement` and `foundation` is under two profiles: **109** rooms carry two or more distinct sets, not 160. The header says "distinct diagnostic sets"; the most-marked floor is sets too (a room under one set is not listed). D-044's class, one line below where D-050 stopped.
5. **The page's own citations were outside D-050's citation test**, which walked ruleset fields. A second test renders the page and, for every §n.n and D-nnn on it, requires the target to exist and to share a content word with the sentence that cites it (D-004 Q3, D-049, §5.3 on every page).
6. **System spec §6, as rewritten at D-051, said the row carries "feature → predicate → signal → validation status".** The row carries no status cell; the status is the header's gate clause, and a decorative row names its unvalidated signal in the reason cell. The sentence is corrected and keeps its first form beside it. The reader's new layer: spec prose about the page is not tested against the page — D-047 reaches the page's fixed texts, not texts about the page. Recorded, not built: a test that reads a spec paragraph is the reading's problem in another place, and the standing check is the seat itself.

**Convergence.** No wrong number on the page; the yield is labels, units and captions, and four of seven are D-045's shape (a fix written for the case that fired it: the lead, the tier words, the citation test, the co-location unit). The reader's rival thesis — the register as recitation in a new dress — failed on its own page: the sets column changed who leads on typeorm (D-050) and the sets unit changed a header count by a third (this entry). *What only prose could have said*: nothing that survives D-004 Q3 (the cut room is a row; the entry's fan-in is a field; "backfill scripts" is a function read off a name). D-035's stop count stays at zero: three findings the register's tests missed.

**Assumes.** A cell reading "3 of 4" is read as a fraction of a tier, not as a score; the record lexicon is complete for the signals the shipped rulesets read. **Breaks if.** The fourteenth seating finds a record beside a position that is not the record the predicate reads — then the lexicon is checked against the substrate's signal sources in a test, not by hand; or finds "3 of 4" read as a rank ("third of four") — then the cell is written out in words.

## D-053 · 2026-09-11 · The fourteenth seating: a blend's records come from its declared inputs; the page defines a wing and counts the packages it pools; brief 0.21.0, maintainability 0.2.5

**Seating.** `hostile-reader` on `reports/2026-09-06x-m3x/mcp-secure-server.brief.md` (brief 0.20.0, rendered by code; the interest-rival a maintainer of a multi-package tree deciding where to send a new contributor). Tracker run 31. HATER_EXPOSED 90, seven substance findings, five rhetoric. Fourteen seatings, zero citation defects; every number on the page recounted here (91 co-located rooms by enumeration; the most-marked rows; the matrix). The reader's "14 packages" is by hand; the sheet now counts 15 `package.json` scopes, the root's included.

**D-052's breaks-if, answered.** *"… finds a record beside a position that is not the record the predicate reads — then the lexicon is checked against the substrate's signal sources in a test, not by hand."* Fires, in its weaker form: "high edit-pressure node (edit record)" beside `crack`, and `toothpick_wing`'s four records, omit the clock — `bug_pressure_index` reads recency (inverse `last_touched_days`) at weight 0.2 under this page's gate (`derived.py`; the effective config under `179d8acb7b0c`). D-048 had corrected `load_index`'s record list by hand and left the other four blends; the same omission was latent for `change_pressure_index`, `neglect_index` and `complexity_proxy_index`. *"… or finds '3 of 4' read as a rank"* — does not fire: the column header and three "3 of 3" rows refute the ordinal reading of "1 of 7", and the reader rejected it as rhetoric.

**Closed here (brief 0.21.0; maintainability 0.2.5; 142 tests; pages under tag `2026-09-06y`).**
1. **`_RECORDS` holds raw signals only; a blend's records are derived from `config.ALLOWED_INPUTS`** through a map from each declared input to the raw signal it is read from (`recency` → `last_touched_days`, `inv_fan_out` → `fan_out`, `fix_ratio` → `fix_count` …). `record_of("bug_pressure_index >= p90")` is "clock and edit record"; the test fixes every blend's answer and checks every declared input maps to a record. `crack` reads "high edit-pressure node (clock and edit record)". The seventh consecutive seating with a defect in the position column, the first in the record beside a decorative position.
2. **The most-marked lead said a partly listed tier shows "the first by path"; the order is marks, then path.** On this page the seven rooms at four sets have four and five marks; `google.ts` is first by path and `transaction-server/src/utils/index.ts` was listed. D-052's fixture had uniform marks, so the false clause could not fail there — D-050 item 2's shape (a test asserting the literal). The lead says "the first by marks, then by path"; the test builds a tier with mixed marks.
3. **A containment the predicates guarantee says so.** Five of the six `within` relations on this page hold because the inner predicate conjoins every term of the outer (`corridor ⊂ hub` on every repository); `toothpick_wing ⊂ lit_room` is the one the repository decided. D-037 disclosed identity's inert conjunct and left containment undisclosed. The overlap carries `by_predicate`, and the cell reads "⊂ hub (by its predicate; 4 hub rooms outside this set)".
4. **The page defines a wing and counts the packages its population pools.** The note: "A wing is a directory at depth 1 of the tree (the ruleset's wing_depth), not a package; the population spans 15 package scopes." The calibration sentence: "every pNN ranks the 202 rooms as one population, across 15 package scopes (package.json) pooled — per-package calibration is an open question of the mapper". `wing_depth` and `packages` are on the sheet (from the skeleton's geometry and the substrate's `metrics.package`). Both were new layers: the page assumed the partition was self-evident and one repository was one population; the architect-brief spec had listed monorepo pooling as open (§5 Q5) and no cell carried it. typeorm pools 5 scopes, eslint 21, registry 1.
5. **`flooded_basement`'s limit was in the ruleset's `note` and not its `caveat`**, so it never reached the page; eight of its 37 rooms here have no import edge either way and clear the load floor on inverse fan-out and size. Maintainability 0.2.5 gives it the caveat ("reads a floor on the load blend, not an importer (D-048): a room that nothing imports can clear the floor on inverse fan-out and size alone"), worded as a limit on the predicate (D-042). The citation test holds it to D-048.
6. **System spec §6 counted the seatings** ("twelve, runs 17–29") one behind the log; the sentence no longer carries a count. D-052 item 6's layer, firing on the next page as predicted.

**Convergence.** Numbers clean for the third page running; five of seven findings are D-045's shape (a fix written for the case that fired it: D-048's one blend, D-052's uniform fixture, D-037's identity-only disclosure, D-048's note-not-caveat, D-052's count). The rival's opening on a multi-package tree — one population, wing = top directory — was field-shaped both times and the substrate carried the fields. *What only prose could have said*: nothing that survives D-004 Q3 (test configs as flooded basements is a function read off a filename; the tier's first-by-path room is a row; thirteen servers is the package count, now a field). D-035's stop count stays at zero: the record lexicon, the lead's order on a mixed tier and the note-not-caveat were untested.

**Not built.** Per-package calibration itself (mapper §7 Q6 territory); a check that a limit recorded in a ruleset `note` is also in its `caveat` (a note is the author's, a caveat the page's — the seat found the one that mattered, and a rule over notes would refuse prose).

**Assumes.** `ALLOWED_INPUTS` is the truth about what a blend reads (a tuned weight of zero still names the record — the record list says what the blend is built from, not what this tuning weights). **Breaks if.** The fifteenth seating finds a record named that the tuned weights give zero and reads it as a claim about this repository's numbers — then the list is computed from the effective weights under the page's gate; or finds "by its predicate" read as "by construction, so uninformative" where the outside count is the finding — then the cell leads with the count.

## D-054 · 2026-09-12 · The fifteenth seating: the scope count was over the wrong population; a tie names its partner; a tier orders by path; a signal's reason is the signal's; the citation grammar; brief 0.22.0, maintainability 0.2.6

**Seating.** `hostile-reader` on `reports/2026-09-06y-m3x/eslint.brief.md` (brief 0.21.0; the interest-rival the maintainer of `packages/eslint-config-eslint`, one of the scopes the calibration sentence pools — the sentence D-053 wrote was written for this reader). Tracker run 32. HATER_EXPOSED 87, six substance findings, five rhetoric. Fifteen seatings, zero citation defects on the page; every number the reader relied on recounted here against the sheet (the tie 4/4; the tier 12 at 7 marks and one at 6; 11 zero pairs; the tuned weights 0.5/0.2/0.3).

**D-053's breaks-if, answered.** *"… finds a record named that the tuned weights give zero and reads it as a claim about this repository's numbers"* — does not fire: under `179d8acb7b0c` the blend reads commit_count, recency and revert_count; "clock" and "edit record" each carry a weighted input, and fix history is one signal of the edit record, not the record. The juxtaposition on `toothpick_wing` (position "edit record", reason "zero to fix history") was not the clause; it was item 4 below. *"… or finds 'by its predicate' read as 'by construction, so uninformative'"* — does not fire as substance: the reader read past `dark_room`'s "8 of these rooms outside it" once, restated it as an ordering, and kept it as rhetoric; the count-first remedy is not taken.

**What the reader could not see, and the recount found.** The page said "every pNN ranks the 473 rooms as one population, across 21 package scopes (package.json) pooled." The 473 rooms span **6** scopes. D-053 counted scopes over every substrate node — 1481 on eslint, the test files the population excludes carrying the other 15 — and typeorm (5), registry (1) and mcp-secure-server (15) coincide over rooms and nodes, so the fixture D-053 wrote (`packages >= 1` on a fixture, asserting the literal — D-050 item 2's shape again) could not fail. D-044's shape: the number coincided on three pages of four. The reader's finding was that the count had no distribution (S6); the distribution is what showed the count was wrong.

**Closed here (brief 0.22.0; maintainability 0.2.6; 143 tests; pages under tag `2026-09-06z`).**
1. **`packages` is counted over the population, and the rooms per scope are on the page.** The sheet carries `by_package` (scope → rooms, largest first; `(root)` is the repository's own manifest) and the register note reads "the population spans 6 package scopes (rooms per scope, largest first: (root) 435 · docs 24 · packages/eslint-config-eslint 5 · …)". mcp-secure-server: 66 of 202 in the root and fourteen `cookbook/` scopes holding 136; typeorm: 502 of 583 in the root. The test builds a substrate whose out-of-population nodes carry scopes the rooms do not, and requires the count to be the rooms'.
2. **A tie names every partner with its numbers.** "(tied)" said a tie existed and named one side, chosen by name order (`brief.py` breaks ties on `(n, name)`); on this page `package_entry`'s cell named `packages/eslint-config-eslint 4 / 5` over `lib` 4 — the wing-named parent the note promises to mark was the one left out. `dominant_dir.tied_with` lists the partners; the cell reads "(tied with lib (as parent, not the wing) 4 / 6)". D-040 made a silent tie-break a claim and shipped the word; D-045 item 3 said a directory the register names carries its numbers; the branch that printed "(tied)" ran under no test fixture with `holds_third` true. Instance of a ruled layer, D-040/D-047.
3. **The most-marked tier orders by sets, then path; marks order nothing.** Within a tier of equal sets, two rooms differ in marks only by the double count the register discounts (a set under two profiles, or two features drawing one set): on this page the 13 rooms at six sets are twelve foundation rooms at 7 marks and `packages/js/src/index.js` at 6, and the "then by marks" key D-050 kept as secondary was foundation's two profiles and nothing else. The lead says marks "are shown and order nothing"; the test fixes a tier whose path order and marks order disagree. Instance of D-050's layer: the primary key moved and the secondary stayed on the discounted unit.
4. **A reason about a signal is written once, on the signal.** `crack` (48 rooms) said "bug_pressure_index is unvalidated (D-015)" and `toothpick_wing` (1 room) said what the tuning did to the blend — one signal, the fact on the row with one room. The ruleset carries a `[signal_reason]` table; the loader composes it into every decorative feature that reads the signal ("bug_pressure_index is unvalidated on the pre-registered test set (D-015): its tuned weights assign zero to fix history and it does not beat busyness by the margin."), refuses a non-decorative feature reading a signal the table excuses, and refuses a reason no feature reads. The feature's own `decorative_reason` is now the feature-level remainder (toothpick_wing's "fragility half" sentence; crack has none). The disclosure section says the signal's reason once, in the same words. The tuning claim is tested against `config/tuned.toml`: every fix-history input of `bug_pressure_index` in `ALLOWED_INPUTS` carries weight zero. New layer (the reader's class c): a `decorative_reason` is a feature field, and nothing had said which of its sentences belonged to the feature.
5. **The citation grammar.** The page cited "mapper §7" for per-package calibration; §7 listed six questions and none was it (the brief spec §5 Q5 says "per-component calibration is a mapper question"; the mapper's list did not hold it). The two citation tests (D-050, D-052) matched `§n.n` only — an undotted section was never checked — and resolved a dotted one against three specs concatenated, whichever spec held the first such heading. The tests now share one grammar: an optional spec name (`system spec`, `mapper`, `architect-brief spec`), a section dotted or not, an optional `Qn` item that must exist as a numbered item in the section; a named citation resolves in the named spec only. Mapper §7 gains Q7 (per-package calibration, *open*, assigned by the brief spec's §5 Q5, with the sizes the pages now state), and the page cites "mapper §7 Q7". A third fix of one width on the citation check (D-050 ruleset fields, D-052 page literals, now the grammar) — the reader's own audit note said the third belongs on the grammar, and it does.

**Recorded, not built.** *S4 — the matrix's zeros.* Eleven pairs share no room; two are fixed by the predicates on one signal (`last_touched_days >= p90` against `<= p10`: dark_room/lit_room, flooded_basement/lit_room), two more only if the fan-out quantiles are positive (corridor and import_root against leaf_utility), seven are facts of the repository. D-053 item 3 marks containment the predicates guarantee; the reader asks the same of disjointness. Not built, because the guarantees differ in kind: a conjunction contains its conjunct on every distribution, but `>= p90` and `<= p10` coincide when the signal's deciles tie, and `== 0` against `>= p75` is disjoint only where p75 is positive — a "by its predicate" on a zero would be true on most repositories and false on a degenerate one, which is D-042's shape (a limit on the predicate written as a fact of the page). A zero is a count; the matrix note stays as it is. *The rival's larger claim* — that pooling 435 rooms with a scope of five misranks the five — is mapper §7 Q7 territory and now has its sizes on the page; the scope-level population would fall under the floor (30 rooms, D-034) on every scope but the root on eslint and mcp-secure-server, so per-package calibration is not a switch but a calibration mode §5.3 does not have.

**Convergence.** Five of six findings are D-045's shape (a fix as wide as the case that fired it: D-053's node count, D-040's "(tied)", D-050's secondary key, D-052's dotted regex, D-015's per-feature reason); one is a new layer (item 4). Every finding was in a fixed text, a citation, a tie-break or an ordering key — none in a number, and none in the position column, which resisted for the first time since D-041 (eight seatings). The reader named nothing only prose could have said that survives D-004 Q3: the tie partner, the per-scope count, a by-wing on the tier, a mark on a zero, the tuning line on crack's row are all fields, and three of them are now on the page. D-035's stop count stays at zero: items 1, 2, 4 and 5 were outside the tests as written (an asserted literal, an untested branch, a feature-level field, a regex narrower than the grammar), and item 3 was enforced by a test rather than caught by one.

**Assumes.** The population's scopes are the pooling that matters — a test file's scope is not ranked by any pNN, so it is not pooled. **Breaks if.** The sixteenth seating finds "rooms per scope, largest first" read as a ranking of scopes by importance rather than a size (then the list is by name, as the wings are); or finds a `[signal_reason]` composed onto a row whose predicate reads the signal through a blend that the tuning zeroes — a reason true of the signal and idle on the row (then the reason is composed per effective weight, D-053's unbuilt remedy); or finds the citation grammar's unnamed `§n.n` resolving to the wrong spec on a heading two specs share (then every citation on the page names its spec).

## D-055 · 2026-09-12 · The sixteenth seating: a containment the predicates guarantee is drawn under the floor; the root scope has its own name; a decorative row prints its predicate; the ordering text is one; brief 0.23.0

**Seating.** `hostile-reader` on `reports/2026-09-06z-m3x/typeorm.brief.md` (brief 0.22.0; the interest-rival the maintainer of `src/error/` — 61 one-class error files, finished years ago, 33 of them marked `dark_room` and `flooded_basement`, the directory D-028 named as the case the stance does not fit). Tracker run 33. HATER_EXPOSED 88, three substance findings, six rhetoric. Sixteen seatings, zero citation defects on the page; the reader had no shell and said so, and every number it relied on is recounted here (the root wing 3 and the root scope 502; both `toothpick_wing` rooms in `foundation` and `crack`, 57 outside each; the sheet's `units` text; the five-sets tier in path order).

**D-054's breaks-if, answered.** None of the three clauses fires. *"rooms per scope, largest first" read as a ranking* — constructed and dissolved: "largest" is a size word and the size is beside each name; kept as rhetoric. *A `[signal_reason]` composed onto a row that reads the signal through a zeroed blend* — cannot fire on this ruleset: the loader composes over predicate terms, and `bug_pressure_index` is an input to no blend in `ALLOWED_INPUTS`; both rows that carry the reason read the signal directly at p90. *An unnamed `§n.n` resolving to the wrong spec* — every `§` on the page is spec-named already; the reader noted that `truthful-extractor-spec.md` also has a §5.3 outside the grammar's three-spec roster, so an unnamed one would never see it. The clause's remedy — every citation names its spec — is now a test rather than a state.

**The rival's central attack failed on the substrate.** "33 of 61 long-untouched" could not be read as a verdict on `src/error` from the page's text (the D-004 Q3 sentence precedes the table; the position names the clock; the directory cell carries its denominator), and "above the load floor" is true of every one of the 61: none has `fan_in` 0 — each is imported at least by the barrel. The one flooded room nothing imports on this repository is `src/query-builder/JoinOptions.ts`. The position that had the most to gain from breaking the page's central claim produced resilience, not findings; its three findings are position-independent.

**Closed here (brief 0.23.0; 144 tests; pages under tag `2026-09-06aa`).**
1. **A containment the predicates guarantee is drawn at any count.** `toothpick_wing`'s two rooms sat inside `foundation` and `crack` by conjunction (`load_index >= p90 and bug_pressure_index >= p90 and …`), and the relation cell said "too few rooms to relate (2)". D-041's floor exists because a tiny set is inside things by accident; a containment by predicate is not an accident, and D-053 item 3's form ("by its predicate") stopped at the floor because the overlap loop filtered by size before `_conjoins` was called. Under the floor the loop now emits by-predicate containments only, and the cell reads "⊂ crack (by its predicate; 57 crack rooms outside this set); ⊂ maintainability/foundation (…); ⊂ onboarding/foundation (…); too few rooms for any other relation (2)"; the other side draws "⊃ toothpick_wing (by its predicate; …)" and the counts count it. The note and the matrix note say so. The test that guarded D-053's form admitted the fallback through an `or "too few rooms" in reg` — the reader quoted the `or`; it is gone, and the new test builds a two-room feature that conjoins a four-room one and a two-room feature that does not.
2. **A decorative row prints its predicate.** The column was "predicate or reason", and a decorative row showed the reason; "the fragility half of this feature" was unresolvable from a row that did not show the conjunction it is half of. The cell is "`predicate` — decorative: reason"; the column is "predicate; caveat or reason".
3. **The root scope is "(root manifest)".** The register note said "(root) 3" for the wing and "(root) 502" for the scope in one paragraph, defined the first sense, and left the second to a sheet gloss the page did not render. The scope key is `(root manifest)` and the note says "(root manifest) is the repository's own package.json, not the (root) wing". D-053 item 4 defined the wing when wings got their count; D-054 item 1 gave scopes their count without the same treatment — D-045's shape.
4. **The most-marked ordering is one text.** The sheet's `units.most_marked_rooms` gloss still said "ordered by sets, then marks, then path" and "the first by marks then by path" after D-054 dropped marks from the key, while the lead on the same page said marks order nothing; the only test asserted the key's presence. `MOST_MARKED_ORDER` is one constant rendered into both, and the test requires it in both and "then marks" in neither. D-047's class, D-050 item 2's shape (a fixed text on the sheet, tested for presence).
5. **Every `§` on the page names its spec** — a test, so D-054's third clause cannot fire on a page that passes it.

**Convergence.** Three findings, all D-045's shape: a definition or an ordering change that landed one step from the text it governs (a sheet gloss, a floor, a `units` string). No finding in a number for the fourth page running; none in the position column for the second seating running. *What only prose could have said*: nothing that survives D-004 Q3 — the flooded caveat's live-case count (1 here), the minimum fan-in over a directory's marked rooms, the eighth room at five sets, the toothpick predicate: fields, the last now on the row. D-035's stop count stays at zero: items 1, 3 and 4 were outside the tests as written (an `or` admitting the fallback; a literal asserted; presence not content).

**Recorded, not built.** The flooded caveat's live case as a count on the row ("1 of 70 flooded rooms has no importer") — a field the caveat's bite could be weighed by; it is a matrix cell for `import_root`'s caveat and none for `flooded_basement`'s because `fan_in == 0` is not a feature. Deferred with the same reason as D-054's S4: a count of a caveat's live case on every caveat is a rule over caveats, and the seat found one. The grammar's roster stays at three specs; the page names every spec it cites, and the ruleset test's unnamed resolution over the three is a latent width for a ruleset text citing a fourth.

**Assumes.** A containment by conjunction of terms holds on every distribution (it does: the inner predicate's rooms satisfy every term of the outer's). **Breaks if.** The seventeenth seating finds a by-predicate containment drawn under the floor read as the tiny feature's *finding* — "toothpick_wing is inside crack" taken as this repository's news where it is the ruleset's construction (then the under-floor cell leads with "by construction" and drops the outside count); or finds "(root manifest)" read as a directory (then the scope list names the manifest path, `package.json`); or finds the decorative row's predicate read as a diagnosis because it now looks like the diagnostic rows' (then the ◌ row's predicate is set in the reason's register — "reads `…`" — not in code).

## D-056 · 2026-09-12 · The seventeenth seating: the marker's silence gets a value; a scope is named by its manifest; the sheet's order survives its own serialisation; ◌ travels with the name; brief 0.24.0

**Seating.** `hostile-reader` on `reports/2026-09-06aa-m3x/mcp-secure-server.brief.md` (brief 0.23.0; the interest-rival the author of the three `src/` files under ◌ `toothpick_wing` — `security/config/tool-policies-config.ts`, `security/constants.ts`, `types/server.ts`). Tracker run 34. HATER_EXPOSED 90, four substance findings, five rhetoric. Seventeen seatings, zero citation defects on the page; the reader had no shell and said so; every number it relied on is recounted here (three rooms in `lit_room`, 18 outside; the `cookbook` scope's one room `cookbook/minimal-server/minimal-test-server.ts`; 13 server scopes 135 + 1 = the wing's 136; one identical pair).

**D-055's breaks-if, answered.** Clauses 1 and 2 each fire one cell over from the text they named; clause 3 does not. *A by-predicate containment read as the tiny feature's finding* — on the cell named, no: "by its predicate" is in the parenthesis and the outside count is the repository's. (The clause said "under the floor"; `toothpick_wing` has three rooms here, which is the floor, so the under-floor branch never ran — the clause was written one step narrower than its shape, as the reader noted.) What fired is the cell's neighbour: `⊂ lit_room (18 lit_room rooms outside this set)`, unmarked beside three "by its predicate", read as independent news — a fourth mark agreeing — while `bug_pressure_index` reads `recency`, which is `last_touched_days`, the signal `lit_room` reads. The marker was two-valued and its silence had acquired a meaning the page never assigned. *"(root manifest)" read as a directory* — no; but its neighbour `cookbook 1` beside the wing's `cookbook 136` in the same paragraph was read as a contradiction or as "the cookbook directory outside the servers": D-055 item 3 renamed the root scope and left every other scope keyed by a directory a wing can share. *A decorative row's predicate read as a diagnosis* — constructed and dissolved: ◌, "— decorative:", the reason and the note are on or above the row; the remedy is not taken.

**What the recount found beneath the reader.** The sheet is written with sorted keys (`cli.py`, `sort_keys=True`); `by_package` was a dict ordered largest-first in memory and alphabetical in the file, under a `units` gloss that says "largest first". True of the render, false of the sheet the viewer would read. D-054's shape (a count over the wrong population) one decision later: an order over the wrong object.

**Closed here (brief 0.24.0; 145 tests; pages under tag `2026-09-06ab`).**
1. **A containment not by predicate says what the two predicates read in common.** The overlap carries `shared_signals` — the raw signals both predicates read, a blend expanded through `config.ALLOWED_INPUTS` and `_INPUT_SIGNAL` (the walk D-053 built for the record column) — and the cell reads "⊂ lit_room (reads last_touched_days with it; 18 lit_room rooms outside this set)" or "⊂ scaffolding (no signal in common; 120 scaffolding rooms outside this set)" (registry's `corridor ⊂ scaffolding`, the only other such containment on the four pages). Three values, none silent. Computed from the declared inputs, not the tuned weights, on D-053's assumption (the record list says what a blend is built from). Instance of D-038/D-053: "by its predicate" was a syntactic test with an undefined complement.
2. **A scope is named by its manifest path.** `package.json` for the root, `cookbook/package.json` for the one-room scope, `packages/codemod/package.json` for typeorm's 65. No scope can share a wing's name; the note says "each scope named by the manifest that holds it". D-055 item 3 fixed the root; the shape was every scope (D-045).
3. **`by_package` is an ordered list** of `{scope, rooms}`, largest first, which survives `sort_keys`; the test round-trips the sheet through sorted keys and requires the order intact.
4. **◌ travels with the name.** Four relation cells on diagnostic rows named `toothpick_wing` without the marker the note defines as "excluded from the diagnosis"; the fixture test had fixed the unmarked form (D-050 item 2's shape). `short()` prepends ◌ to a decorative feature wherever a cell names it; the count phrase ("18 crack rooms outside this set") stays plain. Instance of D-050 item 4: a qualifier travels with the name.
5. **Containers that are one set are one entry.** `toothpick_wing`'s cell listed four containers, two of them `foundation` under two profiles — the marks unit beside a header that counts in sets. The cell reads "⊂ maintainability/foundation = onboarding/foundation (by its predicate; 18 maintainability/foundation rooms outside this set)". Instance of D-050/D-052's unit discipline.

**The shape question, asked of the reader and answered.** Three of four findings are the sixteenth's shape — a definition or unit landing one step from the text it governs (the root scope renamed and not the others; ◌ defined in the note and printed in one column; the sets unit adopted for the header and the table and not the relation list) — and the fifth, found by recount, is the same shape on the serialisation seam. One is different: a two-valued marker whose silent value was never defined, so the silence acquired a meaning. That is a classification with an undefined complement — the shape D-054 declined to build for the matrix zeros — and the fix is a new value, not a moved definition. So the evidence for re-reading D-035 is: three seatings running, every finding but one has been the seam between a change and the text one step from it, and the breaks-if clauses themselves have twice been written one step too narrow (D-055's clause 1 named the under-floor branch; the page's cell was at the floor). That seam is a lint's job. The one finding that was not the seam is the kind a reader finds and a lint does not.

**Convergence.** No finding in a number for the fifth page running; none in the position column for the third seating running. The rival's central attack — the three files read as fragile, insecure, or where the bugs are — failed: no ◌ room is named on the page, no consequence word survives on any row, and the ◌ row carries its position, its records, its predicate, its reason and the note's definition. *What only prose could have said*: nothing that survives D-004 Q3 (the shared signal is now a field; `revert_count` constant on this population is a flag under the gate, not built; the three rooms being config/constants/types is a function read off a name). D-035's stop count stays at zero: items 1, 2, 4 and 5 were outside the tests as written and item 3 was invisible to a test that reads the in-memory sheet.

**Recorded, not built.** A flag for a blend input constant on the population under the gate (`revert_count` is 0 on all 202 rooms here, so "edit pressure" ranks by commit count and recency) — the reader's R2; D-053 decided the record list says what a blend is built from, and a per-population constancy flag is a different field, deferred.

**Assumes.** The shared-signal walk over declared inputs is the right unit for "reads with it" — a weight of zero on the shared input would still name it (D-053's assumption, restated). **Breaks if.** The eighteenth seating finds "reads last_touched_days with it" read as the containment's *cause* — the three rooms are in `lit_room` *because* the blend reads the clock — where a 0.2 weight guarantees nothing (then the value reads "the predicates share a signal, last_touched_days; the containment is the repository's"); or finds the seam shape again with nothing else — then D-035 is re-read as this entry proposes and the seat's standing rule is put to Alex with the seventeen readings as the evidence.

## D-057 · 2026-09-12 · The eighteenth seating: the seat is not seam-only; a feature that fired on nothing keeps its row; the marker's values are defined on the page; the import graph and the test graph are one edge set, said; brief 0.25.0, maintainability 0.2.7

**Seating.** `hostile-reader` on `reports/2026-09-06ab-m3x/registry.brief.md` (brief 0.24.0; the interest-rival the lead for test strategy on uluops-registry-api, whose standing rises if the test-graph column reads as a coverage claim). Tracker run 35. HATER_EXPOSED 91, five substance findings, six rhetoric. Eighteen seatings, zero citation defects on the page; the reader had no shell and said so; every number recounted here with one (corridor 13 ⊂ scaffolding 133, 120 outside; 262 in-edges to the 13 corridor rooms, 58 from files under the test convention; `toothpick_wing` = crack ∩ foundation ∩ {test_fan_in = 0} = ∅, the 7 crack∩foundation rooms all test-imported; two scaffolding rooms whose only test-convention importer is a helper under `test/integration/helpers/`; PageRank over all 456 nodes, 189 of them test files).

**D-056's breaks-if, answered — and the D-035 question with it.** *Clause 1* ("reads last_touched_days with it" read as the containment's cause) — not on this page; its complement fired: `⊂ scaffolding (no signal in common; 120 …)` read as *independent measurements agreeing* — the corridors are covered, two records say so — where the sheet supports only that no raw-signal *name* is shared, and the substrate shows one instrument and one edge set: a test file is a node in the graph centrality is computed on, so a test import raises `centrality` and `test_fan_in` together. D-056's own remedy sentence ("the containment is the repository's") is the half-truth this reader already takes. *Clause 2* ("the seam shape again with nothing else — then D-035 is re-read and the seat's standing rule goes to Alex") — **does not fire.** Finding by finding: the marker undefined on the page and `reinforcement_index` unexpanded — seam; the 0.5 floor in a ruleset comment and not the caveat — seam (D-053 item 5's exact shape, the second note-not-caveat); "test-imported" including helpers and fixtures — **not the seam** (no definition anywhere landed wrong; a limit on the predicate nobody had written, found by reading edges); the unfired feature with no row — **not the seam** (an absent row; the sheet was built from firings and carried no roster); the ruleset version absent from the header — an omission, not a seam. Two of five, and the central finding is the kind a reader finds and a lint does not. The reader was asked to be exact and not to round toward either pole, and it did not. **So the standing rule stays, on evidence: three seatings of mostly-seam findings were followed by one where the seat found what only a reader who asks "which file is the test?" finds.** The seam findings keep coming and are a lint's job; the log will carry a running count from here (D-056: 4 seam of 5 incl. the recount's; D-057: 2 of 5), and the rule is re-read when a seating is seam-only, as D-056 wrote.

**The rival's central attack failed on the page's words and landed one layer under them.** "Test-imported room" is the predicate's own word (D-047) and no consequence word is the page's; but the predicate it names is a path-convention filter over the same edge set the import-graph positions read, and the page said neither.

**Closed here (brief 0.25.0; maintainability 0.2.7; 146 tests; pages under tag `2026-09-06ac`).**
1. **A feature that fired on no room keeps its row at 0.** The skeleton carries a roster of every feature each ruleset defines (`profile.roster`, `overlays[].roster`, from the engine); the sheet builds its rows from the roster and the firings, and an unfired feature renders "| 0 | no wing (0) | no rooms | no rooms to relate (0) | `predicate` — … |". The header's decorative list carries counts ("crack 27, toothpick_wing 0"); the disclosure says "toothpick_wing fired on no room." The ruleset's own note had said the feature is kept on the page so its absence is visible; the page could not say it. New layer. An older skeleton without a roster renders the fired set as before.
2. **The marker's values are defined on the page,** in the note beside the "at any count" clause: "by its predicate" when the inner conjoins the outer's terms; otherwise the raw signals both read, a blend or index expanded through its declared inputs, or "no signal in common" — *a signal, not an instrument.* `_signals_read` expands a derived index outside the tuned blends through `validation.config.GROUNDING` (`reinforcement_index` → `test_fan_in`), not by hand. D-056's value had a definition in a docstring and the log.
3. **The import graph and the test graph are one edge set read twice** — the note says so: a test file is a node whose imports count in `fan_in` and `centrality`, and `test_fan_in` counts those importers alone. A fixed text held to the substrate by the invariant it states (`fan_in >= test_fan_in` on every node). This is the sentence that makes "no signal in common" unable to mean "no instrument in common".
4. **`scaffolding` carries the caveat its siblings got one seating at a time** (maintainability 0.2.7): "reads whether any file under the test convention imports the room (test_fan_in > 0, D-011): the index is 0 with no test importer and 0.5 or more with one, so 0.5 is its floor, not a midpoint; a helper or fixture under the test paths counts as a test importer, and one import suffices." Worded as a limit on the predicate (D-042); the test holds every `reinforcement_index` on the fixture to 0 or ≥ 0.5. The third floor-on-an-index predicate; a rule over "a predicate that reads a derived index at a literal" would have closed all three at once (D-045) — recorded, not built: the three are now closed, and the rule would refuse the next literal threshold before a reader has read it.
5. **Ruleset versions in the header** ("Profile maintainability 0.2.7 + onboarding 0.2.4"; `profile_versions` on the sheet). Instance of D-035 item 3.
6. Three fixed texts corrected: "1 of these rooms outside it" (the partitive stays plural); "rests on" when one decorative feature fired; "too few rooms to relate (n)" where nothing is drawn under the floor and "…; too few rooms for any other relation (n)" only after something is — "other" needs an antecedent (the reader's R4 was enforced by a test, not caught).

**Convergence.** No finding in a number for the sixth page running; none in the position column for the fourth seating running. *What only prose could have said*: nothing that survives D-004 Q3 — the helper-only rooms are a count given a helper/test-case split in config; the test share of a feature's in-edges is a cell; the unfired feature is a row; the version is a field; the marker's definition is a sentence generated from the constant the cell uses. D-035's stop count stays at zero: none of the five was caught by a test as written.

**Recorded, not built.** The helper/test-case split as a config and a count ("rooms whose only test importers are helpers") — the caveat states the limit; the count is a field for when the split is a config, not a path glob. A "test share of in-edges" cell per feature (58 of 262 on corridor). The rule over literal thresholds on derived indices (above).

**Assumes.** A caveat on the predicate is where a path convention's reach belongs (D-042), and the note is where a shared instrument belongs — the first is the ruleset's limit, the second the substrate's. **Breaks if.** The nineteenth seating finds the one-edge-set sentence read as "so the import-graph positions are test artefacts" — corridor's centrality inflated by tests, the mark dismissed — where 58 of 262 is a share, not a cause (then the share is a cell per feature and the sentence points at it); or finds a zero row read as a claim that the repository lacks the thing ("no toothpick wings here") where a decorative feature's zero is the tuned blend's, not the repository's (then the zero row's reason leads); or is seam-only — then D-035 is re-read as D-056 proposed.

## D-058 · 2026-09-12 · Forecast cycle 3: the register is clean and the practice around it has no stop, no miss and no null result — the findings that touch the series' identity are put to Alex

**Roster and rationale (D-013 rule 2; scores against runs 13 and 15).** The pair the handoff queued at D-049: `cascade-depth-analyzer` on what the *practice* cascades into over 20–50 seatings and into Phase 3; `perverse-outcome-detector` (cycle 1's lens, on a system whose criteria have since changed) on what the practice now optimizes against. Artifact: `docs/breaks-if-ledger-2026-09-12.md` — every Assumes/Breaks-if D-034 → D-057, extracted verbatim — with D-045 → D-057, the handoff, the registry page 0.25.0 and the code. Tracker run 36. SHALLOW 84 (the chains are mapped, not absent), PERVERSE 84 (one critical). Both reports note they are inputs to the loop they describe and that this entry is written by the loop's operator; stated once here too.

**Convergence — four findings both lenses reached independently, each verified here against the file.**
1. **D-035's stop rule cannot fire, and its second horn has fired on every seating.** D-035's clause has two horns: *"two consecutive readings find nothing the lint missed — then the seat is ceremony"* and *"a reading finds something on every brief — then the lint's authority is the ceremony and D-028's 'the lint is the hostile reader' is revisited."* Eight consecutive entries (D-050 → D-057) print "D-035's stop count stays at zero" — horn one — and no entry records horn two, which has fired eighteen of eighteen (on the code-rendered page, transposed to the register's tests: eight of eight). The transposition itself (lint → tests, D-050) was never a decision. And the tests cannot miss or catch a page: `tests/` reads nothing under `reports/` (zero references); every test runs on a six-file scripted fixture, so *every* page-level finding is "something the tests missed" by construction. The running sentence is D-035's own finding shape — a check performed once and thereafter presented as performed — inside the entry that introduced the rule. (Cascade C3, EPI-SCP/H; Perverse P1, PRA-ALI/C.)
2. **The seat is pointed at the clause, the clause is written to the newest cell, and the seam rule is administered by its subject.** Three consecutive rivals were cast to the previous decision's breaks-if (D-054's "the sentence D-053 wrote was written for this reader"; D-057's test lead for D-056's test-graph clause); the clauses name the cell just built with the remedy attached; the answer grammar admits no failure (fired = confirmed; not fired = held; one cell over = "D-045's shape"; complement = "the clause's own half-truth"). The seam classification that decides whether the rule is re-read is three-valued at first use ("an omission, not a seam", D-057) and made by the agent whose rule is at stake; the retire trigger is *seam-only*, which one non-seam finding per seating defeats. The seam pattern is partly an artefact of where the seat is pointed. (Cascade C1 and CV1 — a feedback loop with two attractors; Perverse P3, P4.)
3. **The persona's pole flips on one finding, and the findings have migrated to implicature.** The hostile-reader definition selects HATER_EXPOSED on *one or more* traceable substance findings (`~/.claude/agents/hostile-reader-agent.md`: "One or more → HATER_EXPOSED … the count decides"); this cycle's own prompt assumed three — a premise error, recorded. A reader penalised for miscounts, without a shell, whose pole flips on one hit, rationally makes reading claims ("X read as Y") that no recount refutes: "no finding in a number for the sixth page running" while substance counts hold at 3–7, and twice the reader constructed the predicted misreading and dissolved it itself. The reported 87–91 is the reader's self-score of its reading. And with no shell, the number findings of the last four seatings were the parent's recounts logged under the seating's name (the 473-vs-1481 scope count; `by_package` alphabetical in the file). (Perverse P2, P10; cascade C1 depth 3.)
4. **"Every fixed text on the page is tested against the computation it labels" is an overclaim, on line 3 of every page.** A definition has no computation; the only available test is presence, and 23 assertions in `test_brief.py` assert a literal of forty characters or more against the render — the same agent moves both sides in one patch. The same test-shape defect has recurred four times in seven decisions (D-050 item 2, D-054's `packages >= 1`, D-055's `or`, D-056's fixture fixing the unmarked form). The header sentence is spec prose about the tests that no test reads — D-052 item 6's layer. (Cascade C2, EPI-VAL/M; Perverse P5, PRA-TST/H.)

**Findings one lens reached.** *D-004 Q3 is analytic* (P6, SEM-LOG/H): the field-or-condition partition is exhaustive, so eighteen "nothing survives Q3" answers are one theorem restated; what can never survive is a cause, a comparison to another repository, or a recommendation — which is what every cast rival wanted and what Phase 3's corpus-relative output *is*. M3 was recorded as answered (D-051) on evidence that could not have come out otherwise. *The skimmer* (C5, P7): eighteen readers who recount and cite have trained the page to keep its defences in a ~480-word italic note the sixty-second reader skips, then read the table as a ranking; no seating can detect this because every seating recounts. *Phase 3* (C6): a diagnostic feature at zero renders "no rooms to relate (0)" with nothing saying whether the zero is the repository's or the instrument's (the "fired on no room" sentence exists for decorative features only); a corpus page whose tests sit outside the path convention has `scaffolding` at 0 and reads "untested"; the seat lands per version bump on the four reference pages and never on a corpus page, so the most-seated pages become the least representative. *The log as build input* (C7): two tests resolve `D-nnn` citations against `DECISIONS.md`, now 44,600 words — larger than one read; the ledger extracted for this cycle is what fits, and clauses written from clauses narrow further. *Caveats and lexicons* (C4, P8, P9): one caveat per seating; a caveat that cannot be false costs nothing and every predicate will get one; the loader's lexicons are closed phrase lists and the fifth ruleset author names around them (`bottleneck`, `orphan`, "most-loaded node"); the operator-blind "high" check is still open from D-049.

**Closed here, on delegated authority — the mechanical part only.**
1. **The header no longer overclaims** (brief 0.25.1): "every fixed text on the page has a test that asserts it against the sheet it is rendered from (`tests/test_brief.py`); the tests run on a fixture, not on this page." A fixed text that said more than its computation, narrowed to what it is (D-042's rule applied to the sentence that states the rule).
2. **Horn two is on the record**: eighteen of eighteen. The running "stop count stays at zero" sentence is retired as a claim; from here an entry says what the tests could not have caught (nothing on a page, by construction) rather than counting toward a rule that cannot fire.
3. **The seating the version bump owes under D-013 rule 5 is held**, explicitly, until Alex decides item 1 below — a bump without a seat is recorded here rather than done silently.

**Put to Alex — decisions about the series' identity, not made on point (the rule this log wrote for itself at D-049).** Recommendation first, then the options, each with what it costs.
1. *The stop rule and the seam rule.* Recommendation: retire D-035's clause as written (it cannot fire; its counter is held by its subject) and replace it with one **control seating** — a reader on an *unchanged* page (registry 0.25.1, no version bump, no breaks-if pointer, a rival cast away from any recent clause, with a shell) — whose result is read against D-057's: if the control finds the same kind of thing, the seat's yield is the page's; if it finds only what the last clause pointed at, the yield was the pointing. Alternatives: keep the rule and hand its counter to a lens that is not the seat's operator (a second agent classifying seam / not-seam blind to the decision text); or end the series at eighteen with the register as it stands. The recommendation costs one seating; the second alternative costs an agent per seating forever; the third costs the yield D-057 showed is still real.
2. *The clause form.* Recommendation: a breaks-if names a *shape* the next reader is not told about — the seating prompt stops quoting the clause and stops casting to it; the clause is answered after the reading, by the parent, against a reading that did not know it. Alternative: keep quoting it and accept the ledger as a regression log with hypotheses attached.
3. *Q3 as solvent and Phase 3's sentence type.* Not a fix; a question for the mapper spec: what is the admissible form of a corpus-relative claim ("this repository's dark-room share is in the top decile of the corpus") — a field with a corpus denominator, which Q3 admits, or a comparison, which it does not. Recommendation: decide before the corpus runs, because every corpus finding will be one or the other.
4. *The skimmer.* Recommendation: one seating cast as the sixty-second reader — no recount, no citations, no log — as the control's twin; it is the only reader the practice has never modelled and the first one the page will meet.

**Recorded, not built.** A golden test over the four real sheets (render on disk equals render from the sheet) — it would make the tests see the page and would not make them catch what a reader catches; declined here so it does not read as an answer to item 1 above. The diagnostic zero row's reason (C6) — a `fired on no room` sentence for diagnostic features, pending item 3's answer on what a corpus zero means. The loader's operator-blind "high" (D-049, still open).

**Why.** Cycles 1 and 2 found mechanical and locational gaps and each became four decisions. This cycle found that the register — every number, every position name, every caveat — is in good order on its own terms, and that the practice which produced that order has become unfalsifiable at every joint: a rule that cannot fire, a persona that cannot return null, a clause that cannot fail, a disposal rule that is a theorem, all operated by one agent. The right response is not four more structures written by that agent; it is the decision about who counts, which this log assigned to Alex when it removed the model from the page.

**Assumes.** A control seating on an unchanged page distinguishes the page's yield from the pointing's. **Breaks if.** The control seating finds the seam shape at the same rate as the pointed seatings — then the pointing was not the cause and the seam pattern is the page's; or it finds nothing (STEELMAN_RESILIENT, the first in eighteen) — then the seat's yield since D-053 was the pointing, and the series' record from there is re-read.
